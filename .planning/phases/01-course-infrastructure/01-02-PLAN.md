---
phase: 01-course-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - course-content/docusaurus.config.ts
  - course-content/sidebars.ts
  - course-content/package.json
  - course-content/src/css/custom.css
  - course-content/docs/index.md
  - course-content/docs/setup/prerequisites.md
  - course-content/docs/setup/preflight.md
  - course-content/docs/labs/lab-00-cluster-setup.md
  - course-content/docs/labs/lab-01-synthetic-data.md
  - course-content/docs/labs/lab-02-rag-retriever.md
  - course-content/docs/labs/lab-03-finetuning.md
  - course-content/docs/labs/lab-04-model-packaging.md
  - course-content/docs/labs/lab-05-model-serving.md
  - course-content/docs/labs/lab-06-web-ui.md
  - course-content/docs/labs/lab-07-agent-core.md
  - course-content/docs/labs/lab-08-agent-sandbox.md
  - course-content/docs/labs/lab-09-observability.md
  - course-content/docs/labs/lab-10-autoscaling.md
  - course-content/docs/labs/lab-11-gitops.md
  - course-content/docs/labs/lab-12-pipelines.md
  - course-content/docs/labs/lab-13-capstone.md
  - course-content/docs/reference/troubleshooting.md
  - course-content/docs/reference/cleanup.md
autonomous: true
requirements:
  - INFRA-02

must_haves:
  truths:
    - "Docusaurus site builds successfully (npm run build exits 0)"
    - "Single sequential sidebar lists Setup group then Labs group (lab-00 through lab-13)"
    - "Dark mode is the default; light mode toggle is available"
    - "All 14 lab pages exist as placeholder MDX files that build without errors"
    - "Docusaurus tabs pattern is used in at least one lab page for OS-specific commands (per D-06)"
  artifacts:
    - path: "course-content/docusaurus.config.ts"
      provides: "Docusaurus 3.10.0 site configuration"
      contains: "title: 'LLMOps & AgentOps with Kubernetes'"
    - path: "course-content/sidebars.ts"
      provides: "Single courseSidebar with Setup + Labs categories"
      exports: ["default"]
    - path: "course-content/docs/labs/lab-00-cluster-setup.md"
      provides: "Lab 00 placeholder page"
    - path: "course-content/src/css/custom.css"
      provides: "Kubernetes.io-like visual overrides"
  key_links:
    - from: "course-content/sidebars.ts"
      to: "course-content/docs/labs/lab-00-cluster-setup.md"
      via: "sidebar item ID 'labs/lab-00-cluster-setup'"
      pattern: "lab-00-cluster-setup"
    - from: "course-content/docusaurus.config.ts"
      to: "course-content/sidebars.ts"
      via: "preset-classic docs sidebarPath"
      pattern: "sidebarPath.*sidebars"
---

<objective>
Scaffold the Docusaurus 3.10.0 course documentation site with full configuration, single-path sequential sidebar, dark-mode-first theme, and placeholder MDX pages for all 14 labs.

Purpose: This is the course site students navigate. It must build cleanly and present a professional Kubernetes.io-like visual experience from the first commit, so later phases can add lab content directly.
Output: course-content/ Docusaurus project that builds and serves locally, with all 14 lab placeholder pages wired into the sidebar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-course-infrastructure/01-CONTEXT.md
@.planning/phases/01-course-infrastructure/01-RESEARCH.md

<interfaces>
<!-- Docusaurus patterns from RESEARCH.md — executor must follow these exactly -->

sidebars.ts pattern (Pattern 1 from RESEARCH.md):
```typescript
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';
const sidebars: SidebarsConfig = {
  courseSidebar: [
    { type: 'category', label: 'Setup', items: ['setup/prerequisites', 'setup/preflight'] },
    { type: 'category', label: 'Labs', items: [
      'labs/lab-00-cluster-setup',
      'labs/lab-01-synthetic-data',
      // ... through lab-13-capstone
    ]},
  ],
};
export default sidebars;
```

Tabs pattern (Pattern 2 from RESEARCH.md — MUST be used in lab-00 placeholder):
```mdx
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs groupId="operating-systems">
  <TabItem value="macos" label="macOS / Linux">
    ```bash
    bash scripts/preflight-check.sh
    ```
  </TabItem>
  <TabItem value="windows" label="Windows">
    ```powershell
    .\scripts\preflight-check.ps1
    ```
  </TabItem>
</Tabs>
```

docusaurus.config.ts key values (from RESEARCH.md Code Examples section):
- title: 'LLMOps & AgentOps with Kubernetes'
- tagline: 'From RAG to production agents on Kubernetes'
- url: 'https://llmops.schoolofdevops.com'
- organizationName: 'schoolofdevops'
- projectName: '302-llmops'
- colorMode.defaultMode: 'dark'
- onBrokenLinks: 'throw'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Docusaurus 3.10.0 site and configure it</name>
  <read_first>
    - .planning/phases/01-course-infrastructure/01-CONTEXT.md (decisions D-04, D-05, D-06)
    - .planning/phases/01-course-infrastructure/01-RESEARCH.md (Standard Stack section, Architecture Patterns section, Code Examples — docusaurus.config.ts)
    - llmops-labuide/mkdocs.yml (reference for navigation structure — do NOT copy, use as reference only)
  </read_first>
  <files>
    course-content/docusaurus.config.ts
    course-content/sidebars.ts
    course-content/package.json
    course-content/src/css/custom.css
  </files>
  <action>
    Run Docusaurus scaffold from the project root /Users/gshah/courses/llmops/:

    ```bash
    npx create-docusaurus@3.10.0 course-content classic --typescript
    ```

    After scaffolding completes, replace the generated files with course-specific content:

    **Replace course-content/docusaurus.config.ts** with exact values:
    - title: 'LLMOps & AgentOps with Kubernetes'
    - tagline: 'From RAG to production agents on Kubernetes'
    - url: 'https://llmops.schoolofdevops.com'
    - baseUrl: '/'
    - onBrokenLinks: 'throw'
    - onBrokenMarkdownLinks: 'warn'
    - organizationName: 'schoolofdevops'
    - projectName: '302-llmops'
    - In themeConfig: colorMode: { defaultMode: 'dark', disableSwitch: false, respectPrefersColorScheme: false }
    - In navbar: title 'LLMOps' with items: [{ label: 'Labs', to: '/docs/intro' }, { label: 'GitHub', href: 'https://github.com/schoolofdevops/302-llmops', position: 'right' }]
    - Keep the classic preset with docs and blog (disable blog by setting blog: false in presets)
    - sidebarPath: require.resolve('./sidebars.ts')  (or import via './sidebars')

    **Replace course-content/sidebars.ts** with the exact pattern from RESEARCH.md Pattern 1:
    ```typescript
    import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

    const sidebars: SidebarsConfig = {
      courseSidebar: [
        {
          type: 'category',
          label: 'Setup',
          collapsed: false,
          items: ['setup/prerequisites', 'setup/preflight'],
        },
        {
          type: 'category',
          label: 'Labs',
          collapsed: false,
          items: [
            'labs/lab-00-cluster-setup',
            'labs/lab-01-synthetic-data',
            'labs/lab-02-rag-retriever',
            'labs/lab-03-finetuning',
            'labs/lab-04-model-packaging',
            'labs/lab-05-model-serving',
            'labs/lab-06-web-ui',
            'labs/lab-07-agent-core',
            'labs/lab-08-agent-sandbox',
            'labs/lab-09-observability',
            'labs/lab-10-autoscaling',
            'labs/lab-11-gitops',
            'labs/lab-12-pipelines',
            'labs/lab-13-capstone',
          ],
        },
      ],
    };

    export default sidebars;
    ```

    **Replace course-content/src/css/custom.css** with Kubernetes.io-like overrides:
    ```css
    /* Kubernetes.io-inspired theme overrides */
    :root {
      --ifm-color-primary: #326ce5;
      --ifm-color-primary-dark: #1e5fd4;
      --ifm-color-primary-darker: #1a58c8;
      --ifm-color-primary-darkest: #1448a5;
      --ifm-color-primary-light: #4a7de8;
      --ifm-color-primary-lighter: #5888eb;
      --ifm-color-primary-lightest: #7ea2f0;
      --ifm-code-font-size: 90%;
      --docusaurus-highlighted-code-line-bg: rgba(0, 0, 255, 0.1);
    }

    [data-theme='dark'] {
      --ifm-color-primary: #4a86e8;
      --ifm-color-primary-dark: #2d72e4;
      --ifm-color-primary-darker: #2069e0;
      --ifm-color-primary-darkest: #1453c0;
      --ifm-color-primary-light: #679bec;
      --ifm-color-primary-lighter: #74a4ee;
      --ifm-color-primary-lightest: #9bbdf3;
    }

    /* Clean sidebar */
    .navbar {
      box-shadow: 0 1px 2px rgba(0,0,0,0.1);
    }
    ```

    Delete the generated tutorial/intro docs that ship with create-docusaurus:
    ```bash
    rm -rf course-content/docs/tutorial-basics course-content/docs/tutorial-extras
    rm -f course-content/docs/intro.md
    rm -rf course-content/blog
    ```

    Install dependencies:
    ```bash
    cd course-content && npm install
    ```

    IMPORTANT: Do NOT delete docs/ itself — only the default tutorial subdirectories. The docs/ directory is where all lab pages will live.
    CRITICAL: Do NOT add backtick or angle bracket characters in prose MDX content outside code fences — MDX 3 treats them as JSX delimiters and will break the build (Pitfall 5 from RESEARCH.md).
  </action>
  <verify>
    <automated>
      cd course-content && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <done>npm run build exits 0 and produces a build/ directory. docusaurus.config.ts contains 'LLMOps & AgentOps with Kubernetes'. sidebars.ts contains 'lab-13-capstone'.</done>
  <acceptance_criteria>
    - `grep "LLMOps & AgentOps with Kubernetes" course-content/docusaurus.config.ts` exits 0
    - `grep "schoolofdevops" course-content/docusaurus.config.ts` exits 0
    - `grep "defaultMode.*dark" course-content/docusaurus.config.ts` exits 0
    - `grep "lab-13-capstone" course-content/sidebars.ts` exits 0
    - `grep "courseSidebar" course-content/sidebars.ts` exits 0
    - `grep "ifm-color-primary.*326ce5" course-content/src/css/custom.css` exits 0
    - `ls course-content/node_modules/.bin/docusaurus` exits 0 (npm install ran)
    - `cd course-content && npm run build` exits 0
    - `ls course-content/build/index.html` exits 0
    - No reference to "atharva" anywhere in course-content/
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create all placeholder lab pages and setup docs</name>
  <read_first>
    - .planning/phases/01-course-infrastructure/01-CONTEXT.md (decisions D-04, D-05, D-06, D-11)
    - .planning/phases/01-course-infrastructure/01-RESEARCH.md (Pattern 2 Docusaurus Tabs, Anti-Patterns section Pitfall 5 MDX parse errors, recommended structure)
    - llmops-labuide/docs/lab00.md (reference for lab structure and content depth — do NOT copy directly, rewrite with new naming)
    - llmops-labuide/docs/index.md (reference for homepage structure)
  </read_first>
  <files>
    course-content/docs/index.md
    course-content/docs/setup/prerequisites.md
    course-content/docs/setup/preflight.md
    course-content/docs/labs/lab-00-cluster-setup.md
    course-content/docs/labs/lab-01-synthetic-data.md
    course-content/docs/labs/lab-02-rag-retriever.md
    course-content/docs/labs/lab-03-finetuning.md
    course-content/docs/labs/lab-04-model-packaging.md
    course-content/docs/labs/lab-05-model-serving.md
    course-content/docs/labs/lab-06-web-ui.md
    course-content/docs/labs/lab-07-agent-core.md
    course-content/docs/labs/lab-08-agent-sandbox.md
    course-content/docs/labs/lab-09-observability.md
    course-content/docs/labs/lab-10-autoscaling.md
    course-content/docs/labs/lab-11-gitops.md
    course-content/docs/labs/lab-12-pipelines.md
    course-content/docs/labs/lab-13-capstone.md
    course-content/docs/reference/troubleshooting.md
    course-content/docs/reference/cleanup.md
  </files>
  <action>
    Create all placeholder documentation pages. Each lab page must:
    1. Have a frontmatter `sidebar_position` (sequential integers)
    2. Have a title H1 matching the lab name
    3. Have a brief "Learning Objectives" section (2-3 bullet points)
    4. Have a "Lab Files" section referencing the companion code repo path
    5. Build without MDX errors

    **Create course-content/docs/index.md** (course homepage):
    ```markdown
    ---
    sidebar_position: 1
    slug: /
    ---

    # LLMOps & AgentOps with Kubernetes

    A comprehensive hands-on course teaching how to take AI systems — LLMs and agents — from prototype to production on Kubernetes.

    Build a dental clinic AI assistant (Smile Dental) from RAG retrieval through fine-tuning, packaging, serving, a chat UI, a multi-tool Hermes agent, GitOps deployments, and production observability.

    ## What You Will Build

    - **RAG Retriever** — FAISS-backed semantic search over Smile Dental clinic data
    - **Fine-Tuned LLM** — SmolLM2-135M trained on CPU with LoRA adapters
    - **OCI Model Image** — Model packaged as a container, mounted via Kubernetes ImageVolumes
    - **vLLM + KServe** — OpenAI-compatible serving with autoscaling on KIND
    - **Chainlit Chat UI** — Streaming chat interface connected to the full RAG + LLM pipeline
    - **Hermes Agent** — Multi-tool agent with triage, lookup, and booking capabilities
    - **K8s Agent Sandbox** — Isolated agent deployment with OTEL tracing
    - **Production Ops** — HPA/KEDA autoscaling, ArgoCD GitOps, Argo Workflows pipelines

    ## Prerequisites

    - Docker Desktop with at least 8GB RAM allocated (12GB recommended)
    - `kind`, `kubectl`, `helm` installed
    - See the [Prerequisites](./setup/prerequisites) page for installation instructions

    ## How to Use This Course

    Follow the labs in order from Lab 00 through Lab 13. Each lab builds on the previous one.
    Use the companion code repository for starter files and reference solutions.

    ```

    **Create course-content/docs/setup/prerequisites.md**:
    ```markdown
    ---
    sidebar_position: 1
    ---

    # Prerequisites

    Install these tools before Lab 00.

    ## Required Tools

    | Tool | Version | Purpose |
    |------|---------|---------|
    | Docker Desktop | 28.x+ | Container runtime for KIND cluster |
    | KIND | 0.27.0+ | Kubernetes in Docker |
    | kubectl | 1.34.x | Kubernetes CLI |
    | Helm | 3.x | Kubernetes package manager |

    ## Docker Desktop Memory

    Open Docker Desktop and navigate to **Settings > Resources > Memory**.
    Set to at least **8GB** (12GB recommended for later labs running vLLM + Prometheus simultaneously).

    ## Install KIND

    import Tabs from '@theme/Tabs';
    import TabItem from '@theme/TabItem';

    <Tabs groupId="operating-systems">
      <TabItem value="macos" label="macOS">

    ```bash
    brew install kind
    ```

      </TabItem>
      <TabItem value="windows" label="Windows">

    ```powershell
    choco install kind
    # or download from https://github.com/kubernetes-sigs/kind/releases
    ```

      </TabItem>
    </Tabs>

    ## Clone the Companion Repo

    ```bash
    git clone https://github.com/schoolofdevops/302-llmops.git
    cd 302-llmops
    ```
    ```

    **Create course-content/docs/setup/preflight.md**:
    ```markdown
    ---
    sidebar_position: 2
    ---

    # Preflight Check

    Run the preflight script before starting Lab 00 to verify your environment.

    import Tabs from '@theme/Tabs';
    import TabItem from '@theme/TabItem';

    <Tabs groupId="operating-systems">
      <TabItem value="macos" label="macOS / Linux">

    ```bash
    cd labs/lab-00/starter
    bash scripts/preflight-check.sh
    ```

      </TabItem>
      <TabItem value="windows" label="Windows">

    ```powershell
    cd labs\lab-00\starter
    .\scripts\preflight-check.ps1
    ```

      </TabItem>
    </Tabs>

    ## What the Script Checks

    - Docker Desktop is running
    - Docker memory allocation (fail below 8GB, warn between 8-12GB)
    - Docker disk space (fail below 20GB free)
    - Required tools: `kind`, `kubectl`, `helm`, `docker`
    - Port availability: 80, 8000, 30000, 32000
    - No stale KIND cluster with name `llmops-kind` already exists

    Fix all `[FAIL]` items before proceeding to Lab 00.
    ```

    **Create course-content/docs/labs/lab-00-cluster-setup.md** (include the Tabs example per D-06):
    ```markdown
    ---
    sidebar_position: 1
    ---

    # Lab 00: Cluster Setup

    **Day 1 | Duration: ~30 minutes**

    ## Learning Objectives

    - Create a 3-node KIND cluster with ImageVolume feature gates enabled
    - Create the five namespaces used throughout the course
    - Verify the cluster is healthy and ready for workloads

    ## Lab Files

    Companion code: `labs/lab-00/`

    ## Setup

    import Tabs from '@theme/Tabs';
    import TabItem from '@theme/TabItem';

    <Tabs groupId="operating-systems">
      <TabItem value="macos" label="macOS / Linux">

    ```bash
    bash scripts/preflight-check.sh
    bash scripts/bootstrap-kind.sh
    ```

      </TabItem>
      <TabItem value="windows" label="Windows">

    ```powershell
    .\scripts\preflight-check.ps1
    bash scripts/bootstrap-kind.sh
    ```

      </TabItem>
    </Tabs>

    ## Lab Content

    Full lab instructions coming in Phase 2 content authoring.

    ## Lab Summary

    After this lab: KIND cluster `llmops-kind` is running with 3 nodes, ImageVolume feature gates enabled,
    and five namespaces created (`llm-serving`, `llm-app`, `monitoring`, `argocd`, `argo-workflows`).
    ```

    **Create placeholder pages for lab-01 through lab-13** using this template (adjust title and objectives per lab):
    - lab-01-synthetic-data.md: "Lab 01: Synthetic Data Generation", objectives: generate Smile Dental clinic data, build JSONL training dataset, understand data quality for fine-tuning
    - lab-02-rag-retriever.md: "Lab 02: RAG Retriever", objectives: build FAISS index, deploy FastAPI retriever to Kubernetes, run end-to-end retrieval queries
    - lab-03-finetuning.md: "Lab 03: Fine-Tuning", objectives: CPU LoRA fine-tuning of SmolLM2-135M, run training as Kubernetes Job, merge LoRA adapter into base model
    - lab-04-model-packaging.md: "Lab 04: Model Packaging", objectives: package merged model as OCI image, push to registry, mount via Kubernetes ImageVolumes
    - lab-05-model-serving.md: "Lab 05: Model Serving", objectives: deploy vLLM with OpenAI-compatible API, wrap with KServe RawDeployment, verify end-to-end inference
    - lab-06-web-ui.md: "Lab 06: Web UI", objectives: deploy Chainlit chat interface, connect to RAG + vLLM pipeline, enable streaming responses
    - lab-07-agent-core.md: "Lab 07: Agent Core", objectives: configure Hermes Agent with Smile Dental tools, connect to free-tier LLM API, demonstrate multi-step workflow
    - lab-08-agent-sandbox.md: "Lab 08: Agent Sandbox", objectives: install K8s Agent Sandbox CRD, deploy Hermes Agent as Sandbox resource, configure SandboxWarmPool
    - lab-09-observability.md: "Lab 09: Observability", objectives: deploy Prometheus + Grafana via Helm, scrape vLLM metrics, visualize OTEL traces in Tempo
    - lab-10-autoscaling.md: "Lab 10: Autoscaling", objectives: configure HPA on Chat API, add KEDA ScaledObject for Prometheus-driven scaling, run load generator
    - lab-11-gitops.md: "Lab 11: GitOps", objectives: deploy ArgoCD App-of-Apps, trigger redeployment via Git tag update, verify sync
    - lab-12-pipelines.md: "Lab 12: Pipelines", objectives: build Argo Workflows DAG for data-to-deploy pipeline, integrate DeepEval quality gate, observe pipeline execution
    - lab-13-capstone.md: "Lab 13: Capstone", objectives: add new tool to Hermes Agent, deploy via GitOps, validate with eval gate, observe in Grafana

    Each placeholder must follow this exact structure:
    ```markdown
    ---
    sidebar_position: N   # 1 for lab-00, 2 for lab-01, etc.
    ---

    # Lab NN: [Title]

    **Day X | Duration: ~YY minutes**

    ## Learning Objectives

    - [objective 1]
    - [objective 2]
    - [objective 3]

    ## Lab Files

    Companion code: `labs/lab-NN/`

    ## Lab Content

    Full lab instructions coming in a later phase.

    ## Lab Summary

    After this lab: [one sentence describing what student has built/achieved].
    ```

    **Create course-content/docs/reference/troubleshooting.md**:
    ```markdown
    ---
    sidebar_position: 1
    ---

    # Troubleshooting

    Common issues and fixes organized by lab.

    ## Lab 00 — Cluster Setup

    **Issue**: KIND cluster creation fails with "bind: address already in use"
    **Fix**: Run `kind delete cluster --name llmops-kind` to remove any stale cluster, then retry.

    **Issue**: `/mnt/project/` is empty inside pods
    **Fix**: Verify the `hostPath` in kind-config.yaml uses your absolute project path with forward slashes.
    Restart the cluster after correcting the path.

    ## Lab 03-05 — vLLM + KServe

    **Issue**: Pods stuck in Pending with "Insufficient memory"
    **Fix**: Increase Docker Desktop memory to 12GB: Settings > Resources > Memory. Restart Docker Desktop.

    More troubleshooting content added as labs are authored.
    ```

    **Create course-content/docs/reference/cleanup.md**:
    ```markdown
    ---
    sidebar_position: 2
    ---

    # Resource Cleanup

    Run cleanup scripts between heavy lab sections to reclaim cluster memory.

    ## When to Clean Up

    | After Labs | Run | Removes |
    |------------|-----|---------|
    | Labs 00-05 | `shared/scripts/cleanup-phase1.sh` | KServe, vLLM, model artifacts |
    | Labs 06-08 | `shared/scripts/cleanup-phase2.sh` | Chainlit, Agent API, Agent Sandbox |
    | Labs 09-13 | `shared/scripts/cleanup-phase3.sh` | Prometheus, Grafana, ArgoCD, Argo Workflows |

    ## Running Cleanup

    ```bash
    bash shared/scripts/cleanup-phase1.sh
    ```

    Cleanup scripts preserve namespaces and cluster infrastructure.
    They only remove workloads to free memory for the next phase.
    ```

    CRITICAL: All MDX files must NOT contain bare `<` or `{` outside of code fences or JSX component usage. If showing command placeholders, use backtick code spans or triple-backtick fences, never bare angle brackets in prose.
  </action>
  <verify>
    <automated>
      cd course-content && npm run build 2>&1 | grep -E "(error|warning|Generated)" | tail -20
    </automated>
  </verify>
  <done>npm run build exits 0. All 14 lab pages, 2 setup pages, 2 reference pages, and index.md exist. No MDX parse errors in build output.</done>
  <acceptance_criteria>
    - `ls course-content/docs/labs/ | wc -l` outputs 14
    - `ls course-content/docs/setup/` shows prerequisites.md and preflight.md
    - `ls course-content/docs/reference/` shows troubleshooting.md and cleanup.md
    - `grep "groupId=\"operating-systems\"" course-content/docs/labs/lab-00-cluster-setup.md` exits 0 (Tabs per D-06)
    - `grep "sidebar_position" course-content/docs/labs/lab-13-capstone.md` exits 0
    - `grep "llmops-kind" course-content/docs/labs/lab-00-cluster-setup.md` exits 0
    - `grep "llm-serving" course-content/docs/labs/lab-00-cluster-setup.md` exits 0
    - `cd course-content && npm run build` exits 0 (no MDX errors)
    - No reference to "atharva" in any course-content/docs/ file
    - `grep "Smile Dental" course-content/docs/index.md` exits 0 (domain branding OK per D-12)
    - `grep "Smile Dental" course-content/docs/setup/prerequisites.md` exits with 1 (infrastructure docs are brand-neutral)
  </acceptance_criteria>
</task>

</tasks>

<verification>
After both tasks complete:
```bash
cd course-content
npm run build 2>&1 | tail -3
# Expected: build succeeds

ls build/
# Expected: index.html plus static assets

ls docs/labs/ | wc -l
# Expected: 14

grep "defaultMode.*dark" docusaurus.config.ts
# Expected: match found
```
</verification>

<success_criteria>
- Docusaurus site builds without errors (npm run build exits 0)
- Single courseSidebar with Setup (2 items) and Labs (14 items) categories
- Dark mode is default (colorMode.defaultMode = 'dark')
- All 14 lab placeholder pages exist and render in sidebar
- Tabs (groupId="operating-systems") used in at least lab-00 and setup/preflight pages
- No hardcoded personal paths, no "atharva" anywhere in course-content/
- Kubernetes.io-like color scheme (#326ce5 primary blue) in custom.css
</success_criteria>

<output>
After completion, create `.planning/phases/01-course-infrastructure/01-02-SUMMARY.md`
</output>
