---
phase: 01-course-infrastructure
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - course-code/labs/lab-00/starter/setup/kind-config.yaml
  - course-code/labs/lab-00/solution/setup/kind-config.yaml
  - course-code/labs/lab-00/starter/scripts/bootstrap-kind.sh
  - course-code/labs/lab-00/solution/scripts/bootstrap-kind.sh
  - course-code/shared/k8s/namespaces.yaml
  - course-code/COURSE_VERSIONS.md
autonomous: true
requirements:
  - INFRA-04
  - K8S-01
  - K8S-02

must_haves:
  truths:
    - "starter/setup/kind-config.yaml contains REPLACE_HOST_PATH placeholder (not a real path)"
    - "solution/setup/kind-config.yaml contains the working 3-node cluster config with dual ImageVolume gates enabled"
    - "bootstrap-kind.sh creates the KIND cluster and applies namespaces in one run"
    - "shared/k8s/namespaces.yaml defines exactly 5 namespaces: llm-serving, llm-app, monitoring, argocd, argo-workflows"
    - "COURSE_VERSIONS.md pins all 14+ course dependencies with compatibility reasons"
  artifacts:
    - path: "course-code/labs/lab-00/starter/setup/kind-config.yaml"
      provides: "KIND config template with REPLACE_HOST_PATH placeholder"
      contains: "REPLACE_HOST_PATH"
    - path: "course-code/labs/lab-00/solution/setup/kind-config.yaml"
      provides: "Fully working KIND config with dual ImageVolume gates"
      contains: "ImageVolume: true"
    - path: "course-code/labs/lab-00/solution/scripts/bootstrap-kind.sh"
      provides: "Bootstrap script creating cluster + namespaces"
    - path: "course-code/shared/k8s/namespaces.yaml"
      provides: "5-namespace YAML for all course workloads"
    - path: "course-code/COURSE_VERSIONS.md"
      provides: "Pinned dependency version table with compatibility reasons"
  key_links:
    - from: "course-code/labs/lab-00/solution/scripts/bootstrap-kind.sh"
      to: "course-code/labs/lab-00/solution/setup/kind-config.yaml"
      via: "kind create cluster --config setup/kind-config.yaml"
      pattern: "kind create cluster.*kind-config.yaml"
    - from: "course-code/labs/lab-00/solution/scripts/bootstrap-kind.sh"
      to: "course-code/shared/k8s/namespaces.yaml"
      via: "kubectl apply -f ../../shared/k8s/namespaces.yaml"
      pattern: "kubectl apply.*namespaces.yaml"
---

<objective>
Create the KIND cluster configuration files, bootstrap script, namespace manifest, and COURSE_VERSIONS.md that form the Lab 00 content — giving students a verified path from zero to a running 3-node Kubernetes cluster.

Purpose: This is the cluster foundation for all subsequent labs. The KIND config must enable ImageVolume feature gates in both required locations (dual-gate pattern) to prevent Pitfall 1 (silent ImageVolume failure). The starter uses a placeholder path; the solution has the working reference.
Output: kind-config.yaml (starter with placeholder + solution with working config), bootstrap-kind.sh, shared/k8s/namespaces.yaml, COURSE_VERSIONS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-course-infrastructure/01-CONTEXT.md
@.planning/phases/01-course-infrastructure/01-RESEARCH.md
@.planning/phases/01-course-infrastructure/01-01-SUMMARY.md
@llmops-labuide/docs/lab00.md

<interfaces>
<!-- From RESEARCH.md Pattern 3: KIND Config with Dual ImageVolume Gate -->
<!-- BOTH blocks are required — single-location enable silently fails (Pitfall 1) -->

Required kind-config.yaml structure:
```yaml
kubeadmConfigPatches:
  - |
    apiVersion: kubeadm.k8s.io/v1beta3
    kind: ClusterConfiguration
    apiServer:
      extraArgs:
        feature-gates: "ImageVolume=true"
    controllerManager:
      extraArgs:
        feature-gates: "ImageVolume=true"
    scheduler:
      extraArgs:
        feature-gates: "ImageVolume=true"
  - |
    apiVersion: kubelet.config.k8s.io/v1beta1
    kind: KubeletConfiguration
    featureGates:
      ImageVolume: true

kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: llmops-kind
nodes:
- role: control-plane
  image: kindest/node:v1.34.0
  extraMounts:
    - hostPath: REPLACE_HOST_PATH
      containerPath: /mnt/project
  extraPortMappings: [...]
- role: worker
  image: kindest/node:v1.34.0
  extraMounts: [...]
- role: worker
  image: kindest/node:v1.34.0
  extraMounts: [...]
```

Port mappings required (from CONTEXT.md and lab00.md):
- 30000:30000 (Prometheus NodePort)
- 32000:32000 (Grafana/ArgoCD NodePort)
- 8000:8000 (vLLM API)
- 80:80 (general HTTP)
- 443:443 (general HTTPS)
- 30080:30080 (Chainlit NodePort)
- 30090:30090 (ArgoCD NodePort)

Namespace pattern (Pattern 5 from RESEARCH.md — per D-10):
- llm-serving: vLLM, KServe InferenceService
- llm-app: Chat UI (Chainlit), Agent API, RAG Retriever
- monitoring: Prometheus, Grafana
- argocd: ArgoCD controller
- argo-workflows: Argo Workflows controller
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create KIND config YAML files (starter with placeholder, solution with working config)</name>
  <read_first>
    - llmops-labuide/docs/lab00.md (existing KIND config YAML — reference for port mappings and structure; DO NOT copy hardcoded paths or old namespace names)
    - .planning/phases/01-course-infrastructure/01-RESEARCH.md (Pattern 3: KIND Config with Dual ImageVolume Gate, Anti-Patterns: hardcoded host paths, Pitfall 1: ImageVolume Silent Failure, Pitfall 3: Hardcoded Host Paths, Pitfall 4: Windows Path Separators)
    - .planning/phases/01-course-infrastructure/01-CONTEXT.md (D-08: 3-node topology, D-10: llmops-kind cluster name)
  </read_first>
  <files>
    course-code/labs/lab-00/starter/setup/kind-config.yaml
    course-code/labs/lab-00/solution/setup/kind-config.yaml
  </files>
  <action>
    Create the setup/ subdirectory under both starter and solution:
    ```bash
    mkdir -p course-code/labs/lab-00/starter/setup
    mkdir -p course-code/labs/lab-00/solution/setup
    ```

    **Create course-code/labs/lab-00/starter/setup/kind-config.yaml** (template with placeholder):
    ```yaml
    # kind-config.yaml — LLMOps Course KIND Cluster Configuration
    #
    # BEFORE USE: Replace REPLACE_HOST_PATH with the absolute path to your project directory.
    # Example (macOS):  /Users/yourname/llmops-project
    # Example (Linux):  /home/yourname/llmops-project
    # Example (Windows Git Bash):  /c/Users/yourname/llmops-project
    #
    # The bootstrap script will prompt you for this path automatically.
    # See Lab 00 instructions for details.

    # ImageVolume feature gate is enabled in TWO places (both required):
    # 1. kubeadmConfigPatches — API server, controller-manager, scheduler
    # 2. KubeletConfiguration — kubelet on each node
    # Enabling only one place silently fails (pods run but volume is empty).
    kubeadmConfigPatches:
      - |
        apiVersion: kubeadm.k8s.io/v1beta3
        kind: ClusterConfiguration
        apiServer:
          extraArgs:
            feature-gates: "ImageVolume=true"
        controllerManager:
          extraArgs:
            feature-gates: "ImageVolume=true"
        scheduler:
          extraArgs:
            feature-gates: "ImageVolume=true"
      - |
        apiVersion: kubelet.config.k8s.io/v1beta1
        kind: KubeletConfiguration
        featureGates:
          ImageVolume: true

    kind: Cluster
    apiVersion: kind.x-k8s.io/v1alpha4
    name: llmops-kind
    nodes:
    - role: control-plane
      image: kindest/node:v1.34.0
      extraMounts:
        - hostPath: REPLACE_HOST_PATH
          containerPath: /mnt/project
      extraPortMappings:
      - containerPort: 30000
        hostPort: 30000
        listenAddress: "0.0.0.0"
        protocol: tcp
      - containerPort: 32000
        hostPort: 32000
        listenAddress: "0.0.0.0"
        protocol: tcp
      - containerPort: 8000
        hostPort: 8000
        listenAddress: "0.0.0.0"
        protocol: tcp
      - containerPort: 80
        hostPort: 80
        listenAddress: "0.0.0.0"
        protocol: tcp
      - containerPort: 443
        hostPort: 443
        listenAddress: "0.0.0.0"
        protocol: tcp
      - containerPort: 30080
        hostPort: 30080
        listenAddress: "0.0.0.0"
        protocol: tcp
      - containerPort: 30090
        hostPort: 30090
        listenAddress: "0.0.0.0"
        protocol: tcp
    - role: worker
      image: kindest/node:v1.34.0
      extraMounts:
        - hostPath: REPLACE_HOST_PATH
          containerPath: /mnt/project
    - role: worker
      image: kindest/node:v1.34.0
      extraMounts:
        - hostPath: REPLACE_HOST_PATH
          containerPath: /mnt/project
    ```

    **Create course-code/labs/lab-00/solution/setup/kind-config.yaml**:
    Same as starter but with a concrete example path so students can see a working reference.
    Replace REPLACE_HOST_PATH with: `./llmops-project`
    (Relative path in Git Bash on Windows and on macOS both work when running bootstrap-kind.sh from the repo root.)
    Add a comment on the line: `# relative path — works on macOS and Windows Git Bash; change to absolute if needed`

    CRITICAL: Do NOT use `/Users/gshah` or any personal path in the solution file.
    CRITICAL: The solution file must contain `ImageVolume: true` in the KubeletConfiguration block (not just in kubeadmConfigPatches).
  </action>
  <verify>
    <automated>
      bash -c 'grep "REPLACE_HOST_PATH" course-code/labs/lab-00/starter/setup/kind-config.yaml && echo "starter has placeholder" && grep "ImageVolume: true" course-code/labs/lab-00/solution/setup/kind-config.yaml && echo "solution has ImageVolume gate" && grep -v "REPLACE_HOST_PATH" course-code/labs/lab-00/solution/setup/kind-config.yaml | grep -q "llmops-project" && echo "solution has concrete path"'
    </automated>
  </verify>
  <done>starter/setup/kind-config.yaml contains REPLACE_HOST_PATH. solution/setup/kind-config.yaml contains ImageVolume: true in KubeletConfiguration and a concrete example path. Neither file contains personal paths (no /Users/gshah).</done>
  <acceptance_criteria>
    - `grep "REPLACE_HOST_PATH" course-code/labs/lab-00/starter/setup/kind-config.yaml` exits 0
    - `grep -c "REPLACE_HOST_PATH" course-code/labs/lab-00/starter/setup/kind-config.yaml` outputs 3 (one per node)
    - `grep "ImageVolume: true" course-code/labs/lab-00/solution/setup/kind-config.yaml` exits 0
    - `grep 'feature-gates: "ImageVolume=true"' course-code/labs/lab-00/solution/setup/kind-config.yaml` exits 0
    - `grep "kindest/node:v1.34.0" course-code/labs/lab-00/solution/setup/kind-config.yaml` exits 0
    - `grep -c "role: worker" course-code/labs/lab-00/solution/setup/kind-config.yaml` outputs 2
    - `grep "llmops-kind" course-code/labs/lab-00/solution/setup/kind-config.yaml` exits 0
    - `grep "gshah\|atharva\|Atharva" course-code/labs/lab-00/solution/setup/kind-config.yaml` exits 1 (no personal paths)
    - `grep "30000" course-code/labs/lab-00/solution/setup/kind-config.yaml` exits 0
    - `grep "8000" course-code/labs/lab-00/solution/setup/kind-config.yaml` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create bootstrap-kind.sh, namespaces.yaml, and COURSE_VERSIONS.md</name>
  <read_first>
    - .planning/phases/01-course-infrastructure/01-RESEARCH.md (Pattern 5: Namespace Strategy, Pattern 6: COURSE_VERSIONS.md Format, Standard Stack table with all pinned versions)
    - .planning/phases/01-course-infrastructure/01-CONTEXT.md (D-10: namespace names, D-12: generic infrastructure naming)
    - llmops-labuide/docs/lab00.md (existing bootstrap commands — reference for kubectl apply pattern; DO NOT copy atharva-ml or atharva-app namespace names)
    - course-code/labs/lab-00/solution/setup/kind-config.yaml (bootstrap script references this file)
  </read_first>
  <files>
    course-code/labs/lab-00/starter/scripts/bootstrap-kind.sh
    course-code/labs/lab-00/solution/scripts/bootstrap-kind.sh
    course-code/shared/k8s/namespaces.yaml
    course-code/COURSE_VERSIONS.md
  </files>
  <action>
    **Create course-code/labs/lab-00/starter/scripts/bootstrap-kind.sh** (template with path prompt):
    ```bash
    #!/usr/bin/env bash
    # bootstrap-kind.sh — Create the LLMOps KIND cluster and namespaces
    # Run from the course-code repo root: bash labs/lab-00/starter/scripts/bootstrap-kind.sh
    set -euo pipefail

    CLUSTER_NAME="llmops-kind"
    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    REPO_ROOT="$(cd "${SCRIPT_DIR}/../../.." && pwd)"
    KIND_CONFIG="${SCRIPT_DIR}/../setup/kind-config.yaml"

    echo "============================================="
    echo " LLMOps — Bootstrap KIND Cluster"
    echo "============================================="
    echo ""

    # Detect if running from starter (has REPLACE_HOST_PATH) or solution
    if grep -q "REPLACE_HOST_PATH" "${KIND_CONFIG}"; then
      echo "Detected REPLACE_HOST_PATH in kind-config.yaml."
      echo "Please enter the absolute path to your project directory."
      echo "This is the directory where your lab files will live on your machine."
      echo ""
      read -r -p "Project directory path (e.g. /Users/yourname/llmops-project): " HOST_PATH
      if [ -z "$HOST_PATH" ]; then
        echo "ERROR: Path cannot be empty."
        exit 1
      fi
      # Create a temporary config with the path substituted
      TMP_CONFIG=$(mktemp /tmp/kind-config-XXXXX.yaml)
      sed "s|REPLACE_HOST_PATH|${HOST_PATH}|g" "${KIND_CONFIG}" > "${TMP_CONFIG}"
      KIND_CONFIG="${TMP_CONFIG}"
      echo ""
      echo "Using project path: ${HOST_PATH}"
    fi

    # Check for existing cluster
    if kind get clusters 2>/dev/null | grep -q "${CLUSTER_NAME}"; then
      echo "Cluster ${CLUSTER_NAME} already exists."
      read -r -p "Delete and recreate? (y/N): " CONFIRM
      if [[ "${CONFIRM}" =~ ^[Yy]$ ]]; then
        kind delete cluster --name "${CLUSTER_NAME}"
      else
        echo "Skipping cluster creation. Applying namespaces only."
        kubectl apply -f "${REPO_ROOT}/shared/k8s/namespaces.yaml"
        echo ""
        echo "Namespaces applied. Your cluster is ready."
        exit 0
      fi
    fi

    echo ""
    echo "==> Creating KIND cluster: ${CLUSTER_NAME}"
    kind create cluster --config "${KIND_CONFIG}" --wait 5m

    echo ""
    echo "==> Verifying cluster nodes..."
    kubectl get nodes

    echo ""
    echo "==> Creating namespaces..."
    kubectl apply -f "${REPO_ROOT}/shared/k8s/namespaces.yaml"

    echo ""
    echo "==> Verifying namespaces..."
    kubectl get namespaces | grep -E "llm-serving|llm-app|monitoring|argocd|argo-workflows"

    echo ""
    echo "============================================="
    echo " Cluster ready: ${CLUSTER_NAME}"
    echo " Run: kubectl get nodes"
    echo "============================================="
    ```

    Make executable: `chmod +x course-code/labs/lab-00/starter/scripts/bootstrap-kind.sh`

    **Copy to solution** (solution bootstrap is identical — the path substitution works for both):
    ```bash
    cp course-code/labs/lab-00/starter/scripts/bootstrap-kind.sh course-code/labs/lab-00/solution/scripts/bootstrap-kind.sh
    chmod +x course-code/labs/lab-00/solution/scripts/bootstrap-kind.sh
    ```

    **Create course-code/shared/k8s/namespaces.yaml** with exactly 5 namespaces per Pattern 5 from RESEARCH.md:
    ```yaml
    # namespaces.yaml — LLMOps Course Kubernetes Namespaces
    # Apply: kubectl apply -f shared/k8s/namespaces.yaml
    # Generic functional names per course convention (D-10)
    apiVersion: v1
    kind: Namespace
    metadata:
      name: llm-serving
      labels:
        course: llmops
        purpose: model-serving
    ---
    apiVersion: v1
    kind: Namespace
    metadata:
      name: llm-app
      labels:
        course: llmops
        purpose: application
    ---
    apiVersion: v1
    kind: Namespace
    metadata:
      name: monitoring
      labels:
        course: llmops
        purpose: observability
    ---
    apiVersion: v1
    kind: Namespace
    metadata:
      name: argocd
      labels:
        course: llmops
        purpose: gitops
    ---
    apiVersion: v1
    kind: Namespace
    metadata:
      name: argo-workflows
      labels:
        course: llmops
        purpose: pipelines
    ```

    **Create course-code/COURSE_VERSIONS.md** following Pattern 6 from RESEARCH.md:
    ```markdown
    # Course Versions

    Tested combination for this course delivery.
    All versions verified on macOS Apple Silicon and x86-64 Windows.

    **Last verified:** 2026-04-12
    **Workshop delivery:** v1.0

    ## Core Infrastructure

    | Component | Pinned Version | Compatibility Reason |
    |-----------|---------------|----------------------|
    | kindest/node | v1.34.0 | ImageVolume beta available; v1.33 requires manual gate enable; v1.35 not yet tested |
    | KIND CLI | 0.27.0 | Supports kind config v1alpha4; tested on macOS Apple Silicon + x86 |
    | kubectl | 1.34.x | Server version match; avoid skew beyond ±1 minor version |
    | Helm | 3.x | 3.18+ preferred; any 3.x works |
    | Docker Desktop | 4.x (engine 28+) | Set Resources > Memory >= 12GB for Labs 04-09 |

    ## ML / LLM Stack

    | Component | Pinned Version | Compatibility Reason |
    |-----------|---------------|----------------------|
    | Python | 3.11 | PEFT + PyTorch + Transformers tested on 3.11; 3.12 has edge cases with some PEFT versions |
    | PyTorch | 2.4+ (CPU) | MKL included in x86_64 wheels; required for NumPy 2.x compatibility |
    | Transformers | 4.50+ | Required by vLLM 0.19.0; SmolLM2-135M tokenizer compatibility |
    | PEFT | 0.14+ | LoRA CPU training on SmolLM2-135M; 0.14 adds stable merge_and_unload |
    | Sentence-Transformers | 3.x | all-MiniLM-L6-v2 embeddings; 22MB, 14.7ms/1K tokens on CPU |
    | FAISS | faiss-cpu latest | In-process vector search; no version constraint beyond Python 3.11 compat |
    | NumPy | 1.26.4 | Pin to avoid NumPy 2.x breaking changes with older scipy/faiss |
    | HuggingFace SmolLM2-135M-Instruct | main | Base model for fine-tuning; 135M params, CPU-compatible |

    ## Serving & Deployment

    | Component | Pinned Version | Compatibility Reason |
    |-----------|---------------|----------------------|
    | vLLM | v0.19.0 | Official CPU image: schoolofdevops/vllm-cpu-nonuma:0.9.1; use this image tag |
    | KServe | 0.14+ | RawDeployment mode for KIND (no Knative required); 0.14 adds stable ImageVolume support |
    | kube-prometheus-stack | latest Helm chart | Pin chart version at workshop delivery time |

    ## Web UI & Agent

    | Component | Pinned Version | Compatibility Reason |
    |-----------|---------------|----------------------|
    | Chainlit | 2.11.0 | Requires `--host 0.0.0.0` for K8s NodePort access; 2.11 has stable WebSocket streaming |
    | FastAPI | 0.x (latest) | Pydantic v2 compatible; use fastapi[standard] for uvicorn inclusion |

    ## Documentation Site

    | Component | Pinned Version | Compatibility Reason |
    |-----------|---------------|----------------------|
    | Docusaurus | 3.10.0 | npm latest as of 2026-04-12; MDX 3, dark/light toggle, versioning |
    | Node.js | 22.x LTS | For Docusaurus build only; 18+ required by Docusaurus 3 |

    ## Notes

    - vLLM CPU image: use `schoolofdevops/vllm-cpu-nonuma:0.9.1` — the official `vllm/vllm-openai-cpu` variant requires NUMA-capable hardware
    - KIND node image: always pin to `v1.34.0` — `latest` is not a valid KIND image tag
    - For Hermes Agent (Lab 07): requires free-tier API key for Gemini (https://aistudio.google.com) or Groq (https://console.groq.com)
    ```
  </action>
  <verify>
    <automated>
      bash -c 'grep "REPLACE_HOST_PATH" course-code/labs/lab-00/starter/scripts/bootstrap-kind.sh && echo "starter bootstrap has placeholder handler" && grep -c "kind: Namespace" course-code/shared/k8s/namespaces.yaml && grep "v1.34.0" course-code/COURSE_VERSIONS.md && echo "COURSE_VERSIONS.md has KIND version"'
    </automated>
  </verify>
  <done>bootstrap-kind.sh detects REPLACE_HOST_PATH and prompts user. namespaces.yaml has exactly 5 Namespace objects. COURSE_VERSIONS.md contains v1.34.0 for KIND node image and vLLM version.</done>
  <acceptance_criteria>
    - `grep "REPLACE_HOST_PATH" course-code/labs/lab-00/starter/scripts/bootstrap-kind.sh` exits 0
    - `grep "kind create cluster" course-code/labs/lab-00/starter/scripts/bootstrap-kind.sh` exits 0
    - `grep "namespaces.yaml" course-code/labs/lab-00/starter/scripts/bootstrap-kind.sh` exits 0
    - `grep -c "kind: Namespace" course-code/shared/k8s/namespaces.yaml` outputs 5
    - `grep "llm-serving" course-code/shared/k8s/namespaces.yaml` exits 0
    - `grep "llm-app" course-code/shared/k8s/namespaces.yaml` exits 0
    - `grep "argo-workflows" course-code/shared/k8s/namespaces.yaml` exits 0
    - `grep "atharva" course-code/shared/k8s/namespaces.yaml` exits 1 (no old names)
    - `grep "v1.34.0" course-code/COURSE_VERSIONS.md` exits 0
    - `grep "vLLM" course-code/COURSE_VERSIONS.md` exits 0
    - `grep "Chainlit" course-code/COURSE_VERSIONS.md` exits 0
    - `grep "Docusaurus" course-code/COURSE_VERSIONS.md` exits 0
    - `ls course-code/labs/lab-00/solution/scripts/` shows bootstrap-kind.sh
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run after both tasks complete:
```bash
grep -c "REPLACE_HOST_PATH" course-code/labs/lab-00/starter/setup/kind-config.yaml
# Expected: 3

grep "ImageVolume: true" course-code/labs/lab-00/solution/setup/kind-config.yaml
# Expected: match found

grep -c "kind: Namespace" course-code/shared/k8s/namespaces.yaml
# Expected: 5

wc -l course-code/COURSE_VERSIONS.md
# Expected: > 40 lines (full version table)
```
</verification>

<success_criteria>
- starter/setup/kind-config.yaml has REPLACE_HOST_PATH in all 3 nodes (control-plane + 2 workers)
- solution/setup/kind-config.yaml has ImageVolume enabled in BOTH kubeadmConfigPatches AND KubeletConfiguration
- bootstrap-kind.sh handles REPLACE_HOST_PATH by prompting user, then substitutes into temp config
- shared/k8s/namespaces.yaml creates exactly 5 namespaces (llm-serving, llm-app, monitoring, argocd, argo-workflows)
- COURSE_VERSIONS.md covers all major components with compatibility reasons
- No personal paths (gshah, atharva) anywhere in any file created by this plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-course-infrastructure/01-04-SUMMARY.md`
</output>
