---
phase: 03-agentops-labs-day-2
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - course-content/docs/labs/lab-07-agent-core.md
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-04
must_haves:
  truths:
    - "Lab 07 page is a complete Docusaurus walkthrough that takes a student from `cp .env.example .env` to a working multi-step agent demo (no missing steps)"
    - "Page references the Lab-07 solution code by exact relative paths (not vague references)"
    - "Page contains the canonical 'severe tooth pain since yesterday' query and shows what tool calls to expect (closes AGENT-04 narrative)"
    - "Page has the expected 6 sections per CLAUDE.md style: Learning Objectives, Lab Files, Prerequisites, Step-by-step Parts, Verification, After This Lab"
    - "Page documents the Hermes 64K context requirement, the Groq+Gemini choice (D-04), and the LLM provider env-var setup"
  artifacts:
    - path: course-content/docs/labs/lab-07-agent-core.md
      provides: "Full Lab 07 student walkthrough"
      min_lines: 250
      contains: "severe tooth pain since yesterday"
  key_links:
    - from: course-content/docs/labs/lab-07-agent-core.md
      to: course-code/labs/lab-07/solution/scripts/verify-hermes-startup.sh
      via: "Prerequisites step instructs students to run the script"
      pattern: "verify-hermes-startup.sh"
    - from: course-content/docs/labs/lab-07-agent-core.md
      to: course-code/labs/lab-07/solution/docker-compose.yaml
      via: "Step-by-step instructs `docker compose up -d --build` from solution directory"
      pattern: "docker compose up"
---

<objective>
Replace the 25-line placeholder `lab-07-agent-core.md` with a complete Docusaurus lab page that walks students through Lab 07 end-to-end: prerequisites, Hermes verification, MCP tool overview (with code excerpts pointing into the solution), Hermes config explanation, running the stack, the canonical multi-step demo query, and a verification + summary section. Closes the documentation half of AGENT-01..04.

Purpose: The code from plan 03-02 is useless to students without a guided walkthrough. This page is the student's hands-on path through Lab 07.
Output: One Docusaurus markdown page (~300-400 lines) using the same style established in lab-01 through lab-06 (Tabs for OS-specific commands, code blocks pointing into `course-code/labs/lab-07/solution/...`, admonitions for warnings/tips).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-agentops-labs-day-2/03-CONTEXT.md
@.planning/phases/03-agentops-labs-day-2/03-RESEARCH.md
@.planning/phases/03-agentops-labs-day-2/03-02-SUMMARY.md
@CLAUDE.md
@course-content/docs/labs/lab-06-web-ui.md
@course-content/docs/labs/lab-05-model-serving.md
@course-content/docs/labs/lab-07-agent-core.md
@course-code/labs/lab-07/solution/docker-compose.yaml
@course-code/labs/lab-07/solution/hermes-config/config.yaml
@course-code/labs/lab-07/solution/hermes-config/SOUL.md
@course-code/labs/lab-07/solution/scripts/verify-hermes-startup.sh
@course-code/labs/lab-07/solution/.env.example
@course-code/labs/lab-07/solution/tools/triage/triage_server.py
@course-code/labs/lab-07/solution/tools/treatment_lookup/treatment_lookup_server.py
@course-code/labs/lab-07/solution/tools/book_appointment/book_appointment_server.py
@course-code/labs/lab-07/solution/ui/app.py

<interfaces>
Docusaurus style established in Day 1 lab pages (lab-01..lab-06):
- Frontmatter: `---\nsidebar_position: N\n---`
- Imports for OS-specific tabs: `import Tabs from '@theme/Tabs'; import TabItem from '@theme/TabItem';`
- H1 title + `**Day N | Duration: ~XX minutes**` line
- Section headings use H2 (##) for parts, H3 (###) for steps
- Code blocks fenced with language tag (```bash, ```python, ```yaml)
- Admonitions: `:::tip Title`, `:::warning Title`, `:::note Title`, `:::info Title`
- Tabs pattern for OS-specific commands: `<Tabs groupId="operating-systems">` with `<TabItem value="macos" label="macOS / Linux">` and `<TabItem value="windows" label="Windows (Git Bash)">`
- "After This Lab" section at the end summarising what's running where
- Lab files reference: `Companion code: \`labs/lab-NN/\``

Existing lab-07 placeholder (overwrite this entirely):
```
--- # marker
sidebar_position: 8
--- # marker

# Lab 07: Agent Core
**Day 2 | Duration: ~60 minutes**

## Learning Objectives
- Configure the Hermes Agent with Smile Dental tools (triage, lookup, booking)
- Connect the agent to a free-tier LLM API (Gemini or Groq)
- Demonstrate a multi-step agentic workflow end-to-end
...
```

The new page MUST keep `sidebar_position: 8` and the Lab 07 title and Day 2 / 60-min header.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite lab-07-agent-core.md as a complete Day 2 walkthrough</name>
  <files>course-content/docs/labs/lab-07-agent-core.md</files>
  <read_first>
    - course-content/docs/labs/lab-07-agent-core.md (existing 25-line placeholder — overwrite entirely)
    - course-content/docs/labs/lab-06-web-ui.md (reference style — sections, tabs, admonitions, "After This Lab" table)
    - course-content/docs/labs/lab-05-model-serving.md (reference style — concept-explanation pattern at top of part, "How X works" subsections quoting code from solution dir)
    - course-code/labs/lab-07/solution/docker-compose.yaml (verbatim relative paths and image names; lab guide MUST reference these)
    - course-code/labs/lab-07/solution/hermes-config/config.yaml (concrete config.yaml content to display in the lab)
    - course-code/labs/lab-07/solution/scripts/verify-hermes-startup.sh (the verification step the lab guides students to run first)
    - course-code/labs/lab-07/solution/.env.example (env-var setup)
    - course-code/labs/lab-07/solution/tools/triage/triage_server.py (excerpt to show in "How triage works" subsection)
    - course-code/labs/lab-07/solution/tools/treatment_lookup/treatment_lookup_server.py (excerpt for "How treatment_lookup wraps the RAG retriever")
    - course-code/labs/lab-07/solution/tools/book_appointment/book_appointment_server.py (excerpt for "How book_appointment persists state")
    - course-code/labs/lab-07/solution/ui/app.py (excerpt to show Day-2 Chainlit changes)
    - .planning/phases/03-agentops-labs-day-2/03-RESEARCH.md (Pitfalls 1, 2, 3, 4, 7 — these become :::warning admonitions; State-of-the-art table — explain why Streamable HTTP and not SSE)
    - .planning/phases/03-agentops-labs-day-2/03-CONTEXT.md (D-12 canonical demo; D-19 wind-down reminder; D-04 Groq+Gemini)
  </read_first>
  <action>
**Overwrite** `course-content/docs/labs/lab-07-agent-core.md` with the structure below. Maintain the Day-1 lab style established in lab-05 and lab-06 (concept paragraphs at the start of each Part; collapsible code excerpts pointing into `course-code/labs/lab-07/solution/...`; OS-specific Tabs only where commands differ; admonitions for pitfalls).

The page MUST contain these sections in this order:

1. **Frontmatter** — `sidebar_position: 8`, then Tabs imports.
2. **H1 title and tagline** — `# Lab 07: Agent Core` and `**Day 2 | Duration: ~60 minutes**`.
3. **Lead paragraph** — 2-3 sentences explaining: today the assistant becomes an agent. We swap the local SmolLM2 model for a free-tier cloud LLM (Groq or Gemini), wrap business logic as MCP tools, orchestrate them with the Hermes Agent gateway, and replace Chainlit's RAG-call path with a single agent call. Lab 07 runs entirely in Docker Compose to keep the iteration loop fast; Lab 08 ports it to Kubernetes.
4. **`## Learning Objectives`** — 5 bullets, each starting with a verb (Understand, Configure, Build, Run, Verify) covering: agent-vs-pipeline distinction; Hermes API gateway role; MCP tool registration via Streamable HTTP; multi-step tool-call workflow; canonical demo. Replace the 3-bullet placeholder with these 5.
5. **`## Lab Files`** — bullet pointing to `course-code/labs/lab-07/{starter,solution}/`. Mention key subdirs: `hermes-config/`, `tools/{triage,treatment_lookup,book_appointment}/`, `ui/`, `scripts/`.
6. **`## Prerequisites`** — checklist:
   - Day 1 labs (00-06) complete; KIND cluster up; Day-1 RAG retriever reachable on host port 8001 (instructions: `kubectl -n llm-app port-forward svc/rag-retriever 8001:8001` running in another terminal).
   - vLLM scaled to 0 (link forward to "Wind down before Day 2" subsection of Lab 06; one-liner: `kubectl scale deployment vllm-smollm2 --replicas=0 -n llm-serving`).
   - Free-tier API key from EITHER [Groq Cloud](https://console.groq.com) (recommended) OR [Google AI Studio](https://aistudio.google.com).
   - Docker Desktop running with ≥6 GB RAM (`docker info | grep -i memory`).
7. **`## Part A: Verify the Hermes image starts on your machine`** —
   - 1-paragraph "why we verify first": the upstream image is 2.4 GB, only `latest` tag, sometimes flaky on CPU-only hosts; better to know now than 30 min in. Mentions this resolves an open question from research.
   - Command (single bash block):
     ```bash
     bash course-code/labs/lab-07/solution/scripts/verify-hermes-startup.sh
     ```
   - Expected output excerpt: ends with "OK: Hermes Agent image starts on CPU-only host…".
   - `:::warning Image pull is large (2.4 GB)` admonition: run on good network or pre-pull.
8. **`## Part B: Set up your LLM provider`** —
   - 1-paragraph why we use a remote LLM here (Hermes 64K context requirement; SmolLM2 doesn't qualify; Groq is the safer primary choice for the workshop free-tier limits).
   - Tabs (groupId="llm-provider") with two TabItems:
     - `<TabItem value="groq" label="Groq (recommended)">` — sign-up link, copy `cp .env.example .env`, uncomment GROQ + LLM_BASE_URL + LLM_MODEL lines, set `GROQ_API_KEY=gsk_...`. Note 30 RPM / 6K TPM / 1000 RPD free-tier limits — fine for one student.
     - `<TabItem value="gemini" label="Gemini (alternative)">` — same shape, with Gemini-specific URL and model. Add `:::note Compatibility caveat` admonition pointing out the Hermes Gemini compat-layer concern from RESEARCH.md Focus 7.
9. **`## Part C: Tour the MCP tool servers`** —
   - 1-paragraph what MCP is (Model Context Protocol — open spec for connecting LLMs to tools, transport-agnostic; this lab uses Streamable HTTP, the current standard since the 2025-03-26 spec). Cite that SSE is now deprecated.
   - Three subsections H3, one per tool, each with:
     - `### triage — LLM-prompted severity classifier` — explain D-09 pattern (LLM call inside the tool). Include 10-15 line excerpt from `tools/triage/triage_server.py` showing `@mcp.tool()` plus the TRIAGE_PROMPT.
     - `### treatment_lookup — wraps the Day-1 RAG retriever` — explain D-10 reuse, the Service URL it calls. Include the 8-line `treatment_lookup` async function.
     - `### book_appointment — persists bookings to JSON (Lab 07) / ConfigMap (Lab 08)` — explain D-11 dual storage; show the `book_appointment` function body.
   - End the Part with a `:::tip How tools become available to the LLM` admonition explaining how Hermes prefixes them as `mcp_<server>_<tool>` and exposes them through the OpenAI tool-calling protocol so any compatible model (Groq Llama-3.3, Gemini 2.5) can use them.
10. **`## Part D: Hermes configuration`** —
    - Show the entirety of `hermes-config/config.yaml` (verbatim) in a yaml code block.
    - Show the entirety of `hermes-config/SOUL.md` in a markdown code block.
    - Inline explanation pointing at: the `model.default` line, the 3 `mcp_servers.<name>.url` lines (CALLOUT: the `/mcp` suffix is required — Pitfall 3 from RESEARCH.md), the `agent.disabled_toolsets` line (we turn off built-in tools to keep the lab focused), and `compression.enabled: false` (simpler for short demos).
    - `:::warning The /mcp path suffix is mandatory` admonition (Pitfall 3): without it Hermes logs "Connection refused" / 404 when registering tools.
11. **`## Part E: Day-2 Chainlit changes`** —
    - 2-paragraph diff narrative: what's removed from Day-1 (RAG retrieval Step, prompt-building Step, vLLM call Step, SSE streaming logic) and what's added (single Agent processing Step + dynamic tool sub-steps + AGENT_URL/HERMES_API_KEY env vars).
    - Show the 12-line `on_message` body excerpt focused on the `httpx.post` call to `f"{AGENT_URL}/v1/chat/completions"`.
    - Note that streaming is `False` for Lab 07 simplicity; mention Lab 09 may revisit.
12. **`## Part F: Run the multi-step demo`** —
    - The exact run sequence from plan 03-02 Task 3 verification:
      ```bash
      cd course-code/labs/lab-07/solution
      cp .env.example .env  # edit to add your key
      docker compose up -d --build
      docker compose ps
      ```
    - Tabs (groupId="operating-systems") if Windows Git Bash needs different envvar quoting (test once and note any difference; if none, drop the Tabs and use a plain bash block).
    - The canonical demo query:
      ```bash
      curl -s -X POST http://localhost:8642/v1/chat/completions \
        -H "Authorization: Bearer smile-dental-course-key" \
        -H "Content-Type: application/json" \
        -d '{
          "model":"hermes",
          "messages":[
            {"role":"system","content":"Use triage, treatment_lookup, and book_appointment tools in sequence."},
            {"role":"user","content":"severe tooth pain since yesterday — please book me in"}
          ],
          "stream": false
        }' | python3 -m json.tool
      ```
    - Then the same query through the Chainlit UI at `http://localhost:8000`. Screenshot placeholder: `<!-- TODO: screenshot of Chainlit Step expansion showing 3 tool calls -->` (per Day-1 lab convention).
    - Show what to look for in the response: `tool_calls` array including triage, treatment_lookup, book_appointment (note the `mcp_<server>_<tool>` prefix Hermes adds).
    - `docker compose exec mcp-book-appointment cat /data/bookings.json` — show the persisted booking with the `SD-YYYYMMDDHHMMSS` appointment_id.
13. **`## Verification`** — bullet checklist with copy-paste commands matching plan 03-02 Task 3 acceptance criteria:
    - `docker compose ps` — all services healthy
    - `curl http://localhost:8642/health` — 200
    - canonical query exercises all 3 tools — tool_calls array length ≥ 3 with names triage, treatment_lookup, book_appointment
    - `bookings.json` contains ≥ 1 entry
    - `docker compose logs hermes | grep -i mcp` shows the 3 MCP servers registered at startup
14. **`## Common Pitfalls`** — 5 admonitions, one per pitfall (RESEARCH.md Pitfalls 1, 2, 3, 4, 7 are all relevant; pick the 5 most actionable for Lab 07):
    - `:::warning Hermes requires a 64K-context model` (Pitfall 1)
    - `:::warning MCP url MUST end in /mcp` (Pitfall 3)
    - `:::warning API_SERVER_HOST=0.0.0.0 is mandatory inside containers` (Pitfall 4)
    - `:::note Linux Docker users` — `host.docker.internal` requires `extra_hosts: ["host.docker.internal:host-gateway"]` which the compose file already includes
    - `:::warning Free-tier rate limits` — Groq 30 RPM / 6K TPM / 1000 RPD; if you hit a 429, wait or switch providers
15. **`## After This Lab`** — table mirroring lab-06 style:
    | Component | URL | Status |
    |-----------|-----|--------|
    | Hermes Agent gateway | `http://localhost:8642/v1/chat/completions` | Running (Docker) |
    | Smile Dental Chat (Day-2) | `http://localhost:8000` | Running (Docker) |
    | mcp-triage / treatment_lookup / book_appointment | container-internal :8010 / :8020 / :8030 | Running (Docker) |
    | RAG retriever (Day 1, port-forwarded) | `http://localhost:8001` | Required upstream |
    Plus a closing paragraph: in Lab 08 we package this exact stack into Kubernetes Agent Sandbox, get per-session isolation and a warm pool, and route Chainlit through a Sandbox Router — so the next lab is "promotion to K8s", not new functionality.
16. **`## Tear down (optional)`** — `docker compose down -v` plus a note that Lab 08 will rebuild on K8s, so you can leave the Compose stack up if you want to compare the two side-by-side.

**Length target:** 250-400 lines. If you hit > 450, you've drifted into bullet-bloat — tighten the prose.

**Style hard-rules (from CLAUDE.md and the Day-1 lab pattern):**
- Use exact relative paths to source files (`course-code/labs/lab-07/solution/tools/triage/triage_server.py`).
- Code excerpts must match the source code byte-for-byte (no paraphrasing).
- Use Smile Dental, never Atharva.
- Every command must work from the repo root; `cd` is shown explicitly when needed.
- Admonitions follow Docusaurus syntax (`:::warning`, blank line, content, blank line, `:::`).
  </action>
  <verify>
    <automated>test $(wc -l < course-content/docs/labs/lab-07-agent-core.md) -ge 250 && grep -q '^# Lab 07: Agent Core' course-content/docs/labs/lab-07-agent-core.md && grep -q 'sidebar_position: 8' course-content/docs/labs/lab-07-agent-core.md && grep -q 'verify-hermes-startup.sh' course-content/docs/labs/lab-07-agent-core.md && grep -q 'docker compose up' course-content/docs/labs/lab-07-agent-core.md && grep -q 'severe tooth pain since yesterday' course-content/docs/labs/lab-07-agent-core.md && grep -q '/mcp' course-content/docs/labs/lab-07-agent-core.md && grep -q 'API_SERVER_HOST' course-content/docs/labs/lab-07-agent-core.md && grep -q 'llama-3.3-70b-versatile' course-content/docs/labs/lab-07-agent-core.md && grep -q 'gemini-2.5-flash' course-content/docs/labs/lab-07-agent-core.md && grep -q '## Learning Objectives' course-content/docs/labs/lab-07-agent-core.md && grep -q '## Prerequisites' course-content/docs/labs/lab-07-agent-core.md && grep -q '## Verification' course-content/docs/labs/lab-07-agent-core.md && grep -q '## After This Lab' course-content/docs/labs/lab-07-agent-core.md && grep -q '## Common Pitfalls' course-content/docs/labs/lab-07-agent-core.md && cd course-content && npx --yes docusaurus@3.10.0 build --no-minify 2>&1 | tail -10 | grep -qi 'compiled successfully' && cd /Users/gshah/courses/llmops</automated>
  </verify>
  <acceptance_criteria>
    - File ≥ 250 lines: `test $(wc -l < course-content/docs/labs/lab-07-agent-core.md) -ge 250`
    - File ≤ 500 lines (no bloat): `test $(wc -l < course-content/docs/labs/lab-07-agent-core.md) -le 500`
    - All 11 required H2 sections present: Learning Objectives, Lab Files, Prerequisites, Part A, Part B, Part C, Part D, Part E, Part F, Verification, Common Pitfalls, After This Lab, Tear down: `for h in 'Learning Objectives' 'Prerequisites' 'Part A' 'Part B' 'Part C' 'Part D' 'Part E' 'Part F' 'Verification' 'Common Pitfalls' 'After This Lab'; do grep -qF "## " course-content/docs/labs/lab-07-agent-core.md && grep -qF "$h" course-content/docs/labs/lab-07-agent-core.md || exit 1; done`
    - Frontmatter `sidebar_position: 8` preserved: `grep -q 'sidebar_position: 8' course-content/docs/labs/lab-07-agent-core.md`
    - Mentions verify-hermes-startup.sh: `grep -q 'verify-hermes-startup.sh' course-content/docs/labs/lab-07-agent-core.md`
    - Canonical demo present: `grep -q 'severe tooth pain since yesterday' course-content/docs/labs/lab-07-agent-core.md`
    - References both Groq and Gemini: `grep -q 'llama-3.3-70b-versatile' course-content/docs/labs/lab-07-agent-core.md && grep -q 'gemini-2.5-flash' course-content/docs/labs/lab-07-agent-core.md`
    - Mentions `/mcp` suffix and the API_SERVER_HOST pitfall: `grep -q '/mcp' course-content/docs/labs/lab-07-agent-core.md && grep -q 'API_SERVER_HOST' course-content/docs/labs/lab-07-agent-core.md`
    - References absolute relative paths to solution: `grep -q 'course-code/labs/lab-07/solution' course-content/docs/labs/lab-07-agent-core.md`
    - No "Atharva" references: `! grep -i 'atharva' course-content/docs/labs/lab-07-agent-core.md`
    - Docusaurus build succeeds (live verification per CLAUDE.md): `cd course-content && npx docusaurus@3.10.0 build` exits 0 and prints "compiled successfully"
    - Page links from Lab 06 wind-down forward (mentions wind-down OR scaling vLLM to 0): `grep -q 'vllm-smollm2' course-content/docs/labs/lab-07-agent-core.md`
  </acceptance_criteria>
  <done>course-content/docs/labs/lab-07-agent-core.md has been overwritten with a complete 250-400 line walkthrough containing all 11 required sections (Learning Objectives → After This Lab → Tear down), code excerpts pointing at exact paths in `course-code/labs/lab-07/solution/`, the canonical "severe tooth pain since yesterday" demo, both Groq and Gemini setups in OS Tabs, all 5 pitfall admonitions, and the prerequisite reminder to run verify-hermes-startup.sh first. Docusaurus builds without errors. Closes the documentation half of AGENT-01..04.</done>
</task>

</tasks>

<verification>
- File overwritten (was 25 lines, now ≥ 250 lines)
- All sections in correct order (use a single `grep -n '^## '` pass to confirm)
- Live `npx docusaurus build` from `course-content/` exits 0 and reports "compiled successfully"
- Spot-check: open the built page in `course-content/build/docs/labs/lab-07-agent-core/index.html` (or run `npx docusaurus serve`) and verify Tabs and admonitions render correctly
</verification>

<success_criteria>
- AGENT-01..04 documentation closed: a student following only this page (with the solution code present) reaches the canonical multi-step demo successfully
- Page is consistent with Day 1 lab style (compare against lab-05/lab-06)
- All concrete pitfalls from RESEARCH.md flow into actionable admonitions
- Build is green
</success_criteria>

<output>
After completion, create `.planning/phases/03-agentops-labs-day-2/03-03-SUMMARY.md` documenting:
- Final line count of lab-07-agent-core.md
- The 5 Common Pitfalls actually included
- Confirmation Docusaurus build succeeded
- Whether OS Tabs were needed in Part F (or dropped because the commands were identical on macOS and Windows Git Bash)
</output>
