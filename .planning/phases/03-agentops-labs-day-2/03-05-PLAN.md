---
phase: 03-agentops-labs-day-2
plan: 05
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - course-content/docs/labs/lab-08-agent-sandbox.md
autonomous: true
requirements:
  - SANDBOX-01
  - SANDBOX-02
  - SANDBOX-03
  - SANDBOX-04
must_haves:
  truths:
    - "Lab 08 page is a complete Docusaurus walkthrough that takes a student from installing Agent Sandbox v0.4.3 to a running multi-step demo through the Sandbox Router (or port-forward fallback)"
    - "Page explains the kindnet NetworkPolicy enforcement caveat clearly so students don't expect egress blocking on KIND"
    - "Page includes the cold-vs-warm timing demo with observed numbers from the SUMMARY of plan 03-04"
    - "Page documents the gcr-vs-port-forward router decision branch so students follow the right path based on their environment"
    - "Page references all 13 K8s manifests and 3 scripts produced by plan 03-04 by exact relative path"
  artifacts:
    - path: course-content/docs/labs/lab-08-agent-sandbox.md
      provides: "Full Lab 08 student walkthrough"
      min_lines: 300
      contains: "SandboxWarmPool"
  key_links:
    - from: course-content/docs/labs/lab-08-agent-sandbox.md
      to: course-code/labs/lab-08/solution/scripts/install-agent-sandbox.sh
      via: "Part A instructs students to run the install script"
      pattern: "install-agent-sandbox.sh"
    - from: course-content/docs/labs/lab-08-agent-sandbox.md
      to: course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh
      via: "Part F instructs students to run the timing demo"
      pattern: "cold-vs-warm-demo.sh"
---

<objective>
Replace the 25-line placeholder `lab-08-agent-sandbox.md` with a complete Docusaurus walkthrough: install Agent Sandbox v0.4.3, build + push MCP tool images, deploy SandboxTemplate + WarmPool + Router (or port-forward fallback) + MCP Deployments + RBAC + ConfigMap + NetworkPolicy + Day-2 Chainlit, run the canonical multi-step query through the Router, run the cold-vs-warm timing demo, and verify per-session isolation. Closes the documentation half of SANDBOX-01..04.

Purpose: Lab 08 is the most novel content in the course (no other course teaches K8s Agent Sandbox). The page must guide a 90-minute lab end-to-end without ambiguity, including the gcr-vs-port-forward branching point.
Output: One Docusaurus markdown page (~350-500 lines) covering install → build → manifests → run → cold/warm → verify → tear down.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-agentops-labs-day-2/03-CONTEXT.md
@.planning/phases/03-agentops-labs-day-2/03-RESEARCH.md
@.planning/phases/03-agentops-labs-day-2/03-04-SUMMARY.md
@CLAUDE.md
@course-content/docs/labs/lab-06-web-ui.md
@course-content/docs/labs/lab-07-agent-core.md
@course-content/docs/labs/lab-08-agent-sandbox.md
@course-code/labs/lab-08/solution/scripts/install-agent-sandbox.sh
@course-code/labs/lab-08/solution/scripts/verify-sandbox-router-image.sh
@course-code/labs/lab-08/solution/scripts/build-mcp-images.sh
@course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh
@course-code/labs/lab-08/solution/k8s/00-namespace.yaml
@course-code/labs/lab-08/solution/k8s/50-sandbox-template.yaml
@course-code/labs/lab-08/solution/k8s/50-sandbox-warmpool.yaml
@course-code/labs/lab-08/solution/k8s/50-sandbox-router.yaml
@course-code/labs/lab-08/solution/k8s/60-bookings-cm.yaml
@course-code/labs/lab-08/solution/k8s/60-booking-rbac.yaml
@course-code/labs/lab-08/solution/k8s/60-mcp-triage-deploy.yaml
@course-code/labs/lab-08/solution/k8s/60-mcp-treatment-lookup-deploy.yaml
@course-code/labs/lab-08/solution/k8s/60-mcp-book-appointment-deploy.yaml
@course-code/labs/lab-08/solution/k8s/60-hermes-config-cm.yaml
@course-code/labs/lab-08/solution/k8s/60-network-policy.yaml
@course-code/labs/lab-08/solution/k8s/40-chainlit-deploy-day2.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite lab-08-agent-sandbox.md as a complete K8s Agent Sandbox walkthrough</name>
  <files>course-content/docs/labs/lab-08-agent-sandbox.md</files>
  <read_first>
    - course-content/docs/labs/lab-08-agent-sandbox.md (existing 25-line placeholder — overwrite)
    - course-content/docs/labs/lab-07-agent-core.md (Day-2 lab style established by plan 03-03)
    - course-content/docs/labs/lab-06-web-ui.md (Day-1 K8s lab style — Tabs, admonitions, "After This Lab" table)
    - .planning/phases/03-agentops-labs-day-2/03-04-SUMMARY.md (router mode decision + observed cold/warm timings — embed actual numbers)
    - course-code/labs/lab-08/solution/k8s/*.yaml (all manifests — code excerpts must match byte-for-byte)
    - course-code/labs/lab-08/solution/scripts/*.sh (all 4 scripts — referenced by exact filename)
    - .planning/phases/03-agentops-labs-day-2/03-RESEARCH.md (Focus 3 §"What Gateway (D-08) Actually Is" — reuse this exact framing; Focus 4 §kindnet limitation; Pitfalls 2, 5, 6, 7)
    - .planning/phases/03-agentops-labs-day-2/03-CONTEXT.md (D-05 per-session Sandbox; D-06 cold/warm; D-07 NetworkPolicy; D-08 Router as gateway; D-11 ConfigMap booking)
  </read_first>
  <action>
**Overwrite** `course-content/docs/labs/lab-08-agent-sandbox.md` with the structure below. Same Day-2 lab style as plan 03-03 (Tabs, admonitions, exact paths to solution code).

**Frontmatter:** keep `sidebar_position: 9`. Update `Day` line to **"Day 2 | Duration: ~90 minutes"** (this lab is longer than 07).

**Lead paragraph** (2-3 sentences):
Day 2 part 2: take the Lab-07 Docker Compose stack and put it on Kubernetes the production way — using the Kubernetes Agent Sandbox project. We get per-session isolation, a pre-warmed pool of agent pods (so cold starts are bounded), an egress NetworkPolicy describing the production isolation pattern, and a stable Service-based gateway. By the end you'll have the exact same multi-step demo from Lab 07, running on KIND with measurable warm-pool benefits.

**Sections in order:**

1. **`## Learning Objectives`** — 6 bullets:
   - Install the Kubernetes Agent Sandbox v0.4.3 CRDs and controller on a KIND cluster
   - Deploy the Hermes Agent as a `Sandbox` resource via a `SandboxTemplate` (with the Lab-07 MCP tools running as plain Deployments)
   - Pre-warm 2 agent instances using `SandboxWarmPool` and observe the cold-vs-warm timing difference
   - Persist agent state to a `ConfigMap` via in-cluster RBAC (`book_appointment` writes to `bookings`)
   - Apply an egress `NetworkPolicy` as a documented production pattern (with the KIND/kindnet enforcement caveat)
   - Reach the agent through a stable cluster-internal gateway from Chainlit (Sandbox Router service)

2. **`## Lab Files`** — bullet list of `course-code/labs/lab-08/solution/{scripts,k8s,ui}` and the 4 scripts + 13 K8s manifests, with one-line descriptions.

3. **`## Prerequisites`** — checklist:
   - **Lab 07 complete** — you have working Hermes config + 3 MCP tool servers + Day-2 Chainlit code in `course-code/labs/lab-07/solution/` (Lab 08 reuses these).
   - vLLM scaled to 0 (link back to Lab 06 wind-down section).
   - kind-registry running (default in the course KIND config).
   - A working `GROQ_API_KEY` (or `GOOGLE_API_KEY`) — same key from Lab 07.
   - Free RAM headroom: WarmPool needs ~3-4 GB for 2 Hermes pods; check with `kubectl top nodes`.

4. **`## Part A: Install the Agent Sandbox CRDs`** — concept first, then commands:
   - 1-paragraph intro: K8s-sigs `agent-sandbox` is a project for running agentic workloads on K8s with first-class primitives (`Sandbox`, `SandboxTemplate`, `SandboxWarmPool`, `SandboxClaim`). Today we use v0.4.3 — alpha API (`v1alpha1`), but production-shape and KIND-supported.
   - Run `verify-sandbox-router-image.sh` first — explain that the Sandbox Router image is hosted on Google Container Registry and may not pull on every machine; the script picks the right path.
   - Then `install-agent-sandbox.sh` — one paragraph explaining what it does (applies `manifest.yaml` + `extensions.yaml`, waits for controller, creates `llm-agent` namespace).
   - Verify: `kubectl get crd | grep x-k8s.io` should show 4 CRDs; `kubectl get deploy -n agent-sandbox-system` should show controller Ready.
   - `:::warning Sandbox Router image pullability` admonition:
     ```text
     The Sandbox Router image is on us-central1-docker.pkg.dev (GCR). On some networks
     this pulls without credentials; on others it doesn't. The verify script writes
     /tmp/lab08-router-mode = ROUTER_MODE=gcr or ROUTER_MODE=port-forward. The lab
     branches in Part D.5 based on that file.
     ```
   - `:::info gvisor on KIND` admonition: GKE examples use `runtimeClassName: gvisor` for hardware isolation. KIND doesn't support gvisor — our SandboxTemplate omits it. Process isolation comes from K8s namespaces + RBAC even without gvisor.

5. **`## Part B: Build and push the MCP tool images`** — quick: students reuse Dockerfiles from Lab 07. Show the build script invocation:
   ```bash
   bash course-code/labs/lab-08/solution/scripts/build-mcp-images.sh
   ```
   Show expected output (3 images pushed to `localhost:5001/mcp-*:v1.0.0`). Verify with `docker images | grep mcp-`.

6. **`## Part C: Configure secrets`** — walk through copying `60-hermes-secret.yaml.example` → `60-hermes-secret.yaml`, filling in the API keys, applying:
   ```bash
   cp course-code/labs/lab-08/solution/k8s/60-hermes-secret.yaml.example /tmp/lab08-hermes-secret.yaml
   # Edit /tmp/lab08-hermes-secret.yaml to put your real key in either groq-api-key or google-api-key
   kubectl apply -f /tmp/lab08-hermes-secret.yaml
   ```
   `:::warning Don't commit your filled secret` admonition.
   Show creating the matching Chainlit-side secret (`hermes-api-secret-chainlit` in `llm-app`):
   ```bash
   kubectl -n llm-app create secret generic hermes-api-secret-chainlit \
     --from-literal=api-key=smile-dental-course-key
   ```

7. **`## Part D: Apply the K8s manifests`** — concept paragraph: we apply in numbered order so dependencies resolve naturally — namespaces → ConfigMaps + RBAC + tool Deployments → SandboxTemplate + WarmPool → Router (conditional) → NetworkPolicy. Then sub-parts:

   **D.1 — Bookings ConfigMap + RBAC** — show `60-bookings-cm.yaml` and `60-booking-rbac.yaml` excerpts. Explain why ServiceAccount lives in `llm-agent` but Role lives in `llm-app` (cross-namespace RoleBinding pattern). Apply:
   ```bash
   kubectl apply -f course-code/labs/lab-08/solution/k8s/60-bookings-cm.yaml
   kubectl apply -f course-code/labs/lab-08/solution/k8s/60-booking-rbac.yaml
   ```

   **D.2 — Hermes config ConfigMap** — show `60-hermes-config-cm.yaml` excerpt highlighting:
   - The 3 `mcp_servers.<name>.url` lines using cluster-internal DNS (`svc.cluster.local`) and the `/mcp` suffix (Pitfall 3).
   - SOUL.md (same content as Lab 07 since the agent's identity hasn't changed).
   Apply:
   ```bash
   kubectl apply -f course-code/labs/lab-08/solution/k8s/60-hermes-config-cm.yaml
   ```

   **D.3 — MCP tool Deployments** — show one full Deployment YAML (`60-mcp-triage-deploy.yaml`) and explain the pattern. Note that `60-mcp-book-appointment-deploy.yaml` is special: it sets `serviceAccountName: mcp-booking-sa` and `BOOKING_BACKEND=configmap`. Apply all 3:
   ```bash
   kubectl apply -f course-code/labs/lab-08/solution/k8s/60-mcp-triage-deploy.yaml
   kubectl apply -f course-code/labs/lab-08/solution/k8s/60-mcp-treatment-lookup-deploy.yaml
   kubectl apply -f course-code/labs/lab-08/solution/k8s/60-mcp-book-appointment-deploy.yaml
   kubectl get pods -n llm-agent -l 'app in (mcp-triage,mcp-treatment-lookup,mcp-book-appointment)'
   ```
   Expected: 3 pods Ready.

   **D.4 — SandboxTemplate and SandboxWarmPool** — concept paragraph: `SandboxTemplate` is the agent pod spec (env, image, probes, resources). `SandboxWarmPool` says "keep N pre-warmed instances of this template ready". Show `50-sandbox-template.yaml` excerpt focusing on:
   - `image: nousresearch/hermes-agent:latest`
   - `command: ["hermes", "gateway"]`
   - `API_SERVER_HOST: "0.0.0.0"` (Pitfall 4)
   - `automountServiceAccountToken: false` (Hermes pod doesn't need K8s API)
   - the `hermes-config` volume mount at `/opt/data`
   Show `50-sandbox-warmpool.yaml` with the `replicas: 2` and `sandboxTemplateRef`.
   Apply:
   ```bash
   kubectl apply -f course-code/labs/lab-08/solution/k8s/50-sandbox-template.yaml
   kubectl apply -f course-code/labs/lab-08/solution/k8s/50-sandbox-warmpool.yaml
   kubectl wait --for=jsonpath='{.status.readyReplicas}'=2 \
     sandboxwarmpool/hermes-agent-warmpool -n llm-agent --timeout=300s
   kubectl get pods -n llm-agent -l app=hermes-agent
   ```
   `:::warning First Hermes pull is 2.4 GB` admonition — the WarmPool wait may take 5+ min on first run.

   **D.5 — Sandbox Router (or port-forward fallback)** — branch on the file written by Part A:
   ```bash
   cat /tmp/lab08-router-mode
   ```
   - If `ROUTER_MODE=gcr`: apply `50-sandbox-router.yaml`, wait for the Router Deployment to be Ready, show how Chainlit will reach it via `sandbox-router-svc.llm-agent.svc.cluster.local:8080`.
   - If `ROUTER_MODE=port-forward`: skip the Router manifest. Use `kubectl port-forward` against the first WarmPool pod's container port 8642. Document this exact fallback command:
     ```bash
     POD=$(kubectl get pod -n llm-agent -l app=hermes-agent -o jsonpath='{.items[0].metadata.name}')
     kubectl -n llm-agent port-forward "pod/${POD}" 18642:8642
     ```
   `:::info Why the Router IS the gateway` admonition: in Agent Sandbox v0.4.3, "Gateway" (D-08) is implemented as the Router Service, not a K8s Gateway-API resource. The Router routes requests to the right pre-warmed Sandbox pod. (Cite RESEARCH.md Focus 3.)

   **D.6 — NetworkPolicy** — concept paragraph: this is the egress allow-list for Hermes pods. Allowed: DNS (UDP+TCP 53), RAG retriever in `llm-app`, the 3 MCP tools in `llm-agent`, and HTTPS port 443 to anywhere (for Groq + Gemini APIs).
   Show full YAML.
   `:::warning kindnet does NOT enforce NetworkPolicy` admonition (Pitfall 2):
   ```text
   KIND's default CNI (kindnet) does not implement NetworkPolicy. We apply this
   manifest because it documents the production isolation pattern — on a real
   cluster with Calico or Cilium, this policy enforces strict egress. On KIND,
   the policy exists but is not blocking traffic.
   ```
   Apply:
   ```bash
   kubectl apply -f course-code/labs/lab-08/solution/k8s/60-network-policy.yaml
   ```

8. **`## Part E: Switch Chainlit to talk to the Sandbox`** —
   - Concept: Day-2 Chainlit replaces Day-1's vLLM call path with a single call to the Sandbox Router (gcr mode) or the port-forwarded Sandbox pod (port-forward mode). The UI is unchanged otherwise.
   - Build + push the Day-2 Chainlit image (commands).
   - Apply `40-chainlit-deploy-day2.yaml`. Wait for rollout.
   - `:::tip If you're in port-forward mode` admonition: edit the AGENT_URL env in `40-chainlit-deploy-day2.yaml` to point at `http://localhost:18642` (your local port-forward) before applying — or, simpler, use the kubectl port-forward instructions in Part F to test directly without Chainlit.

9. **`## Part F: Run the multi-step demo`** —
   - Open Chainlit at `http://localhost:30300` (NodePort), submit "severe tooth pain since yesterday" — same canonical query as Lab 07.
   - Show how to verify the booking landed in the ConfigMap:
     ```bash
     kubectl get cm bookings -n llm-app -o yaml | yq '.data.bookings'
     ```
     Expected: a non-empty JSON array with one element matching the SD-YYYYMMDDHHMMSS format.
   - For the curl-only path (port-forward mode):
     ```bash
     curl -s -X POST http://localhost:18642/v1/chat/completions \
       -H "Authorization: Bearer smile-dental-course-key" \
       -d '{"model":"hermes","messages":[{"role":"user","content":"severe tooth pain since yesterday"}],"stream":false}' \
       | python3 -m json.tool
     ```

10. **`## Part G: Cold-vs-warm timing demo`** —
    - Concept: WarmPool pre-warms pods so the first request to a fresh Sandbox is fast. Drain the pool, send a request, then refill the pool and send the same request — compare wall times.
    - Run the script:
      ```bash
      bash course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh
      ```
    - Embed the actual cold + warm numbers from the SUMMARY of plan 03-04 (e.g. "Warm: ~2.1s; Cold WarmPool refill: ~64s; first request after refill: ~2.0s — the cost is borne in the WarmPool refill, not the user request").
    - Explain what students should see: a clear order-of-magnitude difference between cold (image pull from cache + Hermes startup ~30-60s) and warm (just the LLM API round-trip ~1-3s).
    - `:::tip Visualising in events` admonition: `kubectl describe sandbox -n llm-agent | grep -A5 Events` shows the ScheduleAttempt → PodCreated → PodReady transitions for a Sandbox. This is where you can see the cold-start cost in real time.

11. **`## Verification`** — automated checks (copy from plan 03-04 acceptance criteria):
    - `kubectl get sandboxwarmpool/hermes-agent-warmpool -n llm-agent -o jsonpath='{.status.readyReplicas}'` returns "2"
    - `kubectl get pods -n llm-agent` — 2 hermes-agent + 3 mcp-* + (optionally) 1 sandbox-router all Running
    - canonical query returns tool_calls covering all 3 tools
    - bookings ConfigMap has at least 1 entry: `kubectl get cm bookings -n llm-app -o jsonpath='{.data.bookings}' | python3 -c "import json, sys; assert len(json.load(sys.stdin))>=1"`
    - cold-vs-warm-demo.sh ran end-to-end with both timings printed

12. **`## Common Pitfalls`** — admonitions (RESEARCH.md Pitfalls 2, 5, 6, 7 are most relevant; pick 5):
    - `:::warning kindnet doesn't enforce NetworkPolicy` (Pitfall 2)
    - `:::warning Sandbox Router image pull may fail on KIND` (Pitfall 5) — link back to Part A's verify script
    - `:::warning book_appointment 403 from K8s API` (Pitfall 6) — must apply 60-booking-rbac.yaml and use `serviceAccountName: mcp-booking-sa`
    - `:::warning DNS broken by NetworkPolicy` (Pitfall 7) — UDP+TCP 53 must be allowed (already in the policy YAML)
    - `:::tip First Hermes pull is slow` — pre-pull `nousresearch/hermes-agent:latest` on the KIND node before starting Part D.4 to avoid a 5+ min wait

13. **`## After This Lab`** — table:
    | Component | Resource | Namespace | Status |
    |-----------|----------|-----------|--------|
    | Agent Sandbox controller | Deploy/agent-sandbox-controller-manager | agent-sandbox-system | Running |
    | Hermes Agent | SandboxWarmPool/hermes-agent-warmpool (replicas=2) | llm-agent | 2 Sandboxes ready |
    | MCP tools | Deploy/mcp-triage / mcp-treatment-lookup / mcp-book-appointment | llm-agent | Running |
    | Sandbox Router | Deploy/sandbox-router (gcr mode) | llm-agent | Running |
    | Bookings store | ConfigMap/bookings | llm-app | Persists across pod restarts |
    | Chainlit (Day-2) | Deploy/chainlit-ui (NodePort 30300) | llm-app | Routes through Sandbox |
    | Egress policy | NetworkPolicy/hermes-agent-egress | llm-agent | Applied (not enforced on KIND) |
    Plus a closing paragraph: in Lab 09 we add OpenTelemetry collector + Grafana Tempo + cost tracking — so the same agent stack becomes observable end-to-end.

14. **`## Tear down (optional)`** — `kubectl delete -f course-code/labs/lab-08/solution/k8s/` plus the WarmPool drain note (scale to 0 first to avoid orphaned Sandbox CRs).

**Length target:** 350-500 lines. Hard cap 600.

**Style:** match plan 03-03 (Tabs only where commands actually differ on Windows; admonitions for warnings/tips/info; exact relative paths to solution code).
  </action>
  <verify>
    <automated>test $(wc -l < course-content/docs/labs/lab-08-agent-sandbox.md) -ge 300 && test $(wc -l < course-content/docs/labs/lab-08-agent-sandbox.md) -le 600 && grep -q '^# Lab 08: Agent Sandbox' course-content/docs/labs/lab-08-agent-sandbox.md && grep -q 'sidebar_position: 9' course-content/docs/labs/lab-08-agent-sandbox.md && grep -q 'install-agent-sandbox.sh' course-content/docs/labs/lab-08-agent-sandbox.md && grep -q 'verify-sandbox-router-image.sh' course-content/docs/labs/lab-08-agent-sandbox.md && grep -q 'cold-vs-warm-demo.sh' course-content/docs/labs/lab-08-agent-sandbox.md && grep -q 'SandboxWarmPool' course-content/docs/labs/lab-08-agent-sandbox.md && grep -q 'sandbox-router-svc' course-content/docs/labs/lab-08-agent-sandbox.md && grep -q 'kindnet does NOT enforce NetworkPolicy\|kindnet doesn.*enforce NetworkPolicy' course-content/docs/labs/lab-08-agent-sandbox.md && grep -q 'mcp-booking-sa' course-content/docs/labs/lab-08-agent-sandbox.md && grep -q 'severe tooth pain since yesterday' course-content/docs/labs/lab-08-agent-sandbox.md && grep -q '## Learning Objectives' course-content/docs/labs/lab-08-agent-sandbox.md && grep -q '## Prerequisites' course-content/docs/labs/lab-08-agent-sandbox.md && grep -q '## Verification' course-content/docs/labs/lab-08-agent-sandbox.md && grep -q '## After This Lab' course-content/docs/labs/lab-08-agent-sandbox.md && ! grep -i 'atharva' course-content/docs/labs/lab-08-agent-sandbox.md && cd course-content && npx --yes docusaurus@3.10.0 build --no-minify 2>&1 | tail -10 | grep -qi 'compiled successfully' && cd /Users/gshah/courses/llmops</automated>
  </verify>
  <acceptance_criteria>
    - File length 300-600 lines
    - All required H2 sections present: Learning Objectives, Lab Files, Prerequisites, Part A through Part G, Verification, Common Pitfalls, After This Lab, Tear down: `for h in 'Learning Objectives' 'Prerequisites' 'Part A' 'Part B' 'Part C' 'Part D' 'Part E' 'Part F' 'Part G' 'Verification' 'Common Pitfalls' 'After This Lab'; do grep -qF "$h" course-content/docs/labs/lab-08-agent-sandbox.md || exit 1; done`
    - References all 4 scripts: `grep -q 'verify-sandbox-router-image.sh' && grep -q 'install-agent-sandbox.sh' && grep -q 'build-mcp-images.sh' && grep -q 'cold-vs-warm-demo.sh'`
    - Documents the gcr-vs-port-forward branch: `grep -q 'ROUTER_MODE=gcr\|gcr mode' course-content/docs/labs/lab-08-agent-sandbox.md && grep -q 'ROUTER_MODE=port-forward\|port-forward mode\|port-forward fallback' course-content/docs/labs/lab-08-agent-sandbox.md`
    - kindnet caveat is present (Pitfall 2): `grep -i 'kindnet' course-content/docs/labs/lab-08-agent-sandbox.md`
    - book_appointment RBAC is mentioned: `grep -q 'mcp-booking-sa' course-content/docs/labs/lab-08-agent-sandbox.md`
    - Cold/warm timings cited (numbers from SUMMARY): `grep -q -E 's[ )].*[Cc]old\|[Cc]old.*s[ )]\|[Ww]arm.*s' course-content/docs/labs/lab-08-agent-sandbox.md`
    - Canonical demo present: `grep -q 'severe tooth pain since yesterday' course-content/docs/labs/lab-08-agent-sandbox.md`
    - References absolute relative paths to solution: `grep -c 'course-code/labs/lab-08/solution' course-content/docs/labs/lab-08-agent-sandbox.md` returns >= 5
    - No "Atharva" references: `! grep -i 'atharva' course-content/docs/labs/lab-08-agent-sandbox.md`
    - Frontmatter sidebar_position preserved: `grep -q 'sidebar_position: 9' course-content/docs/labs/lab-08-agent-sandbox.md`
    - `npx docusaurus build` from course-content/ exits 0 and prints "compiled successfully"
  </acceptance_criteria>
  <done>course-content/docs/labs/lab-08-agent-sandbox.md is a 300-600 line walkthrough covering install → build images → secrets → manifest application (with the gcr/port-forward branch in Part D.5) → Day-2 Chainlit → multi-step demo → cold-vs-warm timing → verification → 5 pitfalls. Cold/warm timings from plan 03-04's SUMMARY are embedded. Docusaurus build is green.</done>
</task>

</tasks>

<verification>
- File overwritten (was 25 lines, now 300-600 lines)
- All sections in correct order
- Live `npx docusaurus build` from `course-content/` exits 0
- Spot-check rendered HTML: admonitions, code blocks, tables all render correctly
</verification>

<success_criteria>
- SANDBOX-01..04 documentation closed: a student following only this page (with the solution code present) lands a multi-step query through the Sandbox Router and sees a booking in the ConfigMap
- gcr-vs-port-forward branching is explicit and easy to follow
- kindnet NetworkPolicy enforcement caveat is unambiguous
- Cold-vs-warm demo has actual observed numbers, not speculation
- Build is green
</success_criteria>

<output>
After completion, create `.planning/phases/03-agentops-labs-day-2/03-05-SUMMARY.md` documenting:
- Final line count of lab-08-agent-sandbox.md
- Whether gcr or port-forward mode was the active path during the verification build
- The 5 Common Pitfalls actually included
- Confirmation Docusaurus build succeeded
</output>
