---
phase: 03-agentops-labs-day-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - course-code/COURSE_VERSIONS.md
  - course-code/config.env
  - course-content/docs/labs/lab-06-web-ui.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "COURSE_VERSIONS.md lists every new pinned version Day 2 introduces (Hermes image, Sandbox CRD v0.4.3, MCP SDK 1.27.0, OTEL SDK 1.41.1, OTEL Collector chart 0.153.0, Tempo chart 1.24.4, Groq + Gemini model IDs)"
    - "config.env exports NS_AGENT=llm-agent and SANDBOX_VERSION=v0.4.3 so downstream lab manifests and scripts use a single source of truth"
    - "Lab 06 page ends with a 'Wind down before Day 2' subsection telling students to scale vLLM Deployment to 0 (D-19/D-20)"
  artifacts:
    - path: course-code/COURSE_VERSIONS.md
      provides: "Pinned versions for Day 2 components"
      contains: "Hermes Agent, Sandbox v0.4.3, mcp 1.27.0, OTEL Collector 0.153.0, Tempo 1.24.4, Groq llama-3.3-70b-versatile, Gemini gemini-2.5-flash"
    - path: course-code/config.env
      provides: "NS_AGENT + SANDBOX_VERSION + HERMES_IMAGE env exports"
      contains: "NS_AGENT=llm-agent"
    - path: course-content/docs/labs/lab-06-web-ui.md
      provides: "Wind down before Day 2 subsection"
      contains: "kubectl scale deploy vllm-smollm2 --replicas=0 -n llm-serving"
  key_links:
    - from: course-code/config.env
      to: Day 2 K8s manifests in lab-07/08/09
      via: "env-var substitution in apply scripts"
      pattern: "NS_AGENT|SANDBOX_VERSION|HERMES_IMAGE"
    - from: course-content/docs/labs/lab-06-web-ui.md
      to: course-content/docs/labs/lab-07-agent-core.md
      via: "Wind-down step at end of Lab 06 reduces RAM before Lab 07"
      pattern: "kubectl scale deploy vllm-smollm2 --replicas=0"
---

<objective>
Land the foundational pieces every Day 2 lab depends on: pin all new component versions in COURSE_VERSIONS.md, extend config.env with the agent namespace + sandbox version + Hermes image variables, and append a "Wind down before Day 2" subsection to Lab 06 so students free ~2-4 GB RAM (vLLM scaled to 0) before they touch Lab 07. Closes locked decisions D-19 and D-20.

Purpose: Without this plan, every downstream Day 2 plan duplicates version pinning and namespace setup, and the cluster will run out of memory partway through Lab 07 because vLLM is still up.
Output: COURSE_VERSIONS.md + config.env updated; Lab 06 page has the wind-down subsection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agentops-labs-day-2/03-CONTEXT.md
@.planning/phases/03-agentops-labs-day-2/03-RESEARCH.md
@CLAUDE.md
@course-code/COURSE_VERSIONS.md
@course-code/config.env
@course-content/docs/labs/lab-06-web-ui.md

<interfaces>
Existing config.env keys (do NOT remove or rename, only ADD):
```
CLUSTER_NAME=llmops-kind
PROJECT_DIR=./llmops-project
MODEL_IMAGE_TAG=v1.0.0
VLLM_IMAGE=schoolofdevops/vllm-cpu-nonuma:0.9.1
BASE_MODEL=HuggingFaceTB/SmolLM2-135M-Instruct
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
NS_SERVING=llm-serving
NS_APP=llm-app
NS_MONITORING=monitoring
NS_ARGOCD=argocd
NS_ARGO_WORKFLOWS=argo-workflows
```

Existing Lab 06 page structure ends with (line ~321 onward):
```
## After This Lab
You have completed the full Day 1 LLMOps pipeline:
| Component | URL | Status |
| RAG Retriever | http://localhost:30100/search | Running |
| vLLM API | http://localhost:30200/v1/chat/completions | Running |
...
```
The new "Wind down before Day 2" subsection MUST go BEFORE the existing "## After This Lab" header — i.e. it lives between the Verification section and the After This Lab summary.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update COURSE_VERSIONS.md with Day 2 pinned versions</name>
  <files>course-code/COURSE_VERSIONS.md</files>
  <read_first>
    - course-code/COURSE_VERSIONS.md (existing structure — table format, sections "Core Infrastructure / ML / LLM Stack / Serving & Deployment / Web UI & Agent / Documentation Site / Notes")
    - .planning/phases/03-agentops-labs-day-2/03-RESEARCH.md (sections "Version Pin Table" and "State of the Art" — every version comes from there, do not invent values)
    - .planning/phases/03-agentops-labs-day-2/03-CONTEXT.md (D-13/D-14/D-15 lab boundaries, D-16 Tempo, D-17 cost tracking)
  </read_first>
  <action>
Add a new section **"Agent + Observability (Day 2)"** to COURSE_VERSIONS.md immediately AFTER the existing "Web UI & Agent" section and BEFORE the "Documentation Site" section. Use the same Markdown table style as existing sections (`| Component | Pinned Version | Compatibility Reason |`).

Concrete rows to add (copy values verbatim from RESEARCH.md "Version Pin Table"):

| Component | Pinned Version | Compatibility Reason |
|-----------|---------------|----------------------|
| `nousresearch/hermes-agent` Docker image | `latest` | Only published Docker Hub tag (2.4 GB); pull at workshop delivery time. Requires 64K+ context window model |
| Kubernetes Agent Sandbox CRDs + controller | v0.4.3 | Latest as of 2026-04-28. KIND explicitly supported. SandboxWarmPool creates Sandbox CRs (v0.3.10+). API: `agents.x-k8s.io/v1alpha1` + `extensions.agents.x-k8s.io/v1alpha1` |
| `k8s-agent-sandbox` Python SDK | 0.4.3 | Matches CRD version; used by Chainlit to claim per-session Sandbox |
| MCP Python SDK (`mcp[cli]`) | 1.27.0 | FastMCP `streamable_http_app()` — Streamable HTTP transport (MCP spec 2025-03-26). SSE deprecated |
| `opentelemetry-sdk` | 1.41.1 | OTLP gRPC exporter; matches collector 0.151.0 |
| `opentelemetry-exporter-otlp-proto-grpc` | 1.41.1 | Tracks SDK version |
| `opentelemetry-instrumentation-httpx` | 0.62b1 | Auto-instruments httpx calls inside MCP tool servers (treatment_lookup → RAG retriever) |
| `opentelemetry-instrumentation-fastapi` | 0.62b1 | Auto-instruments FastMCP HTTP server |
| Grafana Tempo Helm chart | 1.24.4 | Single-binary mode, in-memory tmpfs storage — fits KIND |
| OpenTelemetry Collector Helm chart | 0.153.0 | `mode: deployment` (one pod) for KIND footprint |
| `kubernetes` Python client | 32.x (latest) | book_appointment MCP tool patches `bookings` ConfigMap via in-cluster RBAC |
| Groq recommended model | `llama-3.3-70b-versatile` | 128K context (satisfies Hermes 64K minimum); free tier 30 RPM / 6K TPM / 1000 RPD |
| Gemini recommended model | `gemini-2.5-flash` | 1M context; OpenAI-compat at `https://generativelanguage.googleapis.com/v1beta/openai/`. Free tier 10 RPM / 500 RPD |

Then in the existing "## Notes" section, ADD these bullets after the existing Hermes bullet:
- Hermes Agent requires a model with at least 64,000 token context window — `llama-3.3-70b-versatile` (Groq) and `gemini-2.5-flash` (Gemini) both qualify; SmolLM2-135M does not. Day 2 uses remote LLM APIs.
- Kubernetes Agent Sandbox v0.4.3 is alpha (`v1alpha1`). KIND default kindnet does NOT enforce NetworkPolicy — Lab 08 applies it as a documented production pattern only.
- The Sandbox Router image (`us-central1-docker.pkg.dev/k8s-staging-images/agent-sandbox/sandbox-router:latest-main`) is on GCR. Pullability without GCP credentials is verified at start of Lab 08; fallback is `kubectl port-forward` mode.
- vLLM image stays at the existing pin; Day 2 scales the Deployment to 0 (manifest preserved for Day 3 autoscaling labs).

Do NOT change any existing pinned versions. Update the "Last verified" line at top of file to today's date and append "+ Day 2 components added".
  </action>
  <verify>
    <automated>grep -c "v0.4.3" course-code/COURSE_VERSIONS.md && grep -c "1.41.1" course-code/COURSE_VERSIONS.md && grep -c "1.24.4" course-code/COURSE_VERSIONS.md && grep -c "0.153.0" course-code/COURSE_VERSIONS.md && grep -c "1.27.0" course-code/COURSE_VERSIONS.md && grep -c "llama-3.3-70b-versatile" course-code/COURSE_VERSIONS.md && grep -c "gemini-2.5-flash" course-code/COURSE_VERSIONS.md && grep -q "kindnet does NOT enforce NetworkPolicy" course-code/COURSE_VERSIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '## Agent + Observability (Day 2)' course-code/COURSE_VERSIONS.md` returns 0
    - `grep -q 'nousresearch/hermes-agent.*latest' course-code/COURSE_VERSIONS.md` returns 0
    - `grep -q 'Kubernetes Agent Sandbox.*v0.4.3' course-code/COURSE_VERSIONS.md` returns 0
    - `grep -q 'mcp\[cli\].*1.27.0' course-code/COURSE_VERSIONS.md` returns 0
    - `grep -q 'opentelemetry-sdk.*1.41.1' course-code/COURSE_VERSIONS.md` returns 0
    - `grep -q 'Grafana Tempo Helm chart.*1.24.4' course-code/COURSE_VERSIONS.md` returns 0
    - `grep -q 'OpenTelemetry Collector Helm chart.*0.153.0' course-code/COURSE_VERSIONS.md` returns 0
    - `grep -q 'llama-3.3-70b-versatile' course-code/COURSE_VERSIONS.md` returns 0
    - `grep -q 'gemini-2.5-flash' course-code/COURSE_VERSIONS.md` returns 0
    - `grep -q 'kindnet does NOT enforce NetworkPolicy' course-code/COURSE_VERSIONS.md` returns 0
    - The original `vllm.*0.9.1` line is still present (Day 1 pins untouched): `grep -c "0.9.1" course-code/COURSE_VERSIONS.md` returns >= 1
  </acceptance_criteria>
  <done>COURSE_VERSIONS.md has a new "Agent + Observability (Day 2)" table section with all 13 Day-2 component pins drawn verbatim from RESEARCH.md, plus 4 new bullets in Notes covering 64K context requirement, kindnet NetworkPolicy limitation, Sandbox Router image risk, and vLLM scale-to-0 strategy. Day 1 entries are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Extend config.env with Day 2 namespace and image variables</name>
  <files>course-code/config.env</files>
  <read_first>
    - course-code/config.env (existing key=value pairs — must preserve order and existing keys)
    - .planning/phases/03-agentops-labs-day-2/03-CONTEXT.md (code_context section: "Day 2 adds `llm-agent` for Sandbox + MCP tool resources")
    - .planning/phases/03-agentops-labs-day-2/03-RESEARCH.md (Lab-by-Lab Tech Stack Summary § "Lab 08" — "config.env additions" subsection)
  </read_first>
  <action>
Append (do NOT rewrite existing keys) the following block to the end of `course-code/config.env`:

```
# Day 2 — AgentOps namespace and image pins (Phase 3)
NS_AGENT=llm-agent
SANDBOX_VERSION=v0.4.3
HERMES_IMAGE=nousresearch/hermes-agent:latest
SANDBOX_ROUTER_IMAGE=us-central1-docker.pkg.dev/k8s-staging-images/agent-sandbox/sandbox-router:latest-main
HERMES_API_KEY=smile-dental-course-key
HERMES_PORT=8642
# LLM provider — students set ONE of these in their shell before running Lab 07
# GROQ_API_KEY=gsk_...
# GOOGLE_API_KEY=AIza...
# Default LLM provider config (Groq is the safer primary recommendation per RESEARCH.md Focus 7)
LLM_BASE_URL=https://api.groq.com/openai/v1
LLM_MODEL=llama-3.3-70b-versatile
# Day 2 observability
OTEL_COLLECTOR_VERSION=0.153.0
TEMPO_VERSION=1.24.4
```

Do NOT change `NS_SERVING`, `NS_APP`, `NS_MONITORING`, `NS_ARGOCD`, `NS_ARGO_WORKFLOWS`, `VLLM_IMAGE`, `BASE_MODEL`, `EMBEDDING_MODEL`, `MODEL_IMAGE_TAG`, `PROJECT_DIR`, or `CLUSTER_NAME`. The two GROQ/GOOGLE lines are commented out — students uncomment one. Comment immediately above each variable cluster explains its purpose.
  </action>
  <verify>
    <automated>grep -q '^NS_AGENT=llm-agent$' course-code/config.env && grep -q '^SANDBOX_VERSION=v0.4.3$' course-code/config.env && grep -q '^HERMES_IMAGE=nousresearch/hermes-agent:latest$' course-code/config.env && grep -q '^HERMES_PORT=8642$' course-code/config.env && grep -q '^LLM_BASE_URL=https://api.groq.com/openai/v1$' course-code/config.env && grep -q '^LLM_MODEL=llama-3.3-70b-versatile$' course-code/config.env && grep -q '^OTEL_COLLECTOR_VERSION=0.153.0$' course-code/config.env && grep -q '^TEMPO_VERSION=1.24.4$' course-code/config.env && grep -q '^NS_APP=llm-app$' course-code/config.env && grep -q '^NS_MONITORING=monitoring$' course-code/config.env</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^NS_AGENT=llm-agent$' course-code/config.env` returns 0
    - `grep -q '^SANDBOX_VERSION=v0.4.3$' course-code/config.env` returns 0
    - `grep -q '^HERMES_IMAGE=nousresearch/hermes-agent:latest$' course-code/config.env` returns 0
    - `grep -q '^LLM_MODEL=llama-3.3-70b-versatile$' course-code/config.env` returns 0
    - `grep -q '^OTEL_COLLECTOR_VERSION=0.153.0$' course-code/config.env` returns 0
    - `grep -q '^TEMPO_VERSION=1.24.4$' course-code/config.env` returns 0
    - Day 1 keys still present: `grep -q '^NS_APP=llm-app$' course-code/config.env` and `grep -q '^VLLM_IMAGE=' course-code/config.env`
    - File ends with newline (no `\` line continuation): `tail -c1 course-code/config.env | od -An -c | grep -q '\\n'`
  </acceptance_criteria>
  <done>config.env has 8 new uncommented exports (NS_AGENT, SANDBOX_VERSION, HERMES_IMAGE, SANDBOX_ROUTER_IMAGE, HERMES_API_KEY, HERMES_PORT, LLM_BASE_URL, LLM_MODEL, OTEL_COLLECTOR_VERSION, TEMPO_VERSION) plus 2 commented placeholders (GROQ_API_KEY, GOOGLE_API_KEY). All Day 1 keys are preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add "Wind down before Day 2" subsection to Lab 06 page</name>
  <files>course-content/docs/labs/lab-06-web-ui.md</files>
  <read_first>
    - course-content/docs/labs/lab-06-web-ui.md (entire file — must preserve all existing content; only INSERT a new subsection)
    - .planning/phases/03-agentops-labs-day-2/03-CONTEXT.md (D-19 + D-20: scale `vllm-smollm2` to 0 BEFORE Day 2; manifest stays in place; symmetric "scale back up" prelude in Phase 4)
    - .planning/phases/03-agentops-labs-day-2/03-RESEARCH.md (KIND Footprint Estimate table — vLLM scaled-to-0 saves ~4 GB)
    - course-code/labs/lab-04/solution/k8s/30-deploy-vllm.yaml (Deployment name: `vllm-smollm2`, namespace: `llm-serving`)
  </read_first>
  <action>
Insert a new subsection titled **"## Wind Down Before Day 2"** in `course-content/docs/labs/lab-06-web-ui.md` between the existing `## Verification` section and the existing `## After This Lab` section.

Verbatim section content (use this exact wording, do not paraphrase):

```markdown
## Wind Down Before Day 2

Day 2 swaps the local SmolLM2 model for a free-tier cloud LLM (Groq or Gemini) and adds the Hermes Agent + MCP tool servers + Kubernetes Agent Sandbox + OTEL collector + Tempo to the same KIND cluster. To make room on a 16 GB laptop, scale the vLLM Deployment to 0 replicas now. The Deployment stays in place — Day 3 autoscaling labs will scale it back up.

```bash
# Free ~2-4 GB RAM by stopping vLLM (keeps RAG retriever, Chainlit, Prometheus, Grafana running)
kubectl scale deployment vllm-smollm2 --replicas=0 -n llm-serving

# Verify pods terminated
kubectl get pods -n llm-serving -l app=vllm-smollm2
# Expected: No resources found in llm-serving namespace.

# Confirm Deployment manifest is preserved (replicas=0 but spec intact)
kubectl get deployment vllm-smollm2 -n llm-serving -o jsonpath='{.spec.replicas}{"\n"}'
# Expected: 0
```

:::warning Skipping the wind-down
If you jump directly from Lab 06 to Lab 07 without scaling vLLM down, your KIND cluster may run out of memory partway through Lab 07. The Hermes Agent container alone needs ~1.5 GB; the SandboxWarmPool in Lab 08 needs ~3-4 GB. If you skipped this step, run the `kubectl scale` command above before starting Lab 07.
:::

:::tip Coming back later
On Day 3 (Lab 10 — Autoscaling), you will scale vLLM back up with `kubectl scale deployment vllm-smollm2 --replicas=1 -n llm-serving` and watch HPA grow it under load.
:::
```

(End of new subsection. Note that the inner triple-backtick code fence inside the markdown sample above is escaped only conceptually — when writing the file, use real triple backticks for the bash code block.)

Do NOT modify any other section of the file. Do NOT change "Day 1 | Duration: ~45 minutes" frontmatter or any existing headings, tables, or commands.
  </action>
  <verify>
    <automated>grep -q '^## Wind Down Before Day 2$' course-content/docs/labs/lab-06-web-ui.md && grep -q 'kubectl scale deployment vllm-smollm2 --replicas=0 -n llm-serving' course-content/docs/labs/lab-06-web-ui.md && grep -q '^## After This Lab$' course-content/docs/labs/lab-06-web-ui.md && grep -q ':::warning Skipping the wind-down' course-content/docs/labs/lab-06-web-ui.md && grep -q ':::tip Coming back later' course-content/docs/labs/lab-06-web-ui.md && [ $(grep -n '^## Wind Down Before Day 2$' course-content/docs/labs/lab-06-web-ui.md | cut -d: -f1) -lt $(grep -n '^## After This Lab$' course-content/docs/labs/lab-06-web-ui.md | cut -d: -f1) ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^## Wind Down Before Day 2$' course-content/docs/labs/lab-06-web-ui.md` returns 0
    - `grep -q 'kubectl scale deployment vllm-smollm2 --replicas=0 -n llm-serving' course-content/docs/labs/lab-06-web-ui.md` returns 0
    - `grep -q ':::warning Skipping the wind-down' course-content/docs/labs/lab-06-web-ui.md` returns 0
    - `grep -q ':::tip Coming back later' course-content/docs/labs/lab-06-web-ui.md` returns 0
    - The new section sits BEFORE "## After This Lab": line number of `^## Wind Down Before Day 2$` is less than line number of `^## After This Lab$`
    - Lab 06 still has its original "## Verification" section: `grep -q '^## Verification$' course-content/docs/labs/lab-06-web-ui.md`
    - Existing Day 1 content preserved: `grep -q 'Time to First Token' course-content/docs/labs/lab-06-web-ui.md`
  </acceptance_criteria>
  <done>Lab 06 page has a new "## Wind Down Before Day 2" subsection between Verification and After This Lab, containing the exact `kubectl scale ... --replicas=0` command, a verification check, a Docusaurus warning admonition, and a tip pointing forward to Day 3. No other Lab 06 content is modified.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
- COURSE_VERSIONS.md has a new "Agent + Observability (Day 2)" section with 13 pinned versions
- config.env has 8 new exports + 2 commented placeholders for student API keys
- Lab 06 page has the wind-down subsection in the correct position
- Day 1 content (config.env existing keys, COURSE_VERSIONS.md existing rows, Lab 06 existing sections) is fully preserved

Run `git diff --stat` — should show 3 files changed, all ADDITIONS (no large deletions).
</verification>

<success_criteria>
- All 13 Day-2 pinned versions visible in COURSE_VERSIONS.md (Hermes, Sandbox v0.4.3, mcp 1.27.0, OTEL SDK 1.41.1, OTEL Collector 0.153.0, Tempo 1.24.4, kubernetes client, Groq + Gemini models)
- config.env defines NS_AGENT, SANDBOX_VERSION, HERMES_IMAGE, LLM_BASE_URL, LLM_MODEL
- Lab 06 page contains the `kubectl scale deployment vllm-smollm2 --replicas=0 -n llm-serving` command in a new "Wind Down Before Day 2" subsection
- Symmetric "scale back up" tip is present (forward reference to Day 3 / Lab 10)
</success_criteria>

<output>
After completion, create `.planning/phases/03-agentops-labs-day-2/03-01-SUMMARY.md` documenting:
- Versions pinned in COURSE_VERSIONS.md
- New config.env exports
- Exact location of the Lab 06 wind-down subsection
- Any deviations from the planned wording (should be none)
</output>
