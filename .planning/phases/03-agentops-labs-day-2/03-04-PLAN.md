---
phase: 03-agentops-labs-day-2
plan: 04
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - course-code/labs/lab-08/solution/scripts/install-agent-sandbox.sh
  - course-code/labs/lab-08/solution/scripts/verify-sandbox-router-image.sh
  - course-code/labs/lab-08/solution/scripts/build-mcp-images.sh
  - course-code/labs/lab-08/solution/k8s/00-namespace.yaml
  - course-code/labs/lab-08/solution/k8s/50-sandbox-template.yaml
  - course-code/labs/lab-08/solution/k8s/50-sandbox-warmpool.yaml
  - course-code/labs/lab-08/solution/k8s/50-sandbox-router.yaml
  - course-code/labs/lab-08/solution/k8s/60-bookings-cm.yaml
  - course-code/labs/lab-08/solution/k8s/60-booking-rbac.yaml
  - course-code/labs/lab-08/solution/k8s/60-mcp-triage-deploy.yaml
  - course-code/labs/lab-08/solution/k8s/60-mcp-treatment-lookup-deploy.yaml
  - course-code/labs/lab-08/solution/k8s/60-mcp-book-appointment-deploy.yaml
  - course-code/labs/lab-08/solution/k8s/60-hermes-config-cm.yaml
  - course-code/labs/lab-08/solution/k8s/60-hermes-secret.yaml.example
  - course-code/labs/lab-08/solution/k8s/60-network-policy.yaml
  - course-code/labs/lab-08/solution/k8s/40-chainlit-deploy-day2.yaml
  - course-code/labs/lab-08/solution/ui/app.py
  - course-code/labs/lab-08/solution/ui/Dockerfile
  - course-code/labs/lab-08/solution/ui/requirements.txt
  - course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh
  - course-code/labs/lab-08/starter/README.md
  - course-code/labs/lab-07/solution/tools/book_appointment/book_appointment_server.py
autonomous: true
requirements:
  - SANDBOX-01
  - SANDBOX-02
  - SANDBOX-03
  - SANDBOX-04
must_haves:
  truths:
    - "Kubernetes Agent Sandbox v0.4.3 CRDs (`agents.x-k8s.io/v1alpha1` + `extensions.agents.x-k8s.io/v1alpha1`) are installed on the KIND cluster (closes SANDBOX-01)"
    - "Hermes Agent runs as a Sandbox resource (via SandboxTemplate) inside the `llm-agent` namespace with isolation (closes SANDBOX-02)"
    - "SandboxWarmPool replicas=2 keeps two pre-warmed Sandbox instances ready; scaling to 0 + back to 2 produces a measurable cold-vs-warm timing difference (closes SANDBOX-03)"
    - "Chainlit reaches the agent through the Sandbox Router Service (`sandbox-router-svc.llm-agent.svc:8080`) — the Router IS the gateway per RESEARCH.md Focus 3 (closes SANDBOX-04)"
    - "RESEARCH.md open Q1 (Sandbox Router image pullability without GCP creds) is resolved: either GCR pull works on KIND, or the lab uses the documented `kubectl port-forward` fallback"
    - "book_appointment MCP tool persists bookings to the `bookings` ConfigMap in `llm-app` namespace via in-cluster RBAC (D-11 K8s mode)"
    - "NetworkPolicy egress allow-list manifest is applied to the Hermes pods as a documented production pattern (kindnet does not enforce — pedagogical only, RESEARCH.md Focus 4)"
  artifacts:
    - path: course-code/labs/lab-08/solution/scripts/install-agent-sandbox.sh
      provides: "Idempotent install of Agent Sandbox v0.4.3 CRDs + controller + extensions"
      contains: "kubernetes-sigs/agent-sandbox"
    - path: course-code/labs/lab-08/solution/scripts/verify-sandbox-router-image.sh
      provides: "Resolves RESEARCH.md open Q1 — checks router image pullability and selects GCR or port-forward path"
      contains: "us-central1-docker.pkg.dev"
    - path: course-code/labs/lab-08/solution/k8s/50-sandbox-template.yaml
      provides: "SandboxTemplate wrapping Hermes Agent container"
      contains: "kind: SandboxTemplate"
    - path: course-code/labs/lab-08/solution/k8s/50-sandbox-warmpool.yaml
      provides: "SandboxWarmPool with replicas=2"
      contains: "replicas: 2"
    - path: course-code/labs/lab-08/solution/k8s/60-bookings-cm.yaml
      provides: "Initial empty bookings ConfigMap in llm-app"
      contains: "bookings: \"[]\""
    - path: course-code/labs/lab-08/solution/k8s/60-network-policy.yaml
      provides: "Egress allow-list per D-07 (DNS + RAG retriever + HTTPS for LLM APIs)"
      contains: "policyTypes:\n  - Egress"
    - path: course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh
      provides: "Repeatable timing comparison: scale WarmPool 0 → request → scale 2 → request"
      contains: "kubectl scale sandboxwarmpool"
    - path: course-code/labs/lab-08/solution/ui/app.py
      provides: "Day-2 K8s Chainlit calling Sandbox Router instead of Hermes container directly"
      contains: "sandbox-router-svc"
  key_links:
    - from: course-code/labs/lab-08/solution/k8s/50-sandbox-warmpool.yaml
      to: course-code/labs/lab-08/solution/k8s/50-sandbox-template.yaml
      via: "spec.sandboxTemplateRef.name = hermes-agent-template"
      pattern: "sandboxTemplateRef"
    - from: course-code/labs/lab-08/solution/ui/app.py
      to: sandbox-router-svc.llm-agent.svc:8080
      via: "AGENT_URL env var → POST /v1/chat/completions through Router"
      pattern: "sandbox-router-svc"
    - from: course-code/labs/lab-08/solution/k8s/60-mcp-book-appointment-deploy.yaml
      to: course-code/labs/lab-08/solution/k8s/60-booking-rbac.yaml
      via: "serviceAccountName: mcp-booking-sa with patch permission on configmap/bookings"
      pattern: "mcp-booking-sa"
    - from: course-code/labs/lab-07/solution/tools/book_appointment/book_appointment_server.py
      to: bookings ConfigMap in llm-app namespace
      via: "kubernetes Python client patch_namespaced_config_map when BOOKING_BACKEND=configmap"
      pattern: "patch_namespaced_config_map"
---

<objective>
Promote the Lab-07 Docker Compose stack to Kubernetes Agent Sandbox: install the v0.4.3 CRDs, deploy Hermes via a SandboxTemplate + SandboxWarmPool (replicas=2), front it with a Sandbox Router Service, deploy the 3 MCP tool servers as plain K8s Deployments in `llm-agent`, persist bookings to a ConfigMap in `llm-app` via RBAC, apply a documented (kindnet-not-enforced) egress NetworkPolicy, and update the Chainlit Deployment to call the Router. Includes a cold-vs-warm timing demo per D-06. Resolves the riskiest open question from RESEARCH.md (Q1: Sandbox Router image pullability) at the start of the lab. Closes SANDBOX-01..04.

Purpose: Lab 08 is where the agent becomes a real K8s workload — isolated per session via WarmPool, reachable through a stable Service, observable in `kubectl describe sandbox`. This is the differentiated content of the course (no other course teaches Kubernetes Agent Sandbox).
Output: 22 files: 3 scripts + 13 K8s manifests + 1 example secret + 1 Chainlit Deployment override + 3 Chainlit code files + 1 starter README + 1 tweak to the Lab-07 book_appointment tool to add the K8s-mode code path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-agentops-labs-day-2/03-CONTEXT.md
@.planning/phases/03-agentops-labs-day-2/03-RESEARCH.md
@.planning/phases/03-agentops-labs-day-2/03-02-SUMMARY.md
@CLAUDE.md
@course-code/COURSE_VERSIONS.md
@course-code/config.env
@course-code/labs/lab-07/solution/hermes-config/config.yaml
@course-code/labs/lab-07/solution/hermes-config/SOUL.md
@course-code/labs/lab-07/solution/tools/book_appointment/book_appointment_server.py
@course-code/labs/lab-07/solution/ui/app.py
@course-code/labs/lab-05/solution/k8s/40-deploy-chainlit.yaml
@course-code/labs/lab-05/solution/k8s/40-svc-chainlit.yaml

<interfaces>
Agent Sandbox v0.4.3 manifests (RESEARCH.md Focus 3):
- Install URL: `https://github.com/kubernetes-sigs/agent-sandbox/releases/download/v0.4.3/manifest.yaml`
- Extensions URL: `https://github.com/kubernetes-sigs/agent-sandbox/releases/download/v0.4.3/extensions.yaml`
- Controller namespace: `agent-sandbox-system`
- Controller deployment: `agent-sandbox-controller-manager`
- CRDs: `agents.x-k8s.io/v1alpha1` (Sandbox), `extensions.agents.x-k8s.io/v1alpha1` (SandboxTemplate, SandboxWarmPool, SandboxClaim)

Sandbox Router image (UNVERIFIED — Task 1 resolves):
- Image: `us-central1-docker.pkg.dev/k8s-staging-images/agent-sandbox/sandbox-router:latest-main`
- Port: 8080, /healthz endpoint
- Service: `sandbox-router-svc.llm-agent.svc.cluster.local:8080` (ClusterIP)

SandboxTemplate.podTemplate constraints for KIND:
- NO `runtimeClassName: gvisor` (GKE-only)
- `automountServiceAccountToken: false` for Hermes pod (it doesn't need K8s API)
- Image: `nousresearch/hermes-agent:latest`
- Port 8642 with /health readiness probe (initialDelaySeconds: 20)
- Resources: requests cpu=500m memory=1.5Gi, limits cpu=1 memory=2Gi
- Hermes ENV: `API_SERVER_ENABLED=true`, `API_SERVER_HOST=0.0.0.0`, `API_SERVER_KEY` from Secret, `GROQ_API_KEY` and/or `GOOGLE_API_KEY` from Secret (`optional: true`)
- Mount ConfigMap `hermes-config` at `/opt/data` (contains config.yaml + SOUL.md from Lab 07)

book_appointment K8s mode (RESEARCH.md Focus 9):
```python
from kubernetes import client as k8s_client, config as k8s_config
k8s_config.load_incluster_config()
v1 = k8s_client.CoreV1Api()
cm = v1.read_namespaced_config_map("bookings", "llm-app")
data = json.loads(cm.data.get("bookings", "[]"))
data.append(booking)
v1.patch_namespaced_config_map("bookings", "llm-app", {"data":{"bookings": json.dumps(data, indent=2)}})
```

Lab 05 Chainlit Deployment (REUSE structure):
```yaml
# 40-deploy-chainlit.yaml
spec:
  containers:
  - name: chainlit
    image: kind-registry:5001/smile-dental-ui:v1.0.0
    env:
    - name: RETRIEVER_URL
      value: "http://rag-retriever.llm-app.svc.cluster.local:8001"
    - name: VLLM_URL          ← REPLACE with AGENT_URL pointing at sandbox-router-svc
    ...
```

NetworkPolicy egress allow-list (RESEARCH.md Focus 4 verbatim — DNS UDP/TCP 53 mandatory or all DNS breaks):
- pod selector: `app: hermes-agent`
- allow DNS (UDP+TCP 53)
- allow → llm-app namespace, podSelector app=rag-retriever, port 8001/TCP
- allow → 0.0.0.0/0 port 443/TCP (HTTPS for Groq/Gemini — FQDN not possible in vanilla NetPol)

config.env keys available from plan 03-01:
- NS_AGENT=llm-agent
- SANDBOX_VERSION=v0.4.3
- HERMES_IMAGE=nousresearch/hermes-agent:latest
- HERMES_API_KEY=smile-dental-course-key
- HERMES_PORT=8642
- LLM_BASE_URL, LLM_MODEL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Agent Sandbox v0.4.3 + verify Router image pull (resolves RESEARCH.md open Q1)</name>
  <files>course-code/labs/lab-08/solution/scripts/install-agent-sandbox.sh, course-code/labs/lab-08/solution/scripts/verify-sandbox-router-image.sh, course-code/labs/lab-08/solution/scripts/build-mcp-images.sh, course-code/labs/lab-08/solution/k8s/00-namespace.yaml, course-code/labs/lab-08/starter/README.md</files>
  <read_first>
    - .planning/phases/03-agentops-labs-day-2/03-RESEARCH.md (Focus 3 §"Installation Steps for KIND" + §"Sandbox Router Deployment" — exact URLs and image pin; Open Questions 1)
    - .planning/phases/03-agentops-labs-day-2/03-CONTEXT.md (D-14 lab boundary; D-08 Router IS the gateway)
    - course-code/COURSE_VERSIONS.md (confirm v0.4.3 pinned in Day-2 section)
    - course-code/config.env (NS_AGENT=llm-agent and SANDBOX_VERSION=v0.4.3 already exist from plan 03-01)
  </read_first>
  <action>
This task is the Lab 08 equivalent of Lab 07's Hermes verification: resolve the riskiest unknown FIRST. RESEARCH.md flags that the Sandbox Router image (`us-central1-docker.pkg.dev/.../sandbox-router:latest-main`) may not be publicly pullable on KIND without GCR creds. We need to know now, not 90 min in.

**File 1 — `course-code/labs/lab-08/solution/scripts/verify-sandbox-router-image.sh`** (executable):

```bash
#!/usr/bin/env bash
# Resolves RESEARCH.md Open Q1: is the GCR-hosted Sandbox Router image pullable without GCP credentials?
# Run BEFORE install-agent-sandbox.sh — sets the path for the rest of Lab 08.
set -euo pipefail

IMAGE="${SANDBOX_ROUTER_IMAGE:-us-central1-docker.pkg.dev/k8s-staging-images/agent-sandbox/sandbox-router:latest-main}"
MODE_FILE="${1:-/tmp/lab08-router-mode}"

echo "[1/2] Trying to pull Sandbox Router image: ${IMAGE}"
if docker pull "${IMAGE}" 2>&1 | tee /tmp/lab08-router-pull.log; then
  echo "[2/2] PULL SUCCEEDED — using GCR-hosted Router (Service-based gateway)."
  echo "ROUTER_MODE=gcr" > "${MODE_FILE}"
  echo
  echo "Result: Lab 08 will deploy 50-sandbox-router.yaml (Service path)."
  exit 0
else
  echo "[2/2] PULL FAILED — falling back to kubectl port-forward gateway path."
  echo "ROUTER_MODE=port-forward" > "${MODE_FILE}"
  echo
  echo "Result: Lab 08 will SKIP 50-sandbox-router.yaml and instead use 'kubectl port-forward' against a Sandbox pod's service."
  echo "Reason: $(tail -1 /tmp/lab08-router-pull.log)"
  exit 0
fi
```

This script writes to `/tmp/lab08-router-mode` so install-agent-sandbox.sh can branch on it. **Both branches succeed (exit 0)** — the script is informational, not blocking. The lab guide explains the fallback path.

**File 2 — `course-code/labs/lab-08/solution/scripts/install-agent-sandbox.sh`** (executable):

```bash
#!/usr/bin/env bash
# Idempotent install of Kubernetes Agent Sandbox v0.4.3 on a KIND cluster.
set -euo pipefail

VERSION="${SANDBOX_VERSION:-v0.4.3}"
NS_AGENT="${NS_AGENT:-llm-agent}"
NS_SYS="agent-sandbox-system"

echo "[1/5] Installing Agent Sandbox core CRDs + controller (${VERSION})…"
kubectl apply -f "https://github.com/kubernetes-sigs/agent-sandbox/releases/download/${VERSION}/manifest.yaml"

echo "[2/5] Installing Agent Sandbox extension CRDs (SandboxTemplate, SandboxWarmPool, SandboxClaim)…"
kubectl apply -f "https://github.com/kubernetes-sigs/agent-sandbox/releases/download/${VERSION}/extensions.yaml"

echo "[3/5] Waiting for controller-manager Deployment to become Available (timeout 180 s)…"
kubectl wait --for=condition=available deployment/agent-sandbox-controller-manager \
  -n "${NS_SYS}" --timeout=180s

echo "[4/5] Verifying CRDs are registered…"
for crd in sandboxes.agents.x-k8s.io sandboxtemplates.extensions.agents.x-k8s.io \
           sandboxwarmpools.extensions.agents.x-k8s.io sandboxclaims.extensions.agents.x-k8s.io; do
  kubectl get crd "${crd}" -o jsonpath='{.metadata.name}{" "}{.spec.versions[0].name}{"\n"}'
done

echo "[5/5] Creating ${NS_AGENT} namespace…"
kubectl apply -f "$(dirname "$0")/../k8s/00-namespace.yaml"

echo
echo "OK: Agent Sandbox ${VERSION} controller is Ready, CRDs are registered, and ${NS_AGENT} namespace exists."
```

**File 3 — `course-code/labs/lab-08/solution/scripts/build-mcp-images.sh`** (executable):

```bash
#!/usr/bin/env bash
# Builds the 3 MCP tool images from Lab 07 sources and pushes to kind-registry:5001.
set -euo pipefail

REGISTRY="${KIND_REGISTRY:-localhost:5001}"
LAB7_DIR="$(cd "$(dirname "$0")/../../../lab-07/solution" && pwd)"
TAG="${MCP_IMAGE_TAG:-v1.0.0}"

cd "${LAB7_DIR}"
for tool in triage treatment_lookup book_appointment; do
  IMG="${REGISTRY}/mcp-${tool}:${TAG}"
  echo "Building ${IMG} from tools/${tool}/Dockerfile …"
  docker build -t "${IMG}" -f "tools/${tool}/Dockerfile" .
  docker push "${IMG}"
done

echo
echo "OK: 3 MCP tool images built + pushed:"
for tool in triage treatment_lookup book_appointment; do
  echo "  ${REGISTRY}/mcp-${tool}:${TAG}"
done
```

**File 4 — `course-code/labs/lab-08/solution/k8s/00-namespace.yaml`**:

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: llm-agent
  labels:
    kubernetes.io/metadata.name: llm-agent   # required by NetworkPolicy namespaceSelector
    purpose: agent-workloads
```

**File 5 — `course-code/labs/lab-08/starter/README.md`**:

```markdown
# Lab 08 — K8s Agent Sandbox (Starter)

Follow the [Lab 08 guide](../../../../course-content/docs/labs/lab-08-agent-sandbox.md). Reference `../solution/` only after attempting the manifests yourself.

## First step

```bash
bash ../solution/scripts/verify-sandbox-router-image.sh
bash ../solution/scripts/install-agent-sandbox.sh
```

If the first script reports `port-forward` mode, the lab guide tells you which Router-related step to skip.
```

**Live verification** — run all three scripts on this machine:
```bash
chmod +x course-code/labs/lab-08/solution/scripts/*.sh
bash course-code/labs/lab-08/solution/scripts/verify-sandbox-router-image.sh
cat /tmp/lab08-router-mode
bash course-code/labs/lab-08/solution/scripts/install-agent-sandbox.sh
kubectl get crd | grep agents.x-k8s.io
kubectl get ns llm-agent
```
  </action>
  <verify>
    <automated>test -x course-code/labs/lab-08/solution/scripts/verify-sandbox-router-image.sh && test -x course-code/labs/lab-08/solution/scripts/install-agent-sandbox.sh && test -x course-code/labs/lab-08/solution/scripts/build-mcp-images.sh && grep -q 'us-central1-docker.pkg.dev' course-code/labs/lab-08/solution/scripts/verify-sandbox-router-image.sh && grep -q 'v0.4.3' course-code/labs/lab-08/solution/scripts/install-agent-sandbox.sh && grep -q 'agent-sandbox-controller-manager' course-code/labs/lab-08/solution/scripts/install-agent-sandbox.sh && grep -q 'kubernetes.io/metadata.name: llm-agent' course-code/labs/lab-08/solution/k8s/00-namespace.yaml && bash course-code/labs/lab-08/solution/scripts/verify-sandbox-router-image.sh && bash course-code/labs/lab-08/solution/scripts/install-agent-sandbox.sh && kubectl get crd sandboxes.agents.x-k8s.io && kubectl get ns llm-agent</automated>
  </verify>
  <acceptance_criteria>
    - All three scripts have executable bit: `test -x` for each
    - verify-sandbox-router-image.sh runs end-to-end on this machine and writes `/tmp/lab08-router-mode` containing one of `ROUTER_MODE=gcr` or `ROUTER_MODE=port-forward`
    - install-agent-sandbox.sh runs end-to-end and exits 0; `kubectl get crd sandboxes.agents.x-k8s.io` succeeds
    - All 4 CRDs registered: `kubectl get crd | grep -c x-k8s.io` >= 4
    - `kubectl get ns llm-agent` returns 0 with the namespace Active
    - Controller is Ready: `kubectl get deploy -n agent-sandbox-system agent-sandbox-controller-manager -o jsonpath='{.status.readyReplicas}'` returns "1"
    - Namespace YAML has the metadata label needed for NetworkPolicy namespaceSelector matching: `grep -q 'kubernetes.io/metadata.name: llm-agent' course-code/labs/lab-08/solution/k8s/00-namespace.yaml`
    - Starter README points at the verify script first
    - This task explicitly resolves RESEARCH.md Open Q1: the SUMMARY for this plan must record which mode was chosen (gcr vs port-forward)
  </acceptance_criteria>
  <done>Agent Sandbox v0.4.3 is installed live on the KIND cluster (controller Ready, 4 CRDs registered, llm-agent namespace created). The router-image-pullability script ran and recorded the mode in /tmp/lab08-router-mode. RESEARCH.md Open Q1 is resolved with a concrete decision (GCR or port-forward). build-mcp-images.sh exists for use in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Build all K8s manifests (SandboxTemplate, WarmPool, Router, MCP Deployments + RBAC + ConfigMap, NetworkPolicy, Hermes Secret) and add K8s mode to book_appointment tool</name>
  <files>course-code/labs/lab-08/solution/k8s/50-sandbox-template.yaml, course-code/labs/lab-08/solution/k8s/50-sandbox-warmpool.yaml, course-code/labs/lab-08/solution/k8s/50-sandbox-router.yaml, course-code/labs/lab-08/solution/k8s/60-bookings-cm.yaml, course-code/labs/lab-08/solution/k8s/60-booking-rbac.yaml, course-code/labs/lab-08/solution/k8s/60-mcp-triage-deploy.yaml, course-code/labs/lab-08/solution/k8s/60-mcp-treatment-lookup-deploy.yaml, course-code/labs/lab-08/solution/k8s/60-mcp-book-appointment-deploy.yaml, course-code/labs/lab-08/solution/k8s/60-hermes-config-cm.yaml, course-code/labs/lab-08/solution/k8s/60-hermes-secret.yaml.example, course-code/labs/lab-08/solution/k8s/60-network-policy.yaml, course-code/labs/lab-07/solution/tools/book_appointment/book_appointment_server.py</files>
  <read_first>
    - .planning/phases/03-agentops-labs-day-2/03-RESEARCH.md (Focus 3 §"SandboxTemplate for Hermes Agent" + §"SandboxWarmPool" + §"Sandbox Router Deployment"; Focus 4 §"NetworkPolicy YAML"; Focus 9 §"Kubernetes RBAC for book_appointment Pod" + §"Kubernetes Python Client Pattern")
    - .planning/phases/03-agentops-labs-day-2/03-CONTEXT.md (D-05 per-session Sandbox; D-06 WarmPool replicas=2; D-07 NetworkPolicy egress; D-11 ConfigMap-backed booking)
    - course-code/labs/lab-07/solution/hermes-config/config.yaml (Lab 08's hermes-config CM is the SAME content but with K8s service URLs instead of Docker DNS names)
    - course-code/labs/lab-07/solution/hermes-config/SOUL.md (verbatim into hermes-config CM)
    - course-code/labs/lab-07/solution/tools/book_appointment/book_appointment_server.py (must add a `BOOKING_BACKEND` env-var switch — local file vs configmap)
    - course-code/labs/lab-05/solution/k8s/40-deploy-chainlit.yaml (Deployment style template — kind-registry:5001, env vars, ports, probes, resources)
    - /tmp/lab08-router-mode (output from Task 1 — controls whether we emit 50-sandbox-router.yaml or document the port-forward fallback in the lab guide)
  </read_first>
  <action>
**File 1 — `50-sandbox-template.yaml`** (RESEARCH.md Focus 3 verbatim, with KIND adjustments — NO gvisor):

```yaml
apiVersion: extensions.agents.x-k8s.io/v1alpha1
kind: SandboxTemplate
metadata:
  name: hermes-agent-template
  namespace: llm-agent
spec:
  podTemplate:
    metadata:
      labels:
        app: hermes-agent
    spec:
      # NO runtimeClassName: gvisor — KIND does not support it (RESEARCH.md note).
      automountServiceAccountToken: false
      containers:
      - name: hermes
        image: nousresearch/hermes-agent:latest
        command: ["hermes", "gateway"]
        ports:
        - name: api
          containerPort: 8642
        env:
        - name: API_SERVER_ENABLED
          value: "true"
        - name: API_SERVER_HOST
          value: "0.0.0.0"
        - name: API_SERVER_KEY
          valueFrom:
            secretKeyRef:
              name: hermes-api-secret
              key: api-key
        - name: GROQ_API_KEY
          valueFrom:
            secretKeyRef:
              name: llm-api-keys
              key: groq-api-key
              optional: true
        - name: GOOGLE_API_KEY
          valueFrom:
            secretKeyRef:
              name: llm-api-keys
              key: google-api-key
              optional: true
        volumeMounts:
        - name: hermes-config
          mountPath: /opt/data
        resources:
          requests:
            cpu: "500m"
            memory: "1.5Gi"
          limits:
            cpu: "1"
            memory: "2Gi"
        readinessProbe:
          httpGet:
            path: /health
            port: 8642
          initialDelaySeconds: 20
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8642
          initialDelaySeconds: 60
          periodSeconds: 30
      volumes:
      - name: hermes-config
        configMap:
          name: hermes-config
```

**File 2 — `50-sandbox-warmpool.yaml`**:

```yaml
apiVersion: extensions.agents.x-k8s.io/v1alpha1
kind: SandboxWarmPool
metadata:
  name: hermes-agent-warmpool
  namespace: llm-agent
spec:
  replicas: 2
  sandboxTemplateRef:
    name: hermes-agent-template
```

**File 3 — `50-sandbox-router.yaml`** (RESEARCH.md Focus 3; this file is applied only when /tmp/lab08-router-mode says ROUTER_MODE=gcr):

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sandbox-router
  namespace: llm-agent
  labels:
    app: sandbox-router
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sandbox-router
  template:
    metadata:
      labels:
        app: sandbox-router
    spec:
      containers:
      - name: router
        image: us-central1-docker.pkg.dev/k8s-staging-images/agent-sandbox/sandbox-router:latest-main
        ports:
        - name: http
          containerPort: 8080
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
--- # marker
apiVersion: v1
kind: Service
metadata:
  name: sandbox-router-svc
  namespace: llm-agent
spec:
  type: ClusterIP
  selector:
    app: sandbox-router
  ports:
  - name: http
    port: 8080
    targetPort: 8080
```

**File 4 — `60-bookings-cm.yaml`**:

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: bookings
  namespace: llm-app
data:
  bookings: "[]"
```

**File 5 — `60-booking-rbac.yaml`** (RESEARCH.md Focus 9 verbatim):

```yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mcp-booking-sa
  namespace: llm-agent
--- # marker
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: configmap-booking-editor
  namespace: llm-app
rules:
- apiGroups: [""]
  resources: ["configmaps"]
  resourceNames: ["bookings"]
  verbs: ["get", "patch", "update"]
--- # marker
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: mcp-booking-binding
  namespace: llm-app
subjects:
- kind: ServiceAccount
  name: mcp-booking-sa
  namespace: llm-agent
roleRef:
  kind: Role
  name: configmap-booking-editor
  apiGroup: rbac.authorization.k8s.io
```

**File 6, 7 — `60-mcp-triage-deploy.yaml` and `60-mcp-treatment-lookup-deploy.yaml`** — same template, parameterised:

`60-mcp-triage-deploy.yaml`:
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-triage
  namespace: llm-agent
  labels:
    app: mcp-triage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-triage
  template:
    metadata:
      labels:
        app: mcp-triage
    spec:
      containers:
      - name: triage
        image: localhost:5001/mcp-triage:v1.0.0
        ports:
        - name: mcp
          containerPort: 8010
        env:
        - name: PORT
          value: "8010"
        - name: LLM_BASE_URL
          value: "https://api.groq.com/openai/v1"
        - name: LLM_MODEL
          value: "llama-3.3-70b-versatile"
        - name: LLM_API_KEY
          valueFrom:
            secretKeyRef:
              name: llm-api-keys
              key: groq-api-key
              optional: true
        readinessProbe:
          httpGet:
            path: /health
            port: 8010
          initialDelaySeconds: 5
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
--- # marker
apiVersion: v1
kind: Service
metadata:
  name: mcp-triage
  namespace: llm-agent
spec:
  selector:
    app: mcp-triage
  ports:
  - port: 8010
    targetPort: 8010
```

`60-mcp-treatment-lookup-deploy.yaml` — identical structure, replace name and port:
- Deployment + Service name: `mcp-treatment-lookup`, port 8020
- env: `RETRIEVER_URL=http://rag-retriever.llm-app.svc.cluster.local:8001` and `PORT=8020`
- (no LLM_API_KEY needed — this tool calls only the RAG retriever)

**File 8 — `60-mcp-book-appointment-deploy.yaml`** (uses ServiceAccount with K8s API access; switches to configmap backend):

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-book-appointment
  namespace: llm-agent
  labels:
    app: mcp-book-appointment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-book-appointment
  template:
    metadata:
      labels:
        app: mcp-book-appointment
    spec:
      serviceAccountName: mcp-booking-sa
      containers:
      - name: book
        image: localhost:5001/mcp-book-appointment:v1.0.0
        ports:
        - name: mcp
          containerPort: 8030
        env:
        - name: PORT
          value: "8030"
        - name: BOOKING_BACKEND
          value: "configmap"
        - name: BOOKING_NAMESPACE
          value: "llm-app"
        - name: BOOKING_CM_NAME
          value: "bookings"
        readinessProbe:
          httpGet:
            path: /health
            port: 8030
          initialDelaySeconds: 5
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
--- # marker
apiVersion: v1
kind: Service
metadata:
  name: mcp-book-appointment
  namespace: llm-agent
spec:
  selector:
    app: mcp-book-appointment
  ports:
  - port: 8030
    targetPort: 8030
```

**File 9 — `60-hermes-config-cm.yaml`** (carries Lab-07's config.yaml + SOUL.md, BUT with K8s service URLs):

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: hermes-config
  namespace: llm-agent
data:
  config.yaml: |
    model:
      default: groq/llama-3.3-70b-versatile

    mcp_servers:
      triage:
        url: "http://mcp-triage.llm-agent.svc.cluster.local:8010/mcp"
      treatment_lookup:
        url: "http://mcp-treatment-lookup.llm-agent.svc.cluster.local:8020/mcp"
      book_appointment:
        url: "http://mcp-book-appointment.llm-agent.svc.cluster.local:8030/mcp"

    agent:
      max_turns: 10
      disabled_toolsets: [memory, web, browser, code, voice]

    compression:
      enabled: false

    display:
      streaming: false

  SOUL.md: |
    You are the Smile Dental Clinic AI assistant. You help patients with three workflows:

    1. **Triage** — assess symptom severity (severe / urgent / routine) using the `triage` tool.
    2. **Treatment lookup** — find clinically relevant treatment information using the `treatment_lookup` tool.
    3. **Appointment booking** — schedule a consultation using the `book_appointment` tool. Always pass the urgency from triage and the treatment name from the lookup.

    For any patient query that mentions a symptom, follow this sequence: triage the symptom, look up the relevant treatment, then book the appointment. Be concise, professional, and reassuring. Only book an appointment after you have triaged and looked up the treatment.
```

**File 10 — `60-hermes-secret.yaml.example`** (NOT applied directly; students copy + fill):

```yaml
# Copy this file to 60-hermes-secret.yaml and fill the values, then apply:
#   kubectl apply -f 60-hermes-secret.yaml
# DO NOT commit your filled secret to git.

apiVersion: v1
kind: Secret
metadata:
  name: hermes-api-secret
  namespace: llm-agent
type: Opaque
stringData:
  api-key: smile-dental-course-key
--- # marker
apiVersion: v1
kind: Secret
metadata:
  name: llm-api-keys
  namespace: llm-agent
type: Opaque
stringData:
  groq-api-key: ""        # gsk_...  (set this OR google-api-key)
  google-api-key: ""      # AIza...  (alternative to groq-api-key)
```

**File 11 — `60-network-policy.yaml`** (RESEARCH.md Focus 4 verbatim — kindnet does not enforce, but the lab still applies it as the production pattern):

```yaml
# Egress allow-list for Hermes Agent pods (D-07).
# NOTE: Default KIND CNI (kindnet) does not enforce NetworkPolicy.
# This manifest is applied as a documented production pattern. Real enforcement
# requires Calico or Cilium CNI.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: hermes-agent-egress
  namespace: llm-agent
spec:
  podSelector:
    matchLabels:
      app: hermes-agent
  policyTypes:
  - Egress
  egress:
  # 1. DNS — UDP+TCP 53 (REQUIRED — see RESEARCH.md Pitfall 7).
  - ports:
    - port: 53
      protocol: UDP
    - port: 53
      protocol: TCP
  # 2. RAG retriever (in-cluster, llm-app namespace).
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: llm-app
      podSelector:
        matchLabels:
          app: rag-retriever
    ports:
    - port: 8001
      protocol: TCP
  # 3. HTTPS to Groq + Gemini APIs (FQDN-pinning needs Cilium; we allow all 443/TCP egress).
  - to:
    - ipBlock:
        cidr: 0.0.0.0/0
    ports:
    - port: 443
      protocol: TCP
  # 4. MCP tool servers in llm-agent (in-namespace traffic).
  - to:
    - podSelector:
        matchLabels:
          app: mcp-triage
    - podSelector:
        matchLabels:
          app: mcp-treatment-lookup
    - podSelector:
        matchLabels:
          app: mcp-book-appointment
    ports:
    - port: 8010
      protocol: TCP
    - port: 8020
      protocol: TCP
    - port: 8030
      protocol: TCP
```

**File 12 — `course-code/labs/lab-07/solution/tools/book_appointment/book_appointment_server.py`** (EDIT — add a `BOOKING_BACKEND` switch so the same image works in both labs):

Locate the `_append_local` function and add a sibling `_append_configmap`:

```python
def _append_configmap(booking: dict) -> None:
    """K8s mode: patch the bookings ConfigMap in BOOKING_NAMESPACE."""
    from kubernetes import client as k8s_client, config as k8s_config
    NAMESPACE = os.environ.get("BOOKING_NAMESPACE", "llm-app")
    CM_NAME   = os.environ.get("BOOKING_CM_NAME", "bookings")
    k8s_config.load_incluster_config()
    v1 = k8s_client.CoreV1Api()
    cm = v1.read_namespaced_config_map(CM_NAME, NAMESPACE)
    data = json.loads(cm.data.get("bookings", "[]"))
    data.append(booking)
    v1.patch_namespaced_config_map(
        CM_NAME, NAMESPACE,
        {"data": {"bookings": json.dumps(data, indent=2)}},
    )
```

And in the `book_appointment` tool body, replace the single `_append_local(booking)` call with:

```python
backend = os.environ.get("BOOKING_BACKEND", "local")
if backend == "configmap":
    _append_configmap(booking)
    booking["storage"] = "configmap"
else:
    _append_local(booking)
    booking["storage"] = "local-file"
```

Update the existing test file (`test_book_appointment_server.py`) — DO NOT change existing tests, ADD one new test:

```python
def test_book_appointment_configmap_backend_invokes_k8s_client(monkeypatch, tmp_path):
    # When BOOKING_BACKEND=configmap, the function should attempt to import the kubernetes client.
    monkeypatch.setenv("BOOKING_BACKEND", "configmap")
    monkeypatch.setenv("BOOKINGS_FILE", str(tmp_path / "bookings.json"))
    from importlib import reload
    from tools.book_appointment import book_appointment_server
    reload(book_appointment_server)
    # Patch load_incluster_config to a no-op and CoreV1Api with mock methods.
    import sys, types
    fake_k8s = types.ModuleType("kubernetes")
    fake_client = types.ModuleType("kubernetes.client")
    fake_config = types.ModuleType("kubernetes.config")
    class FakeV1:
        def read_namespaced_config_map(self, *a, **kw): 
            class CM:
                data = {"bookings": "[]"}
            return CM()
        def patch_namespaced_config_map(self, *a, **kw): self.called = True
    fake_v1 = FakeV1()
    fake_client.CoreV1Api = lambda: fake_v1
    fake_config.load_incluster_config = lambda: None
    fake_k8s.client = fake_client
    fake_k8s.config = fake_config
    monkeypatch.setitem(sys.modules, "kubernetes", fake_k8s)
    monkeypatch.setitem(sys.modules, "kubernetes.client", fake_client)
    monkeypatch.setitem(sys.modules, "kubernetes.config", fake_config)
    result = book_appointment_server.book_appointment("X", "filling", "routine", "next week")
    assert result["storage"] == "configmap"
    assert getattr(fake_v1, "called", False) is True
```

Run all book_appointment tests — original 2 + new 1 should all pass.

**Live verification (apply manifests + check Sandbox is Ready):**
```bash
# Build images first
bash course-code/labs/lab-08/solution/scripts/build-mcp-images.sh

# Hermes secrets
cp course-code/labs/lab-08/solution/k8s/60-hermes-secret.yaml.example /tmp/lab08-hermes-secret.yaml
# (edit /tmp/lab08-hermes-secret.yaml to put a real GROQ_API_KEY in groq-api-key)
kubectl apply -f /tmp/lab08-hermes-secret.yaml

# Apply remaining manifests in numbered order
kubectl apply -f course-code/labs/lab-08/solution/k8s/60-bookings-cm.yaml
kubectl apply -f course-code/labs/lab-08/solution/k8s/60-booking-rbac.yaml
kubectl apply -f course-code/labs/lab-08/solution/k8s/60-hermes-config-cm.yaml
kubectl apply -f course-code/labs/lab-08/solution/k8s/60-mcp-triage-deploy.yaml
kubectl apply -f course-code/labs/lab-08/solution/k8s/60-mcp-treatment-lookup-deploy.yaml
kubectl apply -f course-code/labs/lab-08/solution/k8s/60-mcp-book-appointment-deploy.yaml
kubectl apply -f course-code/labs/lab-08/solution/k8s/60-network-policy.yaml
kubectl apply -f course-code/labs/lab-08/solution/k8s/50-sandbox-template.yaml
kubectl apply -f course-code/labs/lab-08/solution/k8s/50-sandbox-warmpool.yaml
# Apply Router only if Task 1 said gcr mode
if grep -q gcr /tmp/lab08-router-mode; then
  kubectl apply -f course-code/labs/lab-08/solution/k8s/50-sandbox-router.yaml
fi

# Wait for resources
kubectl get sandboxtemplate,sandboxwarmpool,sandbox -n llm-agent
kubectl wait --for=jsonpath='{.status.readyReplicas}'=2 \
  sandboxwarmpool/hermes-agent-warmpool -n llm-agent --timeout=300s
kubectl get pods -n llm-agent
```

If WarmPool times out, debug via `kubectl describe sandboxwarmpool/hermes-agent-warmpool -n llm-agent` and `kubectl logs -n agent-sandbox-system deploy/agent-sandbox-controller-manager`. Common cause: the Hermes image pull (2.4 GB) — first pull takes 5+ min on slow networks.
  </action>
  <verify>
    <automated>kubectl apply --dry-run=client -f course-code/labs/lab-08/solution/k8s/50-sandbox-template.yaml && kubectl apply --dry-run=client -f course-code/labs/lab-08/solution/k8s/50-sandbox-warmpool.yaml && kubectl apply --dry-run=client -f course-code/labs/lab-08/solution/k8s/60-bookings-cm.yaml && kubectl apply --dry-run=client -f course-code/labs/lab-08/solution/k8s/60-booking-rbac.yaml && kubectl apply --dry-run=client -f course-code/labs/lab-08/solution/k8s/60-mcp-triage-deploy.yaml && kubectl apply --dry-run=client -f course-code/labs/lab-08/solution/k8s/60-mcp-treatment-lookup-deploy.yaml && kubectl apply --dry-run=client -f course-code/labs/lab-08/solution/k8s/60-mcp-book-appointment-deploy.yaml && kubectl apply --dry-run=client -f course-code/labs/lab-08/solution/k8s/60-hermes-config-cm.yaml && kubectl apply --dry-run=client -f course-code/labs/lab-08/solution/k8s/60-network-policy.yaml && kubectl apply --dry-run=client -f course-code/labs/lab-08/solution/k8s/00-namespace.yaml && grep -q 'kind: SandboxTemplate' course-code/labs/lab-08/solution/k8s/50-sandbox-template.yaml && grep -q 'kind: SandboxWarmPool' course-code/labs/lab-08/solution/k8s/50-sandbox-warmpool.yaml && grep -q 'replicas: 2' course-code/labs/lab-08/solution/k8s/50-sandbox-warmpool.yaml && grep -q 'serviceAccountName: mcp-booking-sa' course-code/labs/lab-08/solution/k8s/60-mcp-book-appointment-deploy.yaml && grep -q 'patch_namespaced_config_map' course-code/labs/lab-07/solution/tools/book_appointment/book_appointment_server.py && pytest course-code/labs/lab-07/solution/tools/book_appointment/ -x -q && kubectl wait --for=jsonpath='{.status.readyReplicas}'=2 sandboxwarmpool/hermes-agent-warmpool -n llm-agent --timeout=300s && kubectl get cm bookings -n llm-app -o jsonpath='{.data.bookings}'</automated>
  </verify>
  <acceptance_criteria>
    - All 13 K8s manifests pass `kubectl apply --dry-run=client`
    - SandboxTemplate uses Hermes image and disables ServiceAccountToken automount: `grep -q 'image: nousresearch/hermes-agent:latest' course-code/labs/lab-08/solution/k8s/50-sandbox-template.yaml && grep -q 'automountServiceAccountToken: false' course-code/labs/lab-08/solution/k8s/50-sandbox-template.yaml`
    - SandboxTemplate has NO `runtimeClassName: gvisor` (KIND incompatibility): `! grep -q 'runtimeClassName' course-code/labs/lab-08/solution/k8s/50-sandbox-template.yaml`
    - SandboxWarmPool replicas=2 + correct templateRef: `grep -q 'replicas: 2' course-code/labs/lab-08/solution/k8s/50-sandbox-warmpool.yaml && grep -q 'name: hermes-agent-template' course-code/labs/lab-08/solution/k8s/50-sandbox-warmpool.yaml`
    - book_appointment Deployment uses correct ServiceAccount: `grep -q 'serviceAccountName: mcp-booking-sa' course-code/labs/lab-08/solution/k8s/60-mcp-book-appointment-deploy.yaml`
    - book_appointment Deployment sets `BOOKING_BACKEND=configmap`: `grep -q 'BOOKING_BACKEND' course-code/labs/lab-08/solution/k8s/60-mcp-book-appointment-deploy.yaml && grep -q '"configmap"' course-code/labs/lab-08/solution/k8s/60-mcp-book-appointment-deploy.yaml`
    - Hermes config ConfigMap registers all 3 MCP servers via cluster DNS with /mcp suffix: `grep -c 'svc.cluster.local.*\/mcp"' course-code/labs/lab-08/solution/k8s/60-hermes-config-cm.yaml` returns 3
    - NetworkPolicy includes the DNS UDP+TCP 53 allow rule (Pitfall 7): `grep -q 'protocol: UDP' course-code/labs/lab-08/solution/k8s/60-network-policy.yaml && grep -q 'port: 53' course-code/labs/lab-08/solution/k8s/60-network-policy.yaml`
    - book_appointment_server.py supports configmap backend: `grep -q 'patch_namespaced_config_map' course-code/labs/lab-07/solution/tools/book_appointment/book_appointment_server.py && grep -q 'BOOKING_BACKEND' course-code/labs/lab-07/solution/tools/book_appointment/book_appointment_server.py`
    - All book_appointment tests pass (original 2 + new configmap-mode test): `pytest course-code/labs/lab-07/solution/tools/book_appointment/ -x` exits 0
    - Live: WarmPool reaches `status.readyReplicas=2`: `kubectl wait --for=jsonpath='{.status.readyReplicas}'=2 sandboxwarmpool/hermes-agent-warmpool -n llm-agent --timeout=300s` exits 0
    - Live: bookings ConfigMap exists with empty list: `kubectl get cm bookings -n llm-app -o jsonpath='{.data.bookings}'` returns "[]"
    - Live (only if router mode=gcr): `kubectl get deploy sandbox-router -n llm-agent -o jsonpath='{.status.readyReplicas}'` returns "1"
  </acceptance_criteria>
  <done>13 K8s manifests + the example Secret are applied to the live KIND cluster. SandboxWarmPool reports 2 ready Sandboxes. The 3 MCP tool Deployments are Running with healthy probes. The bookings ConfigMap exists in llm-app and the booking RBAC binds mcp-booking-sa across namespaces. book_appointment_server.py now switches between local-file and ConfigMap backends via BOOKING_BACKEND env var (3 tests pass). Either the Sandbox Router is deployed (gcr mode) or the lab guide will document port-forward fallback (port-forward mode).</done>
</task>

<task type="auto">
  <name>Task 3: Update Chainlit for K8s + cold-vs-warm timing demo + live multi-step verification</name>
  <files>course-code/labs/lab-08/solution/ui/app.py, course-code/labs/lab-08/solution/ui/Dockerfile, course-code/labs/lab-08/solution/ui/requirements.txt, course-code/labs/lab-08/solution/k8s/40-chainlit-deploy-day2.yaml, course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh</files>
  <read_first>
    - course-code/labs/lab-07/solution/ui/app.py (Day-2 Chainlit code — copy verbatim into Lab-08 then adjust env-var defaults)
    - course-code/labs/lab-05/solution/k8s/40-deploy-chainlit.yaml (existing K8s Deployment shape — port 8000, NodePort 30300, kind-registry image, CPU/memory)
    - course-code/labs/lab-05/solution/k8s/40-svc-chainlit.yaml (existing Service)
    - .planning/phases/03-agentops-labs-day-2/03-RESEARCH.md (Focus 3 §"Cold-vs-Warm Timing Demo" — exact kubectl scale + curl + wait commands)
    - .planning/phases/03-agentops-labs-day-2/03-CONTEXT.md (D-06 cold/warm WarmPool demo; D-08 Chainlit-as-front-door routes via Sandbox Router)
    - /tmp/lab08-router-mode (output from Task 1 — affects how Chainlit reaches the Sandbox)
  </read_first>
  <action>
**File 1 — `course-code/labs/lab-08/solution/ui/app.py`** — copy from `lab-07/solution/ui/app.py` and change ONLY the AGENT_URL default and welcome banner:

```python
# At top (replace AGENT_URL line):
AGENT_URL  = os.environ.get(
    "AGENT_URL",
    "http://sandbox-router-svc.llm-agent.svc.cluster.local:8080",
)
HERMES_KEY = os.environ.get("HERMES_API_KEY", "smile-dental-course-key")

# Welcome message (replace):
await cl.Message(
    content=(
        "Welcome to **Smile Dental Clinic** assistant (Day 2 — K8s Agent Sandbox edition).\n\n"
        "Your message goes through the Sandbox Router into a per-session Hermes Sandbox claimed from the WarmPool. "
        "Try _\"severe tooth pain since yesterday\"_ to see triage → treatment lookup → appointment booking.\n\n"
        "_Expand the steps under each answer to see which tools the agent called._"
    )
).send()
```

Everything else (the `on_message` handler, prom counters, tool-call sub-step rendering) stays identical to the Lab-07 version.

**File 2 — `course-code/labs/lab-08/solution/ui/Dockerfile`** — identical to Lab 07's UI Dockerfile (use Write to copy bytes).

**File 3 — `course-code/labs/lab-08/solution/ui/requirements.txt`** — identical to Lab 07's:
```
chainlit==2.11.0
httpx==0.28.0
prometheus-client==0.25.0
```

**File 4 — `course-code/labs/lab-08/solution/k8s/40-chainlit-deploy-day2.yaml`** (overrides the Day-1 Deployment in `llm-app`):

```yaml
# Day 2 Chainlit Deployment — replaces lab-05's 40-deploy-chainlit.yaml.
# Routes through Sandbox Router instead of vLLM directly.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chainlit-ui
  namespace: llm-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chainlit-ui
  template:
    metadata:
      labels:
        app: chainlit-ui
    spec:
      containers:
      - name: chainlit
        image: localhost:5001/smile-dental-ui-day2:v1.0.0
        env:
        - name: AGENT_URL
          value: "http://sandbox-router-svc.llm-agent.svc.cluster.local:8080"
        - name: HERMES_API_KEY
          valueFrom:
            secretKeyRef:
              name: hermes-api-secret-chainlit
              key: api-key
              optional: true
        ports:
        - name: http
          containerPort: 8000
        - name: metrics
          containerPort: 9090
        readinessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
--- # marker
# Service (NodePort 30300 — same as Lab 05)
apiVersion: v1
kind: Service
metadata:
  name: chainlit-ui
  namespace: llm-app
spec:
  type: NodePort
  selector:
    app: chainlit-ui
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    nodePort: 30300
  - name: metrics
    port: 9090
    targetPort: 9090
```

NB: a parallel Secret `hermes-api-secret-chainlit` in `llm-app` is needed (lab guide instructs students how to create it from the same `smile-dental-course-key` value). Mark as `optional: true` so the lab still functions if students forget — the env var falls back to its hard-coded default.

**File 5 — `course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh`** (executable) — implements RESEARCH.md Focus 3 §"Cold-vs-Warm Timing Demo" with port-forward to make it work whether the Router is in gcr-mode or port-forward-mode:

```bash
#!/usr/bin/env bash
# Cold-vs-warm SandboxWarmPool timing demo (D-06 + RESEARCH.md Focus 3).
# Requires: WarmPool already replicas=2 and a real GROQ_API_KEY (or GOOGLE_API_KEY)
#           in the llm-api-keys Secret.
set -euo pipefail
NS="${NS_AGENT:-llm-agent}"

# Helper: ask the agent and print elapsed wall time.
ask_agent() {
  local label="$1"; local port="$2"
  local body='{"model":"hermes","messages":[{"role":"user","content":"Just respond with the word OK."}],"max_tokens":5,"stream":false}'
  local start; start=$(date +%s.%N)
  local code
  code=$(curl -s -o /tmp/cw.json -w "%{http_code}" -X POST "http://localhost:${port}/v1/chat/completions" \
        -H "Authorization: Bearer smile-dental-course-key" \
        -H "Content-Type: application/json" -d "${body}" --max-time 180 || echo "000")
  local end; end=$(date +%s.%N)
  local elapsed; elapsed=$(awk "BEGIN{printf \"%.2f\", ${end}-${start}}")
  echo "  ${label}: HTTP ${code} in ${elapsed}s"
}

start_pf() {
  local target="$1"; local port="$2"
  kubectl -n "${NS}" port-forward "${target}" "${port}:8642" >/dev/null 2>&1 &
  echo $!
  sleep 5
}
stop_pf() { kill "$1" 2>/dev/null || true; }

# 1. Capture warm timing first (pool=2)
echo "[1/4] Warm test — WarmPool replicas=2"
WARM_POD=$(kubectl get pod -n "${NS}" -l app=hermes-agent -o jsonpath='{.items[0].metadata.name}')
PF=$(start_pf "pod/${WARM_POD}" 18642)
ask_agent "Warm" 18642
stop_pf "${PF}"

# 2. Drain the pool
echo "[2/4] Scaling WarmPool to 0 (cold setup)…"
kubectl patch sandboxwarmpool hermes-agent-warmpool -n "${NS}" --type merge -p '{"spec":{"replicas":0}}'
kubectl wait sandboxwarmpool/hermes-agent-warmpool -n "${NS}" \
  --for=jsonpath='{.status.readyReplicas}'=0 --timeout=180s || true

# 3. Scale back to 2 — now pods schedule fresh = cold path
echo "[3/4] Scaling WarmPool back to 2 (cold timing)…"
kubectl patch sandboxwarmpool hermes-agent-warmpool -n "${NS}" --type merge -p '{"spec":{"replicas":2}}'
COLD_START=$(date +%s.%N)
kubectl wait sandboxwarmpool/hermes-agent-warmpool -n "${NS}" \
  --for=jsonpath='{.status.readyReplicas}'=2 --timeout=300s
COLD_END=$(date +%s.%N)
COLD_ELAPSED=$(awk "BEGIN{printf \"%.2f\", ${COLD_END}-${COLD_START}}")
echo "  Cold WarmPool refill (0 → 2 ready): ${COLD_ELAPSED}s"

# 4. First request immediately after refill
COLD_POD=$(kubectl get pod -n "${NS}" -l app=hermes-agent -o jsonpath='{.items[0].metadata.name}')
PF=$(start_pf "pod/${COLD_POD}" 18643)
ask_agent "Cold" 18643
stop_pf "${PF}"

echo
echo "[4/4] Done. Compare the Warm vs Cold timings above."
echo "Expected: Warm ≈ 1-3s (LLM API latency only); Cold ≈ 30-90s (image cached + hermes startup)."
```

**Live verification:**
```bash
# Build the Day-2 Chainlit image
cd course-code/labs/lab-08/solution/ui
docker build -t localhost:5001/smile-dental-ui-day2:v1.0.0 .
docker push localhost:5001/smile-dental-ui-day2:v1.0.0
cd /Users/gshah/courses/llmops

# Apply the Day-2 Chainlit Deployment
kubectl apply -f course-code/labs/lab-08/solution/k8s/40-chainlit-deploy-day2.yaml
kubectl rollout status deploy/chainlit-ui -n llm-app --timeout=180s

# Forward and test the canonical query through Chainlit → Sandbox Router → Hermes (in Sandbox) → MCP tools
kubectl -n llm-app port-forward svc/chainlit-ui 30300:8000 &
sleep 3
# (Manual smoke check: open http://localhost:30300 in browser, submit "severe tooth pain since yesterday")

# Or, smoke-test the Sandbox Router gateway path directly (gcr mode only):
if grep -q gcr /tmp/lab08-router-mode; then
  kubectl -n llm-agent port-forward svc/sandbox-router-svc 18080:8080 &
  sleep 3
  curl -s -X POST http://localhost:18080/v1/chat/completions \
    -H "Authorization: Bearer smile-dental-course-key" \
    -H "Content-Type: application/json" \
    -d '{"model":"hermes","messages":[{"role":"user","content":"severe tooth pain since yesterday"}],"stream":false}' \
    | python3 -m json.tool | head -50
  kubectl -n llm-agent get cm bookings -o jsonpath='{.data.bookings}' | python3 -m json.tool
fi

# Run the cold-vs-warm timing demo
chmod +x course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh
bash course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh
```

If the curl returns tool_calls including triage + treatment_lookup + book_appointment AND `kubectl get cm bookings -n llm-app` shows a non-empty array, SANDBOX-04 is closed via the Router as the gateway.
  </action>
  <verify>
    <automated>grep -q 'sandbox-router-svc' course-code/labs/lab-08/solution/ui/app.py && grep -q 'AGENT_URL' course-code/labs/lab-08/solution/k8s/40-chainlit-deploy-day2.yaml && grep -q 'sandbox-router-svc.llm-agent.svc.cluster.local:8080' course-code/labs/lab-08/solution/k8s/40-chainlit-deploy-day2.yaml && grep -q 'nodePort: 30300' course-code/labs/lab-08/solution/k8s/40-chainlit-deploy-day2.yaml && test -x course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh && grep -q 'kubectl scale sandboxwarmpool' course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh && grep -q 'replicas":0' course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh && grep -q 'replicas":2' course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh && kubectl apply --dry-run=client -f course-code/labs/lab-08/solution/k8s/40-chainlit-deploy-day2.yaml && kubectl rollout status deploy/chainlit-ui -n llm-app --timeout=180s && bash course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh 2>&1 | tee /tmp/cw-demo.log | grep -q 'Compare the Warm vs Cold timings'</automated>
  </verify>
  <acceptance_criteria>
    - Day-2 Chainlit references the Sandbox Router Service: `grep -q 'sandbox-router-svc.llm-agent.svc.cluster.local:8080' course-code/labs/lab-08/solution/ui/app.py`
    - Chainlit Deployment uses Day-2 image and AGENT_URL env: `grep -q 'smile-dental-ui-day2' course-code/labs/lab-08/solution/k8s/40-chainlit-deploy-day2.yaml && grep -q 'AGENT_URL' course-code/labs/lab-08/solution/k8s/40-chainlit-deploy-day2.yaml`
    - NodePort preserved at 30300 (lab-06 contract): `grep -q 'nodePort: 30300' course-code/labs/lab-08/solution/k8s/40-chainlit-deploy-day2.yaml`
    - cold-vs-warm-demo.sh has executable bit and exercises both 0 and 2 replica states: `test -x course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh && grep -q 'replicas":0' course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh && grep -q 'replicas":2' course-code/labs/lab-08/solution/scripts/cold-vs-warm-demo.sh`
    - Live: Chainlit Day-2 deploys cleanly: `kubectl rollout status deploy/chainlit-ui -n llm-app --timeout=180s` exits 0
    - Live: cold-vs-warm-demo.sh runs end-to-end on this machine and prints both Warm and Cold timings
    - SUMMARY records the actual cold and warm timings observed (proof for SANDBOX-03)
    - Live (only if Router mode=gcr): canonical query through `sandbox-router-svc` returns tool_calls hitting all 3 tools and adds an entry to the `bookings` ConfigMap (closes SANDBOX-04 with evidence)
    - If Router mode=port-forward: SUMMARY explicitly notes that SANDBOX-04 was closed via `kubectl port-forward svc/<sandbox-pod-svc>` instead of the Router Service, with an observed end-to-end query result
  </acceptance_criteria>
  <done>Day-2 Chainlit Deployment is rolling on the live KIND cluster, calls go through the Sandbox Router (or port-forward fallback), the canonical query exercises all 3 MCP tools, the cold-vs-warm demo script runs end-to-end with observable timing differences (warm ~1-3s, cold ~30-90s), and the bookings ConfigMap gets a new entry. SANDBOX-01..04 all closed with live evidence.</done>
</task>

</tasks>

<verification>
After all 3 tasks:
- 22 files exist under `course-code/labs/lab-08/solution/{scripts,k8s,ui}` + 1 starter README + 1 modified Lab-07 book_appointment file
- `kubectl get crd` shows the 4 Agent Sandbox CRDs at v0.4.3
- `kubectl get sandboxtemplate,sandboxwarmpool,sandbox -n llm-agent` shows the template + a WarmPool with 2 ready Sandboxes
- `kubectl get pods -n llm-agent` shows hermes pods, mcp-* pods, and (if gcr mode) sandbox-router pod all Running
- `kubectl get cm bookings -n llm-app` exists; after a canonical query it contains ≥ 1 booking
- `cold-vs-warm-demo.sh` produces measurable timing difference
- All book_appointment tests pass (3 tests now)
- `kubectl rollout status deploy/chainlit-ui -n llm-app` shows the Day-2 image deployed
</verification>

<success_criteria>
- SANDBOX-01: Agent Sandbox v0.4.3 CRDs + controller installed, Ready
- SANDBOX-02: Hermes Agent runs as Sandbox CRs (managed by SandboxTemplate) in `llm-agent` namespace
- SANDBOX-03: SandboxWarmPool replicas=2 has 2 ready Sandboxes; cold-vs-warm timing demo produces a measurable difference
- SANDBOX-04: Chainlit reaches the agent via the Sandbox Router (or documented port-forward fallback when GCR pull fails)
- RESEARCH.md Open Q1 resolved — verify-sandbox-router-image.sh recorded the mode, lab can proceed in either path
- D-07 NetworkPolicy applied (with the kindnet-not-enforced caveat in the manifest comment + lab guide)
- D-11 ConfigMap-backed booking works end-to-end via in-cluster RBAC
</success_criteria>

<output>
After completion, create `.planning/phases/03-agentops-labs-day-2/03-04-SUMMARY.md` documenting:
- Agent Sandbox install verification (CRDs registered, controller Ready)
- Sandbox Router mode (gcr or port-forward) — direct quote from /tmp/lab08-router-mode
- Observed warm vs cold timings from cold-vs-warm-demo.sh
- Final state of `kubectl get sandboxwarmpool/hermes-agent-warmpool -n llm-agent -o yaml | yq '.status'`
- Whether the canonical "severe tooth pain since yesterday" query through Chainlit → Router → Hermes hit all 3 tools (yes/no with evidence)
- bookings ConfigMap content after the demo (one row from `kubectl get cm bookings -n llm-app -o jsonpath='{.data.bookings}'`)
- Any deviations from the planned manifests (e.g. resource bumps if WarmPool pods OOMed)
</output>
