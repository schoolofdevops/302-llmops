---
phase: 03-agentops-labs-day-2
plan: 07
type: execute
wave: 5
depends_on: [03-06]
files_modified:
  - course-content/docs/labs/lab-09-observability.md
autonomous: true
requirements:
  - OBS-05
  - OBS-06
  - OBS-07
must_haves:
  truths:
    - "Lab 09 page is a complete Docusaurus walkthrough that takes a student from `helm install tempo` to a working Grafana dashboard showing real cost + traces"
    - "Page references the live numbers from plan 03-06's SUMMARY (e.g. observed cost-USD value after one canonical query)"
    - "Page documents the Hermes-internal MCP context-propagation limitation honestly so students know what they're seeing in Tempo"
    - "Page lays out the new request path: Chainlit → cost-middleware → Sandbox Router → Hermes Sandbox → MCP tools → (RAG retriever for treatment_lookup) → LLM API"
    - "Page includes a 'Click through Tempo' subsection that walks students from the dashboard trace panel into a single trace's span tree"
  artifacts:
    - path: course-content/docs/labs/lab-09-observability.md
      provides: "Full Lab 09 student walkthrough"
      min_lines: 300
      contains: "Tempo"
  key_links:
    - from: course-content/docs/labs/lab-09-observability.md
      to: course-code/labs/lab-09/solution/scripts/install-otel-tempo.sh
      via: "Part A instructs student to run the install script"
      pattern: "install-otel-tempo.sh"
    - from: course-content/docs/labs/lab-09-observability.md
      to: course-code/labs/lab-09/solution/scripts/run-canonical-query-traced.sh
      via: "Part E instructs student to run the traced query"
      pattern: "run-canonical-query-traced.sh"
---

<objective>
Replace the 25-line placeholder `lab-09-observability.md` with a complete Docusaurus walkthrough: install Tempo + OTEL Collector via Helm, instrument the 3 MCP tools (already done in code by plan 03-06; the lab guide explains what changed), deploy the cost middleware proxy + price ConfigMap + ServiceMonitors + auto-discovered Grafana dashboard, point Chainlit at the cost middleware, run the traced canonical query, and walk through Tempo + Grafana to find the trace + the cost panel value. Closes the documentation half of OBS-05..07.

Purpose: Lab 09 is the final Day 2 lab — students should leave with a real, interactive observability stack. The page must explain WHY each piece exists (cost middleware vs OTEL Collector vs Tempo) and HOW to read the resulting dashboard, not just APPLY some YAML.
Output: One Docusaurus markdown page (~350-500 lines), same Day-2 lab style as plans 03-03 and 03-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-agentops-labs-day-2/03-CONTEXT.md
@.planning/phases/03-agentops-labs-day-2/03-RESEARCH.md
@.planning/phases/03-agentops-labs-day-2/03-06-SUMMARY.md
@CLAUDE.md
@course-content/docs/labs/lab-08-agent-sandbox.md
@course-content/docs/labs/lab-09-observability.md
@course-code/labs/lab-09/solution/scripts/install-otel-tempo.sh
@course-code/labs/lab-09/solution/scripts/run-canonical-query-traced.sh
@course-code/labs/lab-09/solution/helm/values-tempo.yaml
@course-code/labs/lab-09/solution/helm/values-otel-collector.yaml
@course-code/labs/lab-09/solution/k8s/70-llm-price-table-cm.yaml
@course-code/labs/lab-09/solution/k8s/70-cost-middleware-deploy.yaml
@course-code/labs/lab-09/solution/k8s/70-grafana-agent-dashboard-cm.yaml
@course-code/labs/lab-09/solution/k8s/70-servicemonitor-cost-middleware.yaml
@course-code/labs/lab-09/solution/k8s/70-servicemonitor-otel.yaml
@course-code/labs/lab-09/solution/cost_middleware/cost_middleware.py
@course-code/labs/lab-07/solution/tools/otel_setup.py
@course-code/labs/lab-07/solution/tools/triage/triage_server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite lab-09-observability.md as a complete agent observability walkthrough</name>
  <files>course-content/docs/labs/lab-09-observability.md</files>
  <read_first>
    - course-content/docs/labs/lab-09-observability.md (existing 25-line placeholder — overwrite)
    - course-content/docs/labs/lab-07-agent-core.md (Day-2 style baseline from plan 03-03)
    - course-content/docs/labs/lab-08-agent-sandbox.md (Day-2 K8s style baseline from plan 03-05)
    - .planning/phases/03-agentops-labs-day-2/03-06-SUMMARY.md (live numbers — observed cost USD, trace counts, any panel-type fallbacks)
    - course-code/labs/lab-09/solution/cost_middleware/cost_middleware.py (excerpt for "How the cost middleware works" section)
    - course-code/labs/lab-07/solution/tools/otel_setup.py (excerpt for "How OTEL is wired into the MCP tools")
    - course-code/labs/lab-07/solution/tools/triage/triage_server.py (excerpt showing OTEL setup_tracing + FastAPIInstrumentor + HTTPXClientInstrumentor lines)
    - course-code/labs/lab-09/solution/k8s/70-grafana-agent-dashboard-cm.yaml (verbatim panel descriptions for "How to read the dashboard")
    - .planning/phases/03-agentops-labs-day-2/03-RESEARCH.md (Focus 6 §"MCP Context Propagation" — verbatim limitation explanation; Focus 5 KIND footprint estimate)
  </read_first>
  <action>
**Overwrite** `course-content/docs/labs/lab-09-observability.md` with the structure below. Keep `sidebar_position: 10`. Update the header to **"Day 2 | Duration: ~75 minutes"**.

**Lead paragraph** (3 sentences): Day 2 part 3: take the working Lab-08 agent stack and add full observability — OpenTelemetry traces, Prometheus token & cost metrics, and a unified Grafana dashboard. Today we deploy Grafana Tempo (alongside the existing Day-1 Prometheus + Grafana stack), install an OpenTelemetry Collector that fans traces into Tempo and metrics into Prometheus, instrument the 3 MCP tool servers with OTEL auto-instrumentation, and add a thin "cost middleware" proxy in front of the agent that emits per-token and per-USD Counter metrics from a price-table ConfigMap. By the end you will read tool-call traces in Tempo and watch a USD counter tick up as you query the agent.

**Sections in order:**

1. **`## Learning Objectives`** — 6 bullets:
   - Deploy Grafana Tempo (single-binary mode, in-memory storage on KIND) alongside the existing Day-1 stack
   - Install the OpenTelemetry Collector in deployment mode, configured to forward traces to Tempo and expose metrics to Prometheus
   - Auto-instrument the 3 MCP tool servers with FastAPI + httpx OpenTelemetry instrumentation (no manual span code)
   - Deploy a Prometheus cost-tracking middleware that intercepts agent traffic and emits token & USD Counters from a price-table ConfigMap
   - Auto-discover a new Grafana dashboard via the `grafana_dashboard: "1"` ConfigMap label
   - Walk through a single canonical query in Tempo and see the tool-server spans + the RAG-retriever child span

2. **`## Lab Files`** — bullet list of `course-code/labs/lab-09/solution/{scripts,helm,k8s,cost_middleware}` and the relevant code/manifests, plus modifications to `course-code/labs/lab-07/solution/tools/{triage,treatment_lookup,book_appointment}` (OTEL imports + setup_tracing call).

3. **`## Prerequisites`**:
   - Lab 08 complete (Hermes Sandbox + WarmPool + MCP tools running)
   - Day 1 Prometheus + Grafana stack running in `monitoring` (kube-prometheus-stack)
   - Free RAM headroom: Tempo + OTEL Collector add ~500-700 MB

4. **`## Architecture`** — 1 paragraph + a small text/ASCII diagram showing the data flow:
   ```
   Chainlit → cost-middleware (Prometheus) → sandbox-router-svc → Hermes (in Sandbox)
                                                                    ↓
                                                                  MCP tool servers (OTEL spans → OTEL Collector → Tempo)
                                                                    ↓
                                                                  RAG retriever (httpx auto-instrumented; child span)
                                                                    ↓
                                                                  Groq / Gemini API
   ```
   Explain that traces and metrics flow into different destinations: spans → OTEL Collector → Tempo (on the trace pipeline); cost-middleware token Counters → Prometheus (scraped by ServiceMonitor); both visualised in the same Grafana dashboard.

5. **`## Part A: Install Tempo + OTEL Collector`** —
   - Concept: 1-paragraph explanation of why we use Tempo over Jaeger (D-16: lighter for KIND, integrates with the same Grafana the Day-1 labs use), and why we use the Collector (decouples instrumented apps from backend; lets us export traces and metrics from a single OTLP endpoint).
   - Run:
     ```bash
     bash course-code/labs/lab-09/solution/scripts/install-otel-tempo.sh
     ```
   - Show what each Helm release does (one paragraph each, point at `helm/values-tempo.yaml` and `helm/values-otel-collector.yaml` excerpts).
   - Apply the ServiceMonitor + Tempo datasource ConfigMap:
     ```bash
     kubectl apply -f course-code/labs/lab-09/solution/k8s/70-servicemonitor-otel.yaml
     kubectl apply -f course-code/labs/lab-09/solution/k8s/70-grafana-tempo-datasource-cm.yaml
     ```
   - Verify: `kubectl get pods -n monitoring | grep -E 'tempo|otel-collector'` (both 1/1 Running). Open Grafana → Connections → Data sources → confirm "Tempo" appears.
   - `:::info In-memory tmpfs storage` admonition explaining that Tempo loses traces on pod restart on KIND — that's fine for the workshop.

6. **`## Part B: How the MCP tools became OTEL-instrumented`** —
   - Concept paragraph: we use FastAPIInstrumentor (auto-spans for incoming HTTP requests) + HTTPXClientInstrumentor (auto-spans for outgoing httpx calls) so we don't have to write manual span code. The reusable setup is `course-code/labs/lab-07/solution/tools/otel_setup.py`.
   - Show the full content of `otel_setup.py` (verbatim) in a Python block.
   - Show the diff applied to `triage_server.py` (just the new imports + the `setup_tracing(...)` call + the `FastAPIInstrumentor.instrument_app(_app)` line) — point students at the analogous changes in `treatment_lookup_server.py` and `book_appointment_server.py`.
   - `:::warning Hermes does not propagate W3C traceparent` admonition (RESEARCH.md Focus 6 verbatim):
     ```text
     The MCP protocol does not natively carry W3C trace context. Hermes Agent
     is a closed binary (we don't fork it per D-01), so we cannot inject
     traceparent into Hermes's outgoing MCP calls. Result: Tempo will show
     each MCP tool's spans as a separate root trace rather than as a child of
     a Hermes "agent.request" span. Use Tempo's time-window search
     (service.name=mcp-treatment-lookup, last 5 minutes) to correlate spans
     for one user query.
     ```
   - Rebuild + roll the tool images (commands from plan 03-06 Task 2).

7. **`## Part C: Cost middleware`** —
   - Concept: explain why a proxy is the right approach (no Hermes fork; works whether the model is Groq or Gemini; the proxy reads `usage.prompt_tokens` + `usage.completion_tokens` from the OpenAI-shape response and emits Prometheus Counters).
   - Show the full `cost_middleware.py` (~50 lines is fine to embed verbatim) — focus students' attention on:
     - `tokens_total` and `cost_usd_total` Counter definitions
     - `compute_cost_usd` price math
     - The proxy `POST /v1/chat/completions` handler reading usage from the upstream response
   - Show the `70-llm-price-table-cm.yaml` (the prices live in a ConfigMap so students can adjust without rebuilding the image).
   - Show the `70-cost-middleware-deploy.yaml` Deployment + Service, calling out the `UPSTREAM_URL` (Sandbox Router) and the price-table volume mount.
   - Apply the manifests:
     ```bash
     kubectl apply -f course-code/labs/lab-09/solution/k8s/70-llm-price-table-cm.yaml
     kubectl apply -f course-code/labs/lab-09/solution/k8s/70-cost-middleware-deploy.yaml
     kubectl apply -f course-code/labs/lab-09/solution/k8s/70-servicemonitor-cost-middleware.yaml
     kubectl apply -f course-code/labs/lab-09/solution/k8s/70-servicemonitor-mcp-tools.yaml
     kubectl rollout status deploy/cost-middleware -n llm-agent --timeout=120s
     ```
   - Switch Chainlit to route through the cost middleware:
     ```bash
     kubectl set env deploy/chainlit-ui -n llm-app \
       AGENT_URL=http://cost-middleware.llm-agent.svc.cluster.local:9100
     kubectl rollout status deploy/chainlit-ui -n llm-app --timeout=120s
     ```
   - `:::tip Why we proxy instead of patch Hermes` admonition: D-01 forbids forking Hermes; the proxy approach gives us cost visibility without modifying upstream code, and it composes cleanly with any OpenAI-compatible agent.

8. **`## Part D: Grafana dashboard`** —
   - Apply the dashboard ConfigMap:
     ```bash
     kubectl apply -f course-code/labs/lab-09/solution/k8s/70-grafana-agent-dashboard-cm.yaml
     ```
   - Concept: ConfigMaps with `grafana_dashboard: "1"` label are auto-loaded by Grafana's sidecar — no manual import.
   - Open Grafana at `http://localhost:30400` (default Day-1 password from kube-prometheus-stack — show how to retrieve it: `kubectl -n monitoring get secret kube-prometheus-stack-grafana -o jsonpath='{.data.admin-password}' | base64 -d`).
   - Find the "Smile Dental — Agent Overview" dashboard. Walk through each panel:
     - Panel 1: LLM tokens / sec by direction — `sum by (direction) (rate(agent_llm_tokens_total[1m]))`
     - Panel 2: Cumulative LLM cost (USD) — `sum(agent_llm_cost_usd_total)`, currency unit
     - Panel 3+4: Tempo trace search panels for `mcp-triage` and `mcp-treatment-lookup`
   - Note (from SUMMARY of 03-06): if the Tempo panel types had to be downgraded to text panels because of Grafana version compat, link out to `Explore → Tempo → service.name=mcp-treatment-lookup` instead.

9. **`## Part E: Run the traced canonical query`** —
   - Run:
     ```bash
     bash course-code/labs/lab-09/solution/scripts/run-canonical-query-traced.sh
     ```
   - Embed the actual cost-USD value observed in plan 03-06's verification (e.g. "After one canonical query, `sum(agent_llm_cost_usd_total)` reads ~$0.00045 — small but non-zero, and you'll see it tick up with each subsequent query").
   - Now go back to the Grafana dashboard:
     - Panel 1: should show two stacked lines (input + output tokens) with non-zero rate
     - Panel 2: should show a non-zero cost value
     - Panel 3+4: click into a trace → expand the span tree → for `mcp-treatment-lookup`, the trace contains a child httpx span pointing at the RAG retriever URL. **This is the literal "agent → retriever → LLM" span chain that closes OBS-06.**
   - `:::tip Click through Tempo` admonition: in Grafana → Explore → Tempo → enter `service.name=mcp-treatment-lookup` → pick any trace → expand spans → confirm the httpx child shows the URL `http://rag-retriever.llm-app.svc.cluster.local:8001/search`.

10. **`## Verification`** — checklist:
    - `kubectl get deploy tempo otel-collector-opentelemetry-collector -n monitoring` — both Available
    - `kubectl get deploy cost-middleware -n llm-agent` — Available
    - Prometheus query `sum(agent_llm_cost_usd_total)` returns > 0 after at least one canonical query
    - Tempo query `service.name=mcp-treatment-lookup` returns ≥ 1 trace
    - Grafana dashboard "Smile Dental — Agent Overview" is visible

11. **`## Common Pitfalls`** —
    - `:::warning Hermes does not propagate traceparent` (re-state from Part B, with the time-window search workaround)
    - `:::warning Tempo loses traces on pod restart` (in-memory storage on KIND — fine for workshop)
    - `:::warning ServiceMonitor not picked up` — common cause: missing `release: kube-prometheus-stack` label
    - `:::warning Grafana dashboard not auto-loading` — common cause: missing `grafana_dashboard: "1"` label or invalid JSON in the ConfigMap data
    - `:::tip Cost data is small for SmolLM-sized prompts` — expect single-digit-cent costs after one query; the dashboard becomes more meaningful at scale

12. **`## After This Lab`** — table:
    | Component | URL / Resource | Status |
    |-----------|----------------|--------|
    | Grafana Tempo | `tempo.monitoring.svc:3100` | Running (in-memory) |
    | OpenTelemetry Collector | `otel-collector-opentelemetry-collector.monitoring.svc:4317` | Running |
    | Cost middleware proxy | `cost-middleware.llm-agent.svc:9100` | Running |
    | Smile Dental Agent dashboard | Grafana → "Smile Dental — Agent Overview" | Auto-discovered |
    | Chainlit (Day-2, traced) | NodePort 30300 → cost-middleware → Sandbox Router → Hermes | Running |
    Closing paragraph: Day 3 (Phase 4) builds on this stack — autoscaling under load (HPA + KEDA driven by `agent_llm_tokens_total` + `chat_requests_total`), GitOps deployment, an Argo Workflows pipeline with a DeepEval gate, and guardrails. The observability stack from Lab 09 is what makes those Day-3 systems debuggable.

13. **`## Tear down (optional)`** — leaves Tempo + OTEL Collector running for Day 3, but shows how to scale cost-middleware to 0 if students need RAM back. Plus `helm uninstall` commands as the nuclear option.

**Length target:** 350-500 lines. Hard cap 600.

**Style:** match plans 03-03 and 03-05.
  </action>
  <verify>
    <automated>test $(wc -l < course-content/docs/labs/lab-09-observability.md) -ge 300 && test $(wc -l < course-content/docs/labs/lab-09-observability.md) -le 600 && grep -q '^# Lab 09:' course-content/docs/labs/lab-09-observability.md && grep -q 'sidebar_position: 10' course-content/docs/labs/lab-09-observability.md && grep -q 'install-otel-tempo.sh' course-content/docs/labs/lab-09-observability.md && grep -q 'run-canonical-query-traced.sh' course-content/docs/labs/lab-09-observability.md && grep -q 'agent_llm_tokens_total' course-content/docs/labs/lab-09-observability.md && grep -q 'agent_llm_cost_usd_total' course-content/docs/labs/lab-09-observability.md && grep -q 'Tempo' course-content/docs/labs/lab-09-observability.md && grep -q 'OpenTelemetry' course-content/docs/labs/lab-09-observability.md && grep -q 'cost-middleware' course-content/docs/labs/lab-09-observability.md && grep -q 'traceparent\|MCP context\|Hermes.*propagate\|propagate.*context' course-content/docs/labs/lab-09-observability.md && grep -q 'grafana_dashboard' course-content/docs/labs/lab-09-observability.md && grep -q 'rag-retriever' course-content/docs/labs/lab-09-observability.md && grep -q '## Learning Objectives' course-content/docs/labs/lab-09-observability.md && grep -q '## Verification' course-content/docs/labs/lab-09-observability.md && grep -q '## After This Lab' course-content/docs/labs/lab-09-observability.md && ! grep -i 'atharva' course-content/docs/labs/lab-09-observability.md && cd course-content && npx --yes docusaurus@3.10.0 build --no-minify 2>&1 | tail -10 | grep -qi 'compiled successfully' && cd /Users/gshah/courses/llmops</automated>
  </verify>
  <acceptance_criteria>
    - File length 300-600 lines
    - All required H2 sections present: Learning Objectives, Lab Files, Prerequisites, Architecture, Part A through Part E, Verification, Common Pitfalls, After This Lab, Tear down: `for h in 'Learning Objectives' 'Prerequisites' 'Architecture' 'Part A' 'Part B' 'Part C' 'Part D' 'Part E' 'Verification' 'Common Pitfalls' 'After This Lab'; do grep -qF "$h" course-content/docs/labs/lab-09-observability.md || exit 1; done`
    - References both scripts: `grep -q 'install-otel-tempo.sh' && grep -q 'run-canonical-query-traced.sh'`
    - References both Counter metric names: `grep -q 'agent_llm_tokens_total' && grep -q 'agent_llm_cost_usd_total'`
    - MCP context-propagation limitation is documented: `grep -q 'traceparent\|MCP context\|Hermes.*propagate\|propagate.*context' course-content/docs/labs/lab-09-observability.md`
    - Grafana auto-discovery label mentioned: `grep -q 'grafana_dashboard' course-content/docs/labs/lab-09-observability.md`
    - References absolute relative paths to solution: `grep -c 'course-code/labs/lab-09/solution' course-content/docs/labs/lab-09-observability.md` returns >= 5
    - References the OTEL setup module from Lab 07: `grep -q 'tools/otel_setup.py' course-content/docs/labs/lab-09-observability.md`
    - Cites the live cost-USD value (numeric): `grep -E '\$0\.|0\.00[0-9]+' course-content/docs/labs/lab-09-observability.md` (USD value embedded from SUMMARY)
    - No "Atharva" references: `! grep -i 'atharva' course-content/docs/labs/lab-09-observability.md`
    - Frontmatter sidebar_position preserved: `grep -q 'sidebar_position: 10' course-content/docs/labs/lab-09-observability.md`
    - Docusaurus build succeeds: `cd course-content && npx docusaurus@3.10.0 build` exits 0
  </acceptance_criteria>
  <done>course-content/docs/labs/lab-09-observability.md is a 300-600 line walkthrough covering Tempo + OTEL Collector install, MCP tool OTEL instrumentation explanation, cost middleware deployment, Grafana dashboard auto-discovery, traced canonical query, and the Tempo span-tree click-through that closes OBS-06 visibly. Hermes-internal MCP context propagation limitation is documented with a workaround. Live cost-USD values from plan 03-06's SUMMARY are embedded. Docusaurus build is green.</done>
</task>

</tasks>

<verification>
- Lab 09 page overwritten (was 25 lines, now 300-600 lines)
- All sections in correct order, including the Hermes traceparent limitation note
- Live `npx docusaurus build` from course-content/ exits 0
- Spot-check rendered HTML: panels list correctly, admonitions render
</verification>

<success_criteria>
- OBS-05..07 documentation closed: a student following only this page (with Lab 08 + the Lab 09 solution code present) sees non-zero cost in the dashboard and a tool-server span in Tempo with the RAG-retriever httpx child
- Hermes context-propagation gap is explained (not glossed over)
- Build is green
</success_criteria>

<output>
After completion, create `.planning/phases/03-agentops-labs-day-2/03-07-SUMMARY.md` documenting:
- Final line count of lab-09-observability.md
- Whether the dashboard panel-type fallback (text panels vs Tempo trace panels) was needed
- Confirmation Docusaurus build succeeded
- Phase-3 closure note: list which requirements (AGENT-01..04, SANDBOX-01..04, OBS-05..07) are now closed by which plans (this is the final SUMMARY of the phase)
</output>
