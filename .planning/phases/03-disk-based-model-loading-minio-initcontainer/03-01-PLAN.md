---
phase: 03-disk-based-model-loading-minio-initcontainer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - course-code/labs/lab-00/solution/setup/kind-config.yaml
  - course-code/labs/lab-00/starter/setup/kind-config.yaml
autonomous: false
requirements: [PACKAGE-02]

must_haves:
  truths:
    - "kind-config.yaml extraPortMappings includes 30203, 30900, and 30901 on the control-plane node"
    - "KIND cluster llmops-kind is running with the updated NodePort bindings"
    - "Phase 02 base stack (vllm-smollm2 in llm-serving, rag-retriever in llm-app, chainlit-ui in llm-app) is redeployed and healthy"
    - "curl localhost:30900/minio/health/live from the student's host does not return connection refused (NodePort 30900 is bound)"
  artifacts:
    - path: "course-code/labs/lab-00/solution/setup/kind-config.yaml"
      provides: "Updated KIND cluster config with Phase 03 NodePorts"
      contains: "30203"
    - path: "course-code/labs/lab-00/starter/setup/kind-config.yaml"
      provides: "Updated student starter KIND config with Phase 03 NodePorts"
      contains: "30203"
  key_links:
    - from: "extraPortMappings"
      to: "NodePort 30900"
      via: "KIND control-plane container bind"
      pattern: "containerPort: 30900"
    - from: "extraPortMappings"
      to: "NodePort 30203"
      via: "KIND control-plane container bind"
      pattern: "containerPort: 30203"
---

<objective>
Fix GAP-2: add NodePorts 30203, 30900, and 30901 to kind-config.yaml extraPortMappings, then
recreate the KIND cluster and redeploy the Phase 02 base stack so Phase 03 lab steps can reach
MinIO (30900/30901) and vllm-smollm2-disk (30203) from the student's host.

Purpose: NodePort bindings in KIND require extraPortMappings on the control-plane node at cluster
creation time. Post-creation addition is impossible without a delete+recreate cycle. This plan
performs that cycle and restores the Phase 02 workloads needed for comparison with Pattern B.

Output: Updated kind-config.yaml (both solution/ and starter/), a running KIND cluster with all
six Phase 02+03 NodePorts bound, and the Phase 02 base stack (retriever + vLLM Pattern A + Chainlit
+ monitoring) redeployed and healthy.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-modernize-llmops-spine-labs-00-05/02-VERIFICATION.md
@.planning/phases/02-modernize-llmops-spine-labs-00-05/02-CONTEXT.md
@.planning/phases/03-disk-based-model-loading-minio-initcontainer/03-RESEARCH.md

<interfaces>
<!-- Existing kind-config.yaml structure (solution file). Executor adds three entries to the
     extraPortMappings list on the control-plane node only. Worker nodes do NOT need extraPortMappings.
     Current portMappings already present: 30000, 32000, 8000, 80, 443, 30080, 30090, 30100,
     31001, 30200, 30300, 30400, 30500. -->

<!-- Phase 02 namespace + deployment names to redeploy:
     - kubectl apply -f course-code/shared/k8s/namespaces.yaml
     - kubectl apply -f course-code/labs/lab-01/solution/k8s/
     - kubectl apply -f course-code/labs/lab-04/solution/k8s/
     - helm upgrade --install kps prometheus-community/kube-prometheus-stack ... (Lab 05)
     NOTE: The executor should redeploy Lab 04 (vLLM Pattern A + Chainlit) but MAY skip kube-prometheus-stack
     for memory headroom — Phase 03 does not need monitoring running simultaneously. -->

<!-- Pattern A vLLM Deployment name: vllm-smollm2 in namespace llm-serving (from 30-deploy-vllm.yaml).
     Chainlit Deployment name: chainlit-ui in namespace llm-app. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 03 NodePorts to kind-config.yaml (solution + starter)</name>
  <files>
    course-code/labs/lab-00/solution/setup/kind-config.yaml
    course-code/labs/lab-00/starter/setup/kind-config.yaml
  </files>
  <read_first>
    - course-code/labs/lab-00/solution/setup/kind-config.yaml — read the full file; add new portMappings after the existing 30500 entry in the control-plane node's extraPortMappings list; do NOT add to worker nodes
    - course-code/labs/lab-00/starter/setup/kind-config.yaml — read the starter file; apply the same three portMapping additions
  </read_first>
  <action>
    In both kind-config.yaml files, add three new entries to the control-plane node's extraPortMappings list, placed directly after the containerPort: 30500 entry:

      - containerPort: 30203
        hostPort: 30203
        listenAddress: "0.0.0.0"
        protocol: tcp
      - containerPort: 30900
        hostPort: 30900
        listenAddress: "0.0.0.0"
        protocol: tcp
      - containerPort: 30901
        hostPort: 30901
        listenAddress: "0.0.0.0"
        protocol: tcp

    The worker and worker2 nodes already have extraMounts but no extraPortMappings — leave them unchanged.
    Preserve all existing comments at the top of each file verbatim.
  </action>
  <verify>
    <automated>grep -c "30203\|30900\|30901" course-code/labs/lab-00/solution/setup/kind-config.yaml | grep -E "^[3-9]|^[0-9]{2,}"</automated>
  </verify>
  <acceptance_criteria>
    - solution/setup/kind-config.yaml contains exactly three new entries: containerPort 30203, 30900, and 30901, each with matching hostPort and listenAddress "0.0.0.0"
    - starter/setup/kind-config.yaml has the same three entries
    - All existing entries (30200, 30300, 30400, 30500, etc.) are preserved unchanged
    - YAML is valid (no indentation errors)
  </acceptance_criteria>
  <done>Both kind-config.yaml files updated; grep confirms 30203, 30900, 30901 present in control-plane extraPortMappings</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Recreate KIND cluster and redeploy Phase 02 base stack</name>
  <read_first>
    - course-code/labs/lab-00/solution/setup/kind-config.yaml — verify it has 30203/30900/30901 before recreating
    - .planning/phases/02-modernize-llmops-spine-labs-00-05/02-VERIFICATION.md — reference for what Phase 02 stack looks like when healthy
  </read_first>
  <what-built>
    Task 1 updated kind-config.yaml with the three missing NodePorts. The cluster must now be recreated
    (NodePort bindings cannot be hot-added to a running KIND cluster) and the Phase 02 base stack
    redeployed so Phase 03 labs have a working comparison baseline.

    Memory budget note: After cluster recreate, redeploy ONLY the components needed for Phase 03
    comparison: RAG retriever (Lab 01 manifests) + vLLM Pattern A (Lab 04 vllm-smollm2 Deployment
    + Service) + Chainlit (Lab 04 UI manifests). Skip kube-prometheus-stack to preserve ~3.25 GB
    headroom for MinIO + vllm-smollm2-disk. Monitoring can be restarted after Phase 03 if needed.
  </what-built>
  <how-to-verify>
    Run these steps sequentially from the llmops project directory (where llmops-project/ lives):

    1. Delete existing cluster:
       kind delete cluster --name llmops-kind

    2. Ensure kind-registry is still running:
       docker ps | grep kind-registry
       (If not running, start it: docker run -d --restart=always -p "5001:5000" --network kind --name kind-registry registry:2)

    3. Create new cluster with updated config:
       kind create cluster --config course-code/labs/lab-00/solution/setup/kind-config.yaml

    4. Verify NodePort bindings in the new cluster:
       docker inspect llmops-kind-control-plane --format '{{json .HostConfig.PortBindings}}' | python3 -m json.tool | grep -E "30203|30900|30901"
       Expected: three entries showing each port bound to 0.0.0.0

    5. Connect registry to kind network (if needed after cluster recreate):
       docker network connect kind kind-registry 2>/dev/null || true

    6. Apply namespaces:
       kubectl apply -f course-code/shared/k8s/namespaces.yaml

    7. Deploy RAG retriever (Lab 01):
       kubectl apply -f course-code/labs/lab-01/solution/k8s/

    8. Deploy vLLM Pattern A + Chainlit (Lab 04):
       kubectl apply -f course-code/labs/lab-04/solution/k8s/

    9. Wait for vLLM readiness (takes 6-10 minutes — image pull + model load):
       kubectl rollout status deployment/vllm-smollm2 -n llm-serving --timeout=900s

    10. Verify vLLM NodePort 30200 responds:
        curl -s http://localhost:30200/health
        Expected output: {"status":"healthy"} or similar 200 OK response

    11. Verify Chainlit NodePort 30300 responds:
        curl -s -o /dev/null -w "%{http_code}" http://localhost:30300
        Expected: 200

    Type "approved" when all NodePort bindings are confirmed and vLLM is healthy.
  </how-to-verify>
  <resume-signal>Type "approved" after confirming NodePorts 30203/30900/30901 are bound AND vLLM /health returns 200 on localhost:30200. Or describe what failed.</resume-signal>
  <acceptance_criteria>
    - docker inspect llmops-kind-control-plane shows hostPort bindings for 30203, 30900, and 30901
    - kubectl get nodes shows 3 nodes (control-plane + worker + worker2) all in Ready state
    - kubectl get pods -n llm-serving shows vllm-smollm2 pod in Running/1/1 state
    - curl http://localhost:30200/health returns HTTP 200
    - curl http://localhost:30300 returns HTTP 200 (Chainlit UI)
  </acceptance_criteria>
  <done>KIND cluster recreated with updated NodePorts; Phase 02 base stack (retriever + vLLM Pattern A + Chainlit) running and reachable from host</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → KIND NodePort | Student's localhost curl → KIND control-plane iptables → Pod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | kind-config.yaml | accept | YAML is course config, not secret; reviewed during checkpoint before cluster create |
| T-03-SC | Tampering | Helm/kubectl installs | accept | No new packages installed in this plan; cluster recreate uses existing pinned kind CLI |
</threat_model>

<verification>
- grep "30203\|30900\|30901" course-code/labs/lab-00/solution/setup/kind-config.yaml confirms three new entries
- grep "30203\|30900\|30901" course-code/labs/lab-00/starter/setup/kind-config.yaml confirms three new entries
- Human checkpoint confirms cluster NodePort bindings and vLLM health
</verification>

<success_criteria>
- Both kind-config.yaml files (solution + starter) contain extraPortMappings for 30203, 30900, 30901
- KIND cluster llmops-kind running with those NodePorts bound on the host
- vllm-smollm2 in llm-serving namespace is Running and returns 200 on localhost:30200
- No existing Phase 02 NodePorts (30200, 30300, 30400, 30500) were removed
</success_criteria>

<output>
Create .planning/phases/03-disk-based-model-loading-minio-initcontainer/03-01-SUMMARY.md when done
</output>
