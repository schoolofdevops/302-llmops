---
phase: 03-disk-based-model-loading-minio-initcontainer
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - course-code/labs/lab-06/solution/k8s/00-namespace-minio.yaml
  - course-code/labs/lab-06/solution/k8s/10-minio-values.yaml
  - course-code/labs/lab-06/solution/k8s/20-job-model-uploader.yaml
  - course-code/labs/lab-06/starter/k8s/00-namespace-minio.yaml
  - course-code/labs/lab-06/starter/k8s/10-minio-values.yaml
  - course-code/labs/lab-06/starter/k8s/20-job-model-uploader.yaml
autonomous: false
requirements: [PACKAGE-02]

must_haves:
  truths:
    - "MinIO is running in namespace 'minio' as a 1-replica Deployment, accessible via NodePort 30900 (S3 API)"
    - "MinIO console is accessible via NodePort 30901"
    - "Bucket 'models' exists in MinIO containing smollm2-finetuned/ with model.safetensors (513 MB) and supporting files"
    - "The model-uploader Job completed successfully (status: Complete 1/1)"
    - "mc ls from inside the cluster confirms the upload: minio/models/smollm2-finetuned/ has model.safetensors"
  artifacts:
    - path: "course-code/labs/lab-06/solution/k8s/00-namespace-minio.yaml"
      provides: "minio namespace manifest"
      contains: "name: minio"
    - path: "course-code/labs/lab-06/solution/k8s/10-minio-values.yaml"
      provides: "MinIO Helm values (standalone mode, NodePort 30900/30901)"
      contains: "mode: standalone"
    - path: "course-code/labs/lab-06/solution/k8s/20-job-model-uploader.yaml"
      provides: "model-uploader one-shot Job"
      contains: "model-uploader"
  key_links:
    - from: "model-uploader Job"
      to: "MinIO S3 API"
      via: "mc alias set minio http://minio.minio:9000 minio minio123"
      pattern: "minio.minio:9000"
    - from: "/mnt/project/training/merged-model"
      to: "minio/models/smollm2-finetuned/"
      via: "mc cp --recursive"
      pattern: "mc cp --recursive"

user_setup: []
---

<objective>
Create Kubernetes manifests for MinIO standalone installation and the one-shot model-uploader Job,
then verify the merged-model artifact is in MinIO's S3 bucket at s3://models/smollm2-finetuned/.

Purpose: MinIO is the in-cluster object store that Pattern B (disk-based loading) depends on. The
model-uploader Job copies the Lab 02 merged-model from the hostPath mount (/mnt/project/training/merged-model)
into MinIO once. All subsequent Pattern B pod starts download from MinIO, not from the host filesystem.

Output: solution/ and starter/ k8s manifests for namespace, Helm values, and uploader Job; MinIO
running at NodePort 30900; bucket models populated; model-uploader Job Complete.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-disk-based-model-loading-minio-initcontainer/03-RESEARCH.md
@.planning/phases/03-disk-based-model-loading-minio-initcontainer/03-01-SUMMARY.md

<interfaces>
<!-- MinIO Helm chart: minio-official/minio, version 5.4.0 from https://charts.min.io/
     Repo add: helm repo add minio-official https://charts.min.io/

     Verified standalone install command:
       helm install minio minio-official/minio \
         --namespace minio --create-namespace \
         -f course-code/labs/lab-06/solution/k8s/10-minio-values.yaml

     Verified values.yaml structure (chart 5.4.0):
       mode: standalone
       replicas: 1
       rootUser: minio
       rootPassword: minio123
       resources:
         requests:
           memory: 512Mi
         limits:
           memory: 1Gi
       persistence:
         enabled: true
         size: 2Gi
       service:
         type: NodePort
         nodePort: 30900
       consoleService:
         type: NodePort
         nodePort: 30901

     MinIO server image: quay.io/minio/minio:RELEASE.2024-12-18T13-15-44Z (multi-arch, pulled by chart)
     mc image: quay.io/minio/mc:RELEASE.2024-11-21T17-21-54Z (used in uploader Job)

     Model artifact location:
       Host: ~/llmops-project/training/merged-model/ (macOS host)
       KIND nodes: /mnt/project/training/merged-model/ (all three nodes via Docker bind-mount)
       Files: model.safetensors (513 MB), config.json, generation_config.json,
              tokenizer.json, tokenizer_config.json, chat_template.jinja (3.4 MB total non-model)
       sha256 of model.safetensors: 4a946ea7a4550946a9ac38a302cfe2703b6724e9e1392fb32c30f32d0a9c5108

     S3 target bucket/path: minio/models/smollm2-finetuned/

     CRITICAL: model-uploader Job does NOT need nodeName constraint. All three KIND nodes
     mount the same macOS host llmops-project/ directory via Docker bind-mount. The merged-model
     files are visible to any node that schedules the Job.

     ClusterIP DNS for mc: http://minio.minio:9000 (NOT NodePort 30900 from inside the cluster)
     NodePort 30900 is for student host access only.

     Wait-for-MinIO strategy: use retry loop in the mc container script (single container, no
     separate initContainer). Pattern: `until mc alias set minio http://minio.minio:9000 minio minio123; do sleep 3; done`

     Starter vs solution distinction:
       - 00-namespace-minio.yaml: complete in both (no blanks needed)
       - 10-minio-values.yaml: complete in both (students install, not write)
       - 20-job-model-uploader.yaml:
           solution/: complete with mc alias + mb + cp command
           starter/: BLANK the mc command sequence (students fill in from lab guide)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MinIO namespace manifest and Helm values file (solution + starter)</name>
  <files>
    course-code/labs/lab-06/solution/k8s/00-namespace-minio.yaml
    course-code/labs/lab-06/solution/k8s/10-minio-values.yaml
    course-code/labs/lab-06/starter/k8s/00-namespace-minio.yaml
    course-code/labs/lab-06/starter/k8s/10-minio-values.yaml
  </files>
  <read_first>
    - course-code/labs/lab-00/solution/setup/kind-config.yaml — verify namespace convention (no special labels needed)
    - .planning/phases/03-disk-based-model-loading-minio-initcontainer/03-RESEARCH.md — Pattern 1 section for exact values structure
  </read_first>
  <action>
    Create the lab-06 directory structure and four files:

    course-code/labs/lab-06/solution/k8s/00-namespace-minio.yaml:
      Standard namespace manifest. name: minio. Add labels: course: llmops, phase: "03".

    course-code/labs/lab-06/solution/k8s/10-minio-values.yaml:
      MinIO Helm values for chart 5.4.0 in standalone mode. Include ALL of these fields:
        mode: standalone
        replicas: 1
        rootUser: minio
        rootPassword: minio123
        resources.requests.memory: 512Mi
        resources.limits.memory: 1Gi
        persistence.enabled: true
        persistence.size: 2Gi
        service.type: NodePort
        service.nodePort: 30900
        consoleService.type: NodePort
        consoleService.nodePort: 30901

      Add a YAML comment block at the top explaining: course-grade credentials (demo only, not production),
      and which chart version this is tested with (minio-official/minio 5.4.0).

    course-code/labs/lab-06/starter/k8s/00-namespace-minio.yaml:
      Identical to solution (namespace needs no blanks).

    course-code/labs/lab-06/starter/k8s/10-minio-values.yaml:
      Identical to solution (students install the chart using this file, they don't write it).
  </action>
  <verify>
    <automated>grep -c "mode: standalone" course-code/labs/lab-06/solution/k8s/10-minio-values.yaml && grep -c "nodePort: 30900" course-code/labs/lab-06/solution/k8s/10-minio-values.yaml && grep -c "nodePort: 30901" course-code/labs/lab-06/solution/k8s/10-minio-values.yaml</automated>
  </verify>
  <acceptance_criteria>
    - 00-namespace-minio.yaml exists in both solution/ and starter/ with apiVersion: v1, kind: Namespace, metadata.name: minio
    - 10-minio-values.yaml exists in both solution/ and starter/ with mode: standalone, replicas: 1, nodePort: 30900, nodePort: 30901, persistence.size: 2Gi
    - Both starter files are identical to their solution counterparts (no blanks for namespace or values)
  </acceptance_criteria>
  <done>MinIO namespace manifest and Helm values created for both solution/ and starter/; values file has all required fields for standalone mode</done>
</task>

<task type="auto">
  <name>Task 2: Create model-uploader Job manifest (solution + starter with blanks)</name>
  <files>
    course-code/labs/lab-06/solution/k8s/20-job-model-uploader.yaml
    course-code/labs/lab-06/starter/k8s/20-job-model-uploader.yaml
  </files>
  <read_first>
    - .planning/phases/03-disk-based-model-loading-minio-initcontainer/03-RESEARCH.md — Pattern 2 section for exact Job structure
    - course-code/labs/lab-04/solution/k8s/30-deploy-vllm.yaml — reference for YAML conventions (4-space indent, label style, namespace convention)
  </read_first>
  <action>
    Create course-code/labs/lab-06/solution/k8s/20-job-model-uploader.yaml — a complete, working Job:

      apiVersion: batch/v1
      kind: Job
      metadata:
        name: model-uploader
        namespace: llm-app
        labels:
          app: model-uploader
          course: llmops
      spec:
        ttlSecondsAfterFinished: 300
        backoffLimit: 2
        template:
          spec:
            restartPolicy: Never
            containers:
            - name: uploader
              image: quay.io/minio/mc:RELEASE.2024-11-21T17-21-54Z
              command:
                - sh
                - -c
                - |
                  set -euo pipefail
                  echo "Waiting for MinIO to be ready..."
                  until mc alias set minio http://minio.minio:9000 minio minio123 2>/dev/null; do
                    echo "MinIO not ready yet, retrying in 5s..."; sleep 5;
                  done
                  echo "MinIO ready. Creating bucket..."
                  mc mb --ignore-existing minio/models
                  echo "Uploading model..."
                  mc cp --recursive /mnt/model/ minio/models/smollm2-finetuned/
                  echo "Upload complete. Listing contents..."
                  mc ls minio/models/smollm2-finetuned/
              resources:
                requests:
                  cpu: 100m
                  memory: 128Mi
                limits:
                  cpu: 500m
                  memory: 256Mi
              volumeMounts:
              - name: model
                mountPath: /mnt/model
                readOnly: true
            volumes:
            - name: model
              hostPath:
                path: /mnt/project/training/merged-model
                type: Directory

      Add a comment block at the top clarifying:
        - No nodeName constraint needed (all KIND nodes share the macOS host bind-mount)
        - Uses ClusterIP DNS minio.minio:9000 (not NodePort 30900)
        - ttlSecondsAfterFinished: 300 cleans up after 5 minutes

    Create course-code/labs/lab-06/starter/k8s/20-job-model-uploader.yaml — identical structure but
    blank out the mc command sequence. Replace the shell script body with:
      # TODO: Add mc commands here (see lab guide Part 2 for the complete command sequence)
      # Hint: mc alias set ... then mc mb ... then mc cp ...
    Keep all other fields (image, resources, volumeMounts, volumes, labels) complete and correct.
  </action>
  <verify>
    <automated>grep -c "minio.minio:9000" course-code/labs/lab-06/solution/k8s/20-job-model-uploader.yaml && grep -c "mc cp --recursive" course-code/labs/lab-06/solution/k8s/20-job-model-uploader.yaml && grep -c "TODO" course-code/labs/lab-06/starter/k8s/20-job-model-uploader.yaml</automated>
  </verify>
  <acceptance_criteria>
    - solution/20-job-model-uploader.yaml is a complete Job using quay.io/minio/mc:RELEASE.2024-11-21T17-21-54Z
    - mc alias set uses http://minio.minio:9000 (ClusterIP, NOT NodePort)
    - mc cp --recursive /mnt/model/ minio/models/smollm2-finetuned/ is in the solution
    - hostPath path is /mnt/project/training/merged-model (exact path from RESEARCH.md verification)
    - ttlSecondsAfterFinished: 300 and backoffLimit: 2 present
    - starter/20-job-model-uploader.yaml has the mc command body blanked with TODO comments
    - Both files have namespace: llm-app (not minio — Job is in the app namespace)
    - nodeName: field is NOT present in solution/20-job-model-uploader.yaml (all KIND nodes share the macOS host bind-mount; no node constraint needed — see RESEARCH.md Pitfall 4)
  </acceptance_criteria>
  <done>model-uploader Job manifests created: solution/ is complete; starter/ has mc command section blanked for students to fill in</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Install MinIO and run model-uploader Job; verify model in S3</name>
  <read_first>
    - course-code/labs/lab-06/solution/k8s/10-minio-values.yaml — verify values before helm install
    - course-code/labs/lab-06/solution/k8s/20-job-model-uploader.yaml — verify Job before apply
    - .planning/phases/03-disk-based-model-loading-minio-initcontainer/03-RESEARCH.md — Pitfall 5 (must pass both mode=standalone AND replicas=1)
  </read_first>
  <what-built>
    Tasks 1 and 2 created the manifests for MinIO install and model upload. This checkpoint
    installs MinIO via Helm and runs the model-uploader Job to populate the S3 bucket.
  </what-built>
  <how-to-verify>
    Run these steps from the repo root:

    1. Add MinIO Helm repo (if not already added):
       helm repo add minio-official https://charts.min.io/
       helm repo update minio-official

    2. Verify chart version:
       helm search repo minio-official/minio --versions | head -3
       Expected: minio-official/minio 5.4.0 RELEASE.2024-12-18T13-15-44Z

    3. Install MinIO:
       kubectl create namespace minio 2>/dev/null || true
       helm install minio minio-official/minio \
         --namespace minio \
         -f course-code/labs/lab-06/solution/k8s/10-minio-values.yaml

    4. Wait for MinIO pod readiness:
       kubectl rollout status deployment/minio -n minio --timeout=120s

    5. Confirm exactly 1 pod running (not 16 — Pitfall 5 check):
       kubectl get pods -n minio
       Expected: exactly 1 pod in Running state

    6. Test MinIO S3 API health from host (NodePort 30900):
       curl -s http://localhost:30900/minio/health/live
       Expected: HTTP 200 with empty body (or echo exit code 0)

    7. Open MinIO console in browser:
       http://localhost:30901 — login with minio / minio123
       (Should show empty object browser)

    8. Apply model-uploader Job:
       kubectl apply -f course-code/labs/lab-06/solution/k8s/20-job-model-uploader.yaml

    9. Wait for Job completion (upload takes ~30-60s on local network):
       kubectl wait --for=condition=complete job/model-uploader -n llm-app --timeout=300s

    10. Verify upload contents:
        kubectl run verify-mc --rm -it --restart=Never \
          --image=quay.io/minio/mc:RELEASE.2024-11-21T17-21-54Z \
          -- sh -c "mc alias set minio http://minio.minio:9000 minio minio123 && mc ls minio/models/smollm2-finetuned/"
        Expected: model.safetensors (513 MB) and supporting files listed

    11. Refresh MinIO console (http://localhost:30901) — bucket "models" should appear with smollm2-finetuned/ folder.

    Type "approved" when:
    - MinIO pod is 1/1 Running
    - localhost:30900/minio/health/live returns 200
    - model-uploader Job shows Complete 1/1
    - mc ls confirms model.safetensors is in minio/models/smollm2-finetuned/
  </how-to-verify>
  <resume-signal>Type "approved" after confirming MinIO is healthy AND model upload Job completed AND mc ls shows model.safetensors in the bucket. Or describe what failed.</resume-signal>
  <acceptance_criteria>
    - kubectl get pods -n minio shows exactly 1 pod in Running state (NOT 16 — Pitfall 5)
    - curl http://localhost:30900/minio/health/live returns HTTP 200
    - kubectl get jobs -n llm-app shows model-uploader with COMPLETIONS 1/1
    - mc ls minio/models/smollm2-finetuned/ shows model.safetensors
  </acceptance_criteria>
  <done>MinIO running in minio namespace; model-uploader Job complete; merged-model accessible at s3://models/smollm2-finetuned/ in MinIO</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → MinIO NodePort 30900 | Student's localhost curl → MinIO S3 API — unauthenticated health check only |
| Job container → MinIO ClusterIP | mc commands use plaintext minio/minio123 credentials in-cluster |
| hostPath /mnt/project | Read-only mount of host project directory into Job container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | minio/minio123 credentials in YAML | accept | Course-grade credentials hardcoded for learning; lab text explicitly notes demo-grade. Not production pattern. |
| T-03-03 | Elevation of Privilege | hostPath /mnt/project in Job | accept | Read-only mount; KIND cluster is local dev only; no production data in course project directory |
| T-03-04 | Denial of Service | MinIO 2Gi PVC | accept | Local cluster dev only; no production SLA; local-path-provisioner handles cleanup |
| T-03-SC | Tampering | quay.io/minio/mc image | mitigate | Package Legitimacy Audit in RESEARCH.md confirms minio/mc is official MinIO org image from quay.io; multi-arch verified |
</threat_model>

<verification>
- helm list -n minio shows minio release at chart version 5.4.0
- kubectl get pods -n minio shows 1/1 Running (not 16)
- curl http://localhost:30900/minio/health/live returns 200
- kubectl get jobs -n llm-app shows model-uploader Complete 1/1
- mc ls confirms model.safetensors in bucket
</verification>

<success_criteria>
- MinIO standalone running in minio namespace, accessible on NodePort 30900 and 30901
- bucket 'models' exists with smollm2-finetuned/ containing the merged model artifact (513 MB model.safetensors + supporting files)
- All solution/ and starter/ manifests created at course-code/labs/lab-06/
- starter/ Job manifest has mc command section blanked for lab exercise
</success_criteria>

<output>
Create .planning/phases/03-disk-based-model-loading-minio-initcontainer/03-02-SUMMARY.md when done
</output>
