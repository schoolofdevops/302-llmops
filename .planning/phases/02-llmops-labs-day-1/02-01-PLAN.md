---
phase: 02-llmops-labs-day-1
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - course-code/config.env
  - course-code/COURSE_VERSIONS.md
autonomous: true
requirements:
  - SERVE-01
  - SERVE-02
must_haves:
  truths:
    - "config.env VLLM_IMAGE references vllm/vllm-openai-cpu:v0.19.0-x86_64 (not the abandoned schoolofdevops image)"
    - "COURSE_VERSIONS.md vLLM row uses the correct official image tag"
    - "All other config.env values (namespaces, BASE_MODEL, EMBEDDING_MODEL) remain unchanged"
  artifacts:
    - path: "course-code/config.env"
      provides: "Central config for all labs"
      contains: "VLLM_IMAGE=vllm/vllm-openai-cpu:v0.19.0-x86_64"
    - path: "course-code/COURSE_VERSIONS.md"
      provides: "Pinned version table"
      contains: "vllm/vllm-openai-cpu:v0.19.0-x86_64"
  key_links:
    - from: "course-code/config.env"
      to: "all lab YAML manifests"
      via: "source config.env in scripts"
      pattern: "VLLM_IMAGE=vllm/vllm-openai-cpu"
---

<objective>
Fix the vLLM image reference in config.env and COURSE_VERSIONS.md.

Purpose: The existing config.env references `schoolofdevops/vllm-cpu-nonuma:0.9.1`, an abandoned image that will fail on `docker pull`. The official CPU image is `vllm/vllm-openai-cpu:v0.19.0-x86_64`. Every Lab 04 (serving) task depends on this being correct. Additionally, COURSE_VERSIONS.md has an internal inconsistency where the vLLM row text says v0.19.0 but the image field still shows the old schoolofdevops tag.

Output: Updated config.env and COURSE_VERSIONS.md with correct vLLM 0.19.0 image references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-llmops-labs-day-1/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update config.env with correct vLLM image</name>
  <files>course-code/config.env</files>
  <read_first>
    - course-code/config.env — Read the full file before editing to preserve all other values
  </read_first>
  <action>
    Update a single line in course-code/config.env:

    Change:
      VLLM_IMAGE=schoolofdevops/vllm-cpu-nonuma:0.9.1
    To:
      VLLM_IMAGE=vllm/vllm-openai-cpu:v0.19.0-x86_64

    Leave ALL other lines unchanged:
    - CLUSTER_NAME=llmops-kind
    - PROJECT_DIR=./llmops-project
    - MODEL_IMAGE_TAG=v1.0.0
    - BASE_MODEL=HuggingFaceTB/SmolLM2-135M-Instruct
    - EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
    - NS_SERVING=llm-serving
    - NS_APP=llm-app
    - NS_MONITORING=monitoring
    - NS_ARGOCD=argocd
    - NS_ARGO_WORKFLOWS=argo-workflows

    Add a comment above VLLM_IMAGE:
    # Official CPU image: x86_64 for Intel/AMD, change to v0.19.0-arm64 on Apple Silicon
  </action>
  <verify>
    <automated>grep "VLLM_IMAGE=vllm/vllm-openai-cpu:v0.19.0-x86_64" /Users/gshah/courses/llmops/course-code/config.env</automated>
  </verify>
  <acceptance_criteria>
    - grep returns exactly one line matching VLLM_IMAGE=vllm/vllm-openai-cpu:v0.19.0-x86_64
    - grep for "schoolofdevops" returns no matches in config.env
    - All 9 other config lines still present (verify with wc -l showing same or +1 line count)
  </acceptance_criteria>
  <done>config.env VLLM_IMAGE points to official CPU image; no other values changed</done>
</task>

<task type="auto">
  <name>Task 2: Fix COURSE_VERSIONS.md vLLM row</name>
  <files>course-code/COURSE_VERSIONS.md</files>
  <read_first>
    - course-code/COURSE_VERSIONS.md — Read full file to find the vLLM row and KServe row
  </read_first>
  <action>
    In course-code/COURSE_VERSIONS.md, make two changes:

    1. Find the vLLM row in the "Serving & Deployment" table. Update it to:
       | vLLM | v0.19.0 | Official CPU image: `vllm/vllm-openai-cpu:v0.19.0-x86_64` (x86_64) or `v0.19.0-arm64` (Apple Silicon); Ubuntu 22.04, Python 3.12, torch 2.11.0 |

    2. Remove or update the KServe row — per D-10, Phase 2 uses plain K8s Deployment, not KServe.
       Change the KServe row to:
       | KServe | N/A (Phase 2) | Not used in Day 1 labs; plain K8s Deployment used for vLLM serving (Phase 3+ only) |

    Do not change any other rows or sections in the file.
  </action>
  <verify>
    <automated>grep "vllm/vllm-openai-cpu:v0.19.0-x86_64" /Users/gshah/courses/llmops/course-code/COURSE_VERSIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - grep returns match for "vllm/vllm-openai-cpu:v0.19.0-x86_64" in COURSE_VERSIONS.md
    - grep for "schoolofdevops" returns no matches in COURSE_VERSIONS.md
    - File still contains all other version table rows (Python, PyTorch, Transformers, etc.)
  </acceptance_criteria>
  <done>COURSE_VERSIONS.md vLLM row shows correct official image; no internal inconsistency between version number and image tag</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep "schoolofdevops" course-code/config.env course-code/COURSE_VERSIONS.md` returns no matches
2. `grep "vllm/vllm-openai-cpu:v0.19.0-x86_64" course-code/config.env` returns 1 match
3. `grep "vllm/vllm-openai-cpu:v0.19.0-x86_64" course-code/COURSE_VERSIONS.md` returns 1 match
</verification>

<success_criteria>
- config.env VLLM_IMAGE uses the official vllm/vllm-openai-cpu:v0.19.0-x86_64 image
- COURSE_VERSIONS.md vLLM row is internally consistent
- No other config values were altered
- The abandoned schoolofdevops image name appears nowhere in tracked files
</success_criteria>

<output>
After completion, create `.planning/phases/02-llmops-labs-day-1/02-01-SUMMARY.md`
</output>
