---
phase: 02-llmops-labs-day-1
plan: "06"
type: execute
wave: 4
depends_on:
  - "02-04"
files_modified:
  - course-code/labs/lab-06/solution/k8s/observability/50-servicemonitor-vllm.yaml
  - course-code/labs/lab-06/solution/k8s/observability/50-servicemonitor-retriever.yaml
  - course-code/labs/lab-06/solution/k8s/observability/50-servicemonitor-chainlit.yaml
  - course-code/labs/lab-06/solution/k8s/observability/50-grafana-dashboard-cm.yaml
  - course-code/labs/lab-06/solution/scripts/install-monitoring.sh
  - course-code/labs/lab-06/starter/k8s/observability/50-servicemonitor-vllm.yaml
  - course-code/labs/lab-06/starter/k8s/observability/50-servicemonitor-retriever.yaml
  - course-code/labs/lab-06/starter/k8s/observability/50-servicemonitor-chainlit.yaml
  - course-code/labs/lab-06/starter/k8s/observability/50-grafana-dashboard-cm.yaml
  - course-code/labs/lab-06/starter/scripts/install-monitoring.sh
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - OBS-03
  - OBS-04
must_haves:
  truths:
    - "Student can run install-monitoring.sh and get kube-prometheus-stack running with Grafana on NodePort 30400"
    - "Prometheus scrapes vLLM /metrics endpoint (vllm: prefixed metrics visible in Grafana)"
    - "Grafana dashboard shows TTFT, latency, and token throughput panels"
    - "ServiceMonitor for retriever scrapes prometheus-client /metrics from llm-app namespace"
  artifacts:
    - path: "course-code/labs/lab-06/solution/k8s/observability/50-servicemonitor-vllm.yaml"
      provides: "Prometheus ServiceMonitor for vLLM metrics"
      contains: "matchNames: [llm-serving]"
    - path: "course-code/labs/lab-06/solution/k8s/observability/50-grafana-dashboard-cm.yaml"
      provides: "Grafana dashboard ConfigMap with vLLM panels"
      contains: "vllm:time_to_first_token_seconds"
    - path: "course-code/labs/lab-06/solution/scripts/install-monitoring.sh"
      provides: "Helm install script for kube-prometheus-stack"
      contains: "kube-prometheus-stack"
  key_links:
    - from: "course-code/labs/lab-06/solution/k8s/observability/50-servicemonitor-vllm.yaml"
      to: "vllm-smollm2 Service in llm-serving"
      via: "selector.matchLabels app: vllm"
      pattern: "matchLabels.*app: vllm"
    - from: "course-code/labs/lab-06/solution/k8s/observability/50-grafana-dashboard-cm.yaml"
      to: "Prometheus datasource"
      via: "ConfigMap label grafana_dashboard: '1'"
      pattern: "grafana_dashboard"
---

<objective>
Create the complete Lab 06 code — Prometheus + Grafana observability for the LLM pipeline.

Purpose: Lab 06 installs kube-prometheus-stack via Helm and configures ServiceMonitors to scrape vLLM, the retriever, and the Chainlit UI. A Grafana dashboard ConfigMap shows TTFT, latency, and token throughput. Per OBS-02, the vLLM v1 metric names use the `vllm:` prefix (colon, not underscore) — a common pitfall from the old lab code that must be corrected. Per OBS-03, the retriever and chat app already expose /metrics via prometheus-client (from Lab 01/Lab 05 code).

Output: solution/ has complete K8s manifests (ServiceMonitors + dashboard ConfigMap) and Helm install script. starter/ has skeleton files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-llmops-labs-day-1/02-CONTEXT.md
@.planning/phases/02-llmops-labs-day-1/02-RESEARCH.md
@course-code/config.env

<!-- CRITICAL: Pitfall 3 from RESEARCH.md — vLLM v1 metric names use COLON not underscore -->
<!-- Correct metric names (vLLM v0.19.x):
     TTFT:        vllm:time_to_first_token_seconds (Histogram)
     E2E latency: vllm:e2e_request_latency_seconds (Histogram)
     Generation:  rate(vllm:generation_tokens[1m])
     Prompt:      rate(vllm:prompt_tokens[1m])
     Running:     vllm:num_requests_running (Gauge)
     Waiting:     vllm:num_requests_waiting (Gauge)
     KV cache:    vllm:kv_cache_usage_perc (Gauge)
-->
<!-- BAD (old vLLM < 0.15): vllm_request_ttft_seconds, vllm_e2e_request_latency_seconds -->
<!-- kube-prometheus-stack chart: 83.4.2, Helm repo: prometheus-community -->

<interfaces>
<!-- ServiceMonitor CRD from RESEARCH.md Pattern 4 -->
```yaml
# ServiceMonitor selects Services by label, not pod labels
# namespace: monitoring (where kube-prometheus-stack is installed)
# namespaceSelector: { matchNames: [target-namespace] }
# endpoints.port must match a named port in the Service spec
```

<!-- Grafana dashboard auto-discovery: label grafana_dashboard: "1" on ConfigMap -->
<!-- Prometheus scrape flags for kube-prometheus-stack Helm:
     prometheus.prometheusSpec.podMonitorSelectorNilUsesHelmValues=false
     prometheus.prometheusSpec.serviceMonitorSelectorNilUsesHelmValues=false
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ServiceMonitors for vLLM, retriever, and Chainlit (solution/ + starter/)</name>
  <files>
    course-code/labs/lab-06/solution/k8s/observability/50-servicemonitor-vllm.yaml,
    course-code/labs/lab-06/solution/k8s/observability/50-servicemonitor-retriever.yaml,
    course-code/labs/lab-06/solution/k8s/observability/50-servicemonitor-chainlit.yaml,
    course-code/labs/lab-06/solution/scripts/install-monitoring.sh,
    course-code/labs/lab-06/starter/k8s/observability/50-servicemonitor-vllm.yaml,
    course-code/labs/lab-06/starter/k8s/observability/50-servicemonitor-retriever.yaml,
    course-code/labs/lab-06/starter/k8s/observability/50-servicemonitor-chainlit.yaml,
    course-code/labs/lab-06/starter/scripts/install-monitoring.sh
  </files>
  <read_first>
    - llmops-labuide/docs/lab05.md — Full file for existing ServiceMonitor patterns and Helm install (reference for rewrite)
    - .planning/phases/02-llmops-labs-day-1/02-RESEARCH.md — Lines 413-437 for ServiceMonitor Pattern 4 (critical vllm: prefix note)
    - .planning/phases/02-llmops-labs-day-1/02-RESEARCH.md — Lines 150-169 for kube-prometheus-stack Helm install flags
    - .planning/phases/02-llmops-labs-day-1/02-RESEARCH.md — Lines 515-527 for Pitfall 3 (CRITICAL: vllm: metric names)
  </read_first>
  <action>
    **course-code/labs/lab-06/solution/scripts/install-monitoring.sh:**
    ```bash
    #!/usr/bin/env bash
    # install-monitoring.sh — Install kube-prometheus-stack for Lab 06 observability
    # Chart: prometheus-community/kube-prometheus-stack 83.4.2
    # Grafana: NodePort 30400 | Prometheus: NodePort 30500
    #
    # Usage: bash install-monitoring.sh

    set -euo pipefail

    NAMESPACE="monitoring"
    RELEASE="kps"
    CHART="prometheus-community/kube-prometheus-stack"
    CHART_VERSION="83.4.2"

    echo "=== Installing kube-prometheus-stack ==="
    echo "  Namespace: ${NAMESPACE}"
    echo "  Grafana:   http://localhost:30400 (admin/prom-operator)"
    echo "  Prometheus: http://localhost:30500"
    echo ""

    # Add Helm repo
    helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
    helm repo update

    # Install / upgrade
    helm upgrade --install "${RELEASE}" "${CHART}" \
      --version "${CHART_VERSION}" \
      --namespace "${NAMESPACE}" \
      --create-namespace \
      --set grafana.service.type=NodePort \
      --set grafana.service.nodePort=30400 \
      --set prometheus.service.type=NodePort \
      --set prometheus.service.nodePort=30500 \
      --set prometheus.prometheusSpec.podMonitorSelectorNilUsesHelmValues=false \
      --set prometheus.prometheusSpec.serviceMonitorSelectorNilUsesHelmValues=false \
      --set grafana.adminPassword=prom-operator \
      --set alertmanager.enabled=false \
      --wait \
      --timeout 5m

    echo ""
    echo "=== kube-prometheus-stack installed ==="
    echo "  Grafana:    http://localhost:30400  (user: admin, pass: prom-operator)"
    echo "  Prometheus: http://localhost:30500"
    echo ""
    echo "Next: Apply ServiceMonitors from k8s/observability/"
    echo "  kubectl apply -f course-code/labs/lab-06/solution/k8s/observability/"
    ```

    **course-code/labs/lab-06/solution/k8s/observability/50-servicemonitor-vllm.yaml:**
    ```yaml
    # Lab 06: Prometheus ServiceMonitor for vLLM
    # IMPORTANT: vLLM v0.19.x uses 'vllm:' prefix in metric names (colon, not underscore)
    # Correct: vllm:time_to_first_token_seconds, vllm:e2e_request_latency_seconds
    # Wrong (old vLLM < 0.15): vllm_request_ttft_seconds
    apiVersion: monitoring.coreos.com/v1
    kind: ServiceMonitor
    metadata:
      name: vllm-monitor
      namespace: monitoring
      labels:
        release: kps   # Must match kube-prometheus-stack release name
    spec:
      namespaceSelector:
        matchNames:
          - llm-serving
      selector:
        matchLabels:
          app: vllm
      endpoints:
        - port: http
          path: /metrics
          interval: 30s
          scrapeTimeout: 10s
    ```

    **course-code/labs/lab-06/solution/k8s/observability/50-servicemonitor-retriever.yaml:**
    ```yaml
    # Lab 06: Prometheus ServiceMonitor for RAG Retriever
    # Retriever exposes prometheus-client metrics at /metrics (port 8001)
    # Metric prefix: retriever_ (application metrics, underscore naming)
    apiVersion: monitoring.coreos.com/v1
    kind: ServiceMonitor
    metadata:
      name: retriever-monitor
      namespace: monitoring
      labels:
        release: kps
    spec:
      namespaceSelector:
        matchNames:
          - llm-app
      selector:
        matchLabels:
          app: rag-retriever
      endpoints:
        - port: http
          path: /metrics
          interval: 30s
          scrapeTimeout: 10s
    ```

    **course-code/labs/lab-06/solution/k8s/observability/50-servicemonitor-chainlit.yaml:**
    ```yaml
    # Lab 06: Prometheus ServiceMonitor for Chainlit UI
    # Chainlit itself doesn't expose /metrics by default.
    # This ServiceMonitor is a placeholder — in Lab 05 solution, the Chainlit
    # app can be extended with prometheus-client to expose custom metrics.
    # For the base lab, this file shows the pattern but Chainlit may not respond on /metrics.
    apiVersion: monitoring.coreos.com/v1
    kind: ServiceMonitor
    metadata:
      name: chainlit-monitor
      namespace: monitoring
      labels:
        release: kps
    spec:
      namespaceSelector:
        matchNames:
          - llm-app
      selector:
        matchLabels:
          app: chainlit-ui
      endpoints:
        - port: http
          path: /metrics
          interval: 60s
          scrapeTimeout: 10s
    ```

    **STARTER FILES:**
    All starter files in lab-06/starter/ are identical to the solution files.
    Rationale: ServiceMonitor YAML and Helm install scripts are infrastructure-as-code
    provided to students (per D-08/D-09). The lab teaches students to understand and apply
    them, not write them from scratch. Copy all solution files to starter/.
  </action>
  <verify>
    <automated>bash -c "
grep 'vllm:time_to_first_token_seconds\|vllm: metric\|vllm:e2e' course-code/labs/lab-06/solution/k8s/observability/50-servicemonitor-vllm.yaml || echo 'Comment only — OK (metric names in dashboard not ServiceMonitor)'
grep 'matchNames' course-code/labs/lab-06/solution/k8s/observability/50-servicemonitor-vllm.yaml && echo 'namespaceSelector OK'
grep 'kube-prometheus-stack' course-code/labs/lab-06/solution/scripts/install-monitoring.sh && echo 'Helm chart OK'
grep 'nodePort=30400' course-code/labs/lab-06/solution/scripts/install-monitoring.sh && echo 'Grafana NodePort OK'
"</automated>
  </verify>
  <acceptance_criteria>
    - install-monitoring.sh uses helm upgrade --install with chart version 83.4.2
    - install-monitoring.sh sets: grafana.service.nodePort=30400, prometheus.service.nodePort=30500
    - install-monitoring.sh sets: serviceMonitorSelectorNilUsesHelmValues=false (critical for cross-namespace scraping)
    - 50-servicemonitor-vllm.yaml: namespace monitoring, release: kps label, namespaceSelector llm-serving, selector app: vllm
    - 50-servicemonitor-retriever.yaml: namespaceSelector llm-app, selector app: rag-retriever
    - Comment in vllm ServiceMonitor warns about vllm: prefix (educational)
    - starter/ files identical to solution/ (infrastructure files provided)
  </acceptance_criteria>
  <done>Lab 06 ServiceMonitors created for vLLM (llm-serving), retriever and Chainlit (llm-app); Helm install script with correct flags</done>
</task>

<task type="auto">
  <name>Task 2: Grafana dashboard ConfigMap (solution/ + starter/)</name>
  <files>
    course-code/labs/lab-06/solution/k8s/observability/50-grafana-dashboard-cm.yaml,
    course-code/labs/lab-06/starter/k8s/observability/50-grafana-dashboard-cm.yaml
  </files>
  <read_first>
    - llmops-labuide/docs/lab05.md — Lines 150-300 for existing Grafana dashboard JSON (reference — update metric names to vllm: prefix)
    - .planning/phases/02-llmops-labs-day-1/02-RESEARCH.md — Lines 515-527 for Pitfall 3 (vllm: metric names to use)
  </read_first>
  <action>
    Create a Grafana dashboard ConfigMap with panels showing key vLLM v0.19.x metrics.
    The ConfigMap must use label `grafana_dashboard: "1"` for kube-prometheus-stack auto-discovery.

    **course-code/labs/lab-06/solution/k8s/observability/50-grafana-dashboard-cm.yaml:**

    The ConfigMap embeds a Grafana dashboard JSON. Structure:
    - ConfigMap metadata.name: "vllm-dashboard"
    - ConfigMap metadata.namespace: monitoring
    - ConfigMap metadata.labels: grafana_dashboard: "1"
    - ConfigMap data key: "vllm-dashboard.json"

    The dashboard JSON must include these panels (use Grafana panel JSON format):
    1. **Time to First Token (TTFT)** — Histogram using `histogram_quantile(0.95, rate(vllm:time_to_first_token_seconds_bucket[5m]))` — shows P95 latency
    2. **End-to-End Request Latency** — `histogram_quantile(0.95, rate(vllm:e2e_request_latency_seconds_bucket[5m]))` — P95 E2E latency
    3. **Token Throughput** — `rate(vllm:generation_tokens_total[1m])` — tokens generated per second
    4. **Active Requests** — `vllm:num_requests_running` — currently running inference requests
    5. **KV Cache Usage** — `vllm:kv_cache_usage_perc` — percentage of KV cache utilized
    6. **Retriever Search Rate** — `rate(search_requests_total[1m])` — retriever query rate from prometheus-client

    Dashboard JSON structure:
    ```json
    {
      "__inputs": [],
      "__requires": [],
      "annotations": {"list": []},
      "description": "Smile Dental LLM Pipeline — vLLM v0.19.x metrics",
      "editable": true,
      "fiscalYearStartMonth": 0,
      "graphTooltip": 1,
      "id": null,
      "links": [],
      "panels": [
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "fieldConfig": {
            "defaults": {"unit": "s"},
            "overrides": []
          },
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
          "id": 1,
          "options": {"tooltip": {"mode": "single"}},
          "targets": [
            {
              "expr": "histogram_quantile(0.95, rate(vllm:time_to_first_token_seconds_bucket[5m]))",
              "legendFormat": "P95 TTFT",
              "refId": "A"
            }
          ],
          "title": "Time to First Token (P95)",
          "type": "timeseries"
        },
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "fieldConfig": {"defaults": {"unit": "s"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
          "id": 2,
          "targets": [
            {
              "expr": "histogram_quantile(0.95, rate(vllm:e2e_request_latency_seconds_bucket[5m]))",
              "legendFormat": "P95 E2E Latency",
              "refId": "A"
            }
          ],
          "title": "End-to-End Request Latency (P95)",
          "type": "timeseries"
        },
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "fieldConfig": {"defaults": {"unit": "reqps"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
          "id": 3,
          "targets": [
            {
              "expr": "rate(vllm:generation_tokens_total[1m])",
              "legendFormat": "Tokens/sec",
              "refId": "A"
            }
          ],
          "title": "Token Throughput (generation tokens/sec)",
          "type": "timeseries"
        },
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "fieldConfig": {"defaults": {"unit": "short"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
          "id": 4,
          "targets": [
            {
              "expr": "vllm:num_requests_running",
              "legendFormat": "Running",
              "refId": "A"
            },
            {
              "expr": "vllm:num_requests_waiting",
              "legendFormat": "Waiting",
              "refId": "B"
            }
          ],
          "title": "Active & Queued Requests",
          "type": "timeseries"
        },
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "fieldConfig": {"defaults": {"unit": "percentunit", "max": 1}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 16},
          "id": 5,
          "targets": [
            {
              "expr": "vllm:kv_cache_usage_perc",
              "legendFormat": "KV Cache %",
              "refId": "A"
            }
          ],
          "title": "KV Cache Utilization",
          "type": "timeseries"
        },
        {
          "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
          "fieldConfig": {"defaults": {"unit": "reqps"}, "overrides": []},
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 16},
          "id": 6,
          "targets": [
            {
              "expr": "rate(search_requests_total[1m])",
              "legendFormat": "Retriever Queries/sec",
              "refId": "A"
            }
          ],
          "title": "RAG Retriever Query Rate",
          "type": "timeseries"
        }
      ],
      "refresh": "30s",
      "schemaVersion": 38,
      "tags": ["llmops", "vllm", "smollm2"],
      "templating": {
        "list": [
          {
            "current": {},
            "hide": 0,
            "includeAll": false,
            "name": "DS_PROMETHEUS",
            "options": [],
            "query": "prometheus",
            "refresh": 1,
            "type": "datasource"
          }
        ]
      },
      "time": {"from": "now-1h", "to": "now"},
      "timepicker": {},
      "timezone": "browser",
      "title": "Smile Dental — LLM Pipeline",
      "uid": "smile-dental-llm",
      "version": 1
    }
    ```

    Embed this JSON as the value for key `vllm-dashboard.json` in the ConfigMap data section.

    The ConfigMap YAML structure:
    ```yaml
    apiVersion: v1
    kind: ConfigMap
    metadata:
      name: vllm-dashboard
      namespace: monitoring
      labels:
        grafana_dashboard: "1"    # kube-prometheus-stack auto-discovers this
    data:
      vllm-dashboard.json: |
        {JSON content here — use | block scalar for multiline}
    ```

    **starter/ file:** Same as solution (dashboard JSON is provided, not student-written).
  </action>
  <verify>
    <automated>python3 -c "
import yaml, json, sys

# Parse the ConfigMap YAML
with open('course-code/labs/lab-06/solution/k8s/observability/50-grafana-dashboard-cm.yaml') as f:
    cm = yaml.safe_load(f)

assert cm['metadata']['labels'].get('grafana_dashboard') == '1', 'Missing grafana_dashboard: 1 label'
assert 'vllm-dashboard.json' in cm['data'], 'Missing vllm-dashboard.json key in data'

# Parse the embedded JSON
dashboard = json.loads(cm['data']['vllm-dashboard.json'])
panels_with_expr = [p for p in dashboard.get('panels', []) if p.get('targets')]
exprs = ' '.join(str(t.get('expr','')) for p in panels_with_expr for t in p['targets'])

# Check correct vllm: prefix (colon not underscore)
assert 'vllm:time_to_first_token_seconds_bucket' in exprs, 'Missing TTFT metric with colon prefix'
assert 'vllm:e2e_request_latency_seconds_bucket' in exprs, 'Missing latency metric'
assert 'vllm:num_requests_running' in exprs, 'Missing running requests metric'
assert 'vllm:kv_cache_usage_perc' in exprs, 'Missing KV cache metric'

# Confirm no wrong underscore prefix
assert 'vllm_request_ttft' not in exprs, 'Using old underscore metric name (wrong)'

print(f'Dashboard OK: {len(panels_with_expr)} panels, all using vllm: prefix')
"</automated>
  </verify>
  <acceptance_criteria>
    - ConfigMap has label grafana_dashboard: "1" for auto-discovery
    - Dashboard JSON parses as valid JSON
    - All vLLM metric expressions use "vllm:" prefix (colon) not "vllm_" (underscore)
    - Dashboard has >= 5 panels covering: TTFT, latency, throughput, request count, KV cache
    - Panel for retriever metrics uses underscore naming (search_requests_total — application metric)
    - Dashboard namespace is monitoring
    - Starter file identical to solution (JSON dashboard is provided)
  </acceptance_criteria>
  <done>Grafana dashboard ConfigMap with 6 panels using correct vllm: metric names; auto-discovery label applied</done>
</task>

</tasks>

<verification>
1. `grep "grafana_dashboard" course-code/labs/lab-06/solution/k8s/observability/50-grafana-dashboard-cm.yaml` returns match
2. `python3 -c "import yaml; d=yaml.safe_load(open('course-code/labs/lab-06/solution/k8s/observability/50-grafana-dashboard-cm.yaml')); print(d['metadata']['labels'])"` shows grafana_dashboard: '1'
3. `grep "vllm:time_to_first_token" course-code/labs/lab-06/solution/k8s/observability/50-grafana-dashboard-cm.yaml` returns match (colon prefix)
4. `grep "vllm_request_ttft" course-code/labs/lab-06/solution/k8s/observability/50-grafana-dashboard-cm.yaml` returns no match (old underscore format absent)
5. `grep "nodePort=30400" course-code/labs/lab-06/solution/scripts/install-monitoring.sh` returns match
</verification>

<success_criteria>
- install-monitoring.sh installs kube-prometheus-stack 83.4.2 with Grafana on NodePort 30400
- All 3 ServiceMonitors: in monitoring namespace, release: kps label, correct namespaceSelector + pod selector
- serviceMonitorSelectorNilUsesHelmValues=false ensures cross-namespace scraping works
- Grafana dashboard uses vllm: colon prefix in all PromQL expressions (critical fix from Pitfall 3)
- Dashboard has 6 panels: TTFT P95, latency P95, token throughput, request counts, KV cache, retriever rate
- Starter files identical to solution (observability config is not student-written)
</success_criteria>

<output>
After completion, create `.planning/phases/02-llmops-labs-day-1/02-06-SUMMARY.md`
</output>
