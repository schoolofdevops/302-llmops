---
phase: 02.1-flatten-workspace-and-switch-to-uv
plan: 01
subsystem: content
tags: [lab-guides, uv, workspace, docusaurus, markdown]

# Dependency graph
requires:
  - phase: 02-llmops-labs-day-1
    provides: Lab guide markdown files for labs 01-03 with original llmops-project/lab-01/ paths and pip install commands
provides:
  - Flat workspace paths (llmops-project/) in lab guides 01-03 — no per-lab sub-directories
  - uv pip install --system commands replacing pip install in student-facing steps
  - uv installation instructions (astral.sh) in lab-01 for one-time setup
  - Consistent cross-lab data dependency path (lab-03 references llmops-project/datasets/train/)
affects: [lab-04-model-packaging, future lab guide authors]

# Tech tracking
tech-stack:
  added: [uv (astral.sh — student tool, not a code dependency)]
  patterns: [flat workspace pattern for llmops-project/ — no per-lab sub-directories]

key-files:
  created: []
  modified:
    - course-content/docs/labs/lab-01-synthetic-data.md
    - course-content/docs/labs/lab-02-rag-retriever.md
    - course-content/docs/labs/lab-03-finetuning.md

key-decisions:
  - "uv pip install --system (not uv pip install) — --system flag avoids requiring students to create a venv in workshop context"
  - "uv install instructions added to lab-01 only (first lab where uv is used) — not preflight script, deferred to lab-00 authoring phase"
  - "mkdir -p llmops-project (not mkdir -p llmops-project/lab-01) — cp -r creates sub-directories automatically"
  - "K8s initContainer keeps pip unchanged — uv is not installed in the container image, pip is correct there"

patterns-established:
  - "Flat workspace pattern: all student files go directly into llmops-project/, not per-lab sub-directories"
  - "uv pip install --system replaces pip install for student-facing commands"
  - "initContainer pip distinction: narrative note clarifies why K8s pods still use pip"

requirements-completed: [FLAT-01, FLAT-02, FLAT-03]

# Metrics
duration: 15min
completed: 2026-04-24
---

# Phase 02.1 Plan 01: Flatten Workspace and Switch to uv Summary

**Surgical 3-file edit: replace llmops-project/lab-01/ sub-dirs with flat llmops-project/ paths and substitute pip install with uv pip install --system in student-facing steps**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-24T00:00:00Z
- **Completed:** 2026-04-24T00:15:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Flattened all student workspace paths in labs 01, 02, and 03 — `llmops-project/lab-01/` -> `llmops-project/` (both macOS/Unix forward-slash and Windows backslash variants)
- Replaced student-facing `pip install` with `uv pip install --system` in labs 01 and 02
- Added uv installation instructions (Astral installer for macOS and Windows) as an admonition block in lab-01 before Step 2
- Added parenthetical clarification in lab-02 that the K8s initContainer uses pip (not uv) to avoid student confusion
- Docusaurus build succeeds with zero errors; all Tabs/TabItem tags balanced

## Task Commits

Each task was committed atomically:

1. **Task 1: Flatten workspace paths and switch pip to uv in all 3 lab guides** - `796daf2` (feat)
2. **Task 2: Cross-file verification sweep and stale reference cleanup** - no new commit (verification-only, no file changes needed — all sweeps passed on first check)

**Plan metadata:** TBD (docs commit)

## Files Created/Modified

- `course-content/docs/labs/lab-01-synthetic-data.md` — Flattened mkdir/cp/cd paths; added uv install admonition; replaced pip install with uv pip install --system
- `course-content/docs/labs/lab-02-rag-retriever.md` — Flattened narrative, cp, xcopy, ls, and cd paths; replaced pip install with uv pip install --system; added initContainer pip clarification note
- `course-content/docs/labs/lab-03-finetuning.md` — Updated cross-lab data verification path from llmops-project/lab-01/datasets/ to llmops-project/datasets/

## Decisions Made

- Used `uv pip install --system` (with `--system` flag) rather than plain `uv pip install` — uv requires a virtual environment by default and `--system` is the cleanest workshop-safe flag
- Added uv install instructions to lab-01 (where students first use it) rather than to preflight scripts — preflight script changes belong to the lab-00 authoring phase
- K8s initContainer `pip install` left completely unchanged in `course-code/` files and in YAML code blocks — those are inside-pod commands where uv is not available

## Deviations from Plan

None — plan executed exactly as written. The RESEARCH.md path substitution rules matched the actual file content perfectly. All 8 forward-slash substitutions and 4 backslash substitutions applied cleanly on first attempt.

## Issues Encountered

None — all verification checks passed on first run. Docusaurus build succeeded immediately.

## Known Stubs

None — all changes are concrete path/command substitutions. No placeholder text or empty data sources introduced.

## Next Phase Readiness

- All lab guides (01-03) now use consistent flat workspace paths matching the K8s layer (`/mnt/project/` maps to `llmops-project/`)
- Students following labs will now put files in the correct location for the K8s training job to find them
- Phase 02.1 requirement FLAT-01, FLAT-02, FLAT-03 fully satisfied
- No blockers for subsequent lab authoring phases

---
*Phase: 02.1-flatten-workspace-and-switch-to-uv*
*Completed: 2026-04-24*
