---
phase: 02.1-flatten-workspace-and-switch-to-uv
verified: 2026-04-24T00:00:00Z
status: passed
score: 5/5 must-haves verified
re_verification: false
---

# Phase 02.1: Flatten Workspace and Switch to uv — Verification Report

**Phase Goal:** Flatten the llmops-project/ workspace (remove per-lab subdirectories) and switch student-facing pip commands to uv across all lab guides
**Verified:** 2026-04-24
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Requirements Coverage Note

FLAT-01, FLAT-02, and FLAT-03 are **ad-hoc requirement IDs defined in the PLAN's `<objective>` block** for this urgent insertion phase. They do not appear in REQUIREMENTS.md's traceability table by design — the PLAN explicitly labels them "Ad-hoc requirement IDs for this urgent insertion." This is not an orphaned requirement gap. No REQUIREMENTS.md entries map to phase 02.1 in the traceability table.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Lab guides reference `llmops-project/` as a flat workspace (no `lab-01/` sub-directory) | VERIFIED | Zero matches for `llmops-project/lab-0[0-9]` (forward-slash) across all 14 lab files; zero matches for backslash variant; grep exit code 2 (no matches) confirms clean sweep |
| 2 | Student-facing pip install commands are replaced with `uv pip install --system` | VERIFIED | lab-01 line 138: `uv pip install --system -r tools/requirements.txt`; lab-02 line 123: `uv pip install --system -r rag/requirements.txt`; zero bare `pip install` lines in student-facing positions in labs 01-03 |
| 3 | K8s manifest pip install commands inside YAML code blocks and course-code/ files are NOT changed | VERIFIED | `git diff --name-only` shows zero course-code/ files modified; lab-02 lines 87 and 164 preserve `pip install` narrative for initContainer context |
| 4 | Cross-lab data dependency paths are consistent (lab-03 references `llmops-project/datasets/train/`) | VERIFIED | lab-03 line 109: `ls llmops-project/datasets/train/dental_chat.jsonl`; no `llmops-project/lab-01/` in lab-03 (grep exit 2) |
| 5 | Lab 01 includes a uv installation prerequisite note before the first uv command | VERIFIED | lab-01 lines 115-132: `:::info Install uv (one-time setup)` admonition block with `astral.sh/uv/install.sh` (macOS) and `astral.sh/uv/install.ps1` (Windows) tabs, placed before Step 2 at line 134 |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `course-content/docs/labs/lab-01-synthetic-data.md` | Flattened workspace paths + uv commands | VERIFIED | Contains `uv pip install --system` (line 138); `mkdir -p llmops-project` (line 102); `cp -r … llmops-project/` (line 103); `cd llmops-project` (lines 137, 160); `Artifacts created in \`llmops-project/\`` (line 237); uv install admonition at lines 115-132 |
| `course-content/docs/labs/lab-02-rag-retriever.md` | Flattened workspace paths + uv commands | VERIFIED | Contains `uv pip install --system -r rag/requirements.txt` (line 123); `llmops-project/datasets/clinic/` in narrative (line 95); `cp -r … llmops-project/rag/` (line 100); `ls llmops-project/rag/` (line 113); `cd llmops-project` (line 122); initContainer pip clarification note (line 87) |
| `course-content/docs/labs/lab-03-finetuning.md` | Updated cross-lab data reference to flat path | VERIFIED | Contains `ls llmops-project/datasets/train/dental_chat.jsonl` (line 109); no `llmops-project/lab-01/` references found |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `lab-01-synthetic-data.md` | `lab-02-rag-retriever.md` | `datasets/clinic/` path reference | VERIFIED | lab-02 line 95: "The clinic data files from Lab 01 are already in `llmops-project/datasets/clinic/`" — flat path, no `lab-01/` sub-directory |
| `lab-01-synthetic-data.md` | `lab-03-finetuning.md` | `datasets/train/dental_chat.jsonl` path reference | VERIFIED | lab-03 line 109: `ls llmops-project/datasets/train/dental_chat.jsonl` — flat path; also referenced at line 144 inside K8s job as `/mnt/project/datasets/train/dental_chat.jsonl` (correct K8s mount path) |

---

### Data-Flow Trace (Level 4)

Not applicable — this phase modifies documentation (lab guide markdown files), not runtime code. No data-flow tracing is required for documentation-only artifacts.

---

### Behavioral Spot-Checks

| Behavior | Check | Result | Status |
|----------|-------|--------|--------|
| Docusaurus build succeeds | SUMMARY claims `npm run build` succeeded | PASS (per SUMMARY; build not re-run — no code changes since commit 796daf2) | PASS |
| Tabs/TabItem tags balanced in lab-01 | Python regex count: Tabs 3/3, TabItem 6/6 | Balanced | PASS |
| Tabs/TabItem tags balanced in lab-02 | Python regex count: Tabs 1/1, TabItem 2/2 | Balanced | PASS |
| Commit 796daf2 exists | `git log --oneline 796daf2` | `796daf2 feat(02.1-01): flatten workspace paths and switch pip to uv in lab guides` | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| FLAT-01 | 02.1-01-PLAN.md | All lab guide workspace paths use flat `llmops-project/` (no per-lab sub-directories) | SATISFIED | Zero `llmops-project/lab-0[0-9]` matches across all 14 lab files (both slash variants); flat paths confirmed in labs 01, 02, and 03 |
| FLAT-02 | 02.1-01-PLAN.md | Student-facing pip commands replaced with `uv pip install --system` (K8s manifest pip untouched) | SATISFIED | `uv pip install --system` in labs 01 and 02; no bare `pip install` in student-facing code blocks; initContainer pip preserved in lab-02 narrative |
| FLAT-03 | 02.1-01-PLAN.md | Lab 01 includes uv installation prerequisite before first uv command | SATISFIED | `:::info Install uv (one-time setup)` admonition block at lines 115-132 in lab-01, preceding Step 2 (Install Python dependencies) at line 134 |

**Orphaned requirements check:** REQUIREMENTS.md traceability table does not reference phase 02.1. FLAT-01/02/03 are ad-hoc IDs scoped to this insertion phase only — no orphaned entries.

---

### Anti-Patterns Found

No anti-patterns detected across the three modified files:
- No TODO/FIXME/placeholder comments introduced
- No empty implementations
- No hardcoded empty data structures in student-visible paths
- No console.log-only stubs
- K8s YAML and course-code/ files untouched (confirmed via git diff)

---

### Human Verification Required

None. All acceptance criteria are verifiable programmatically for a documentation-only phase.

Optional manual check (not blocking):

**Test:** Follow Lab 01 steps on a clean machine — run `mkdir -p llmops-project`, `cp -r course-code/labs/lab-01/solution/* llmops-project/`, install uv, then `uv pip install --system -r tools/requirements.txt` and `python tools/synth_data.py`
**Expected:** 164 examples generated to `llmops-project/datasets/train/dental_chat.jsonl` with no path errors
**Why human:** Requires an actual laptop with Docker Desktop — cannot simulate in CI

---

## Gaps Summary

No gaps. All 5 observable truths are verified, all 3 artifacts pass all levels (exists, substantive, wired), both key links are confirmed, and all 3 ad-hoc requirements are satisfied. The commit exists in git history. No collateral damage to course-code/ files.

---

_Verified: 2026-04-24_
_Verifier: Claude (gsd-verifier)_
