---
phase: 01-curriculum-migration-to-303-agentops
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - course-content/package.json
  - course-content/docusaurus.config.ts
  - course-content/sidebars.ts
  - CHANGELOG.md
  - README.md
autonomous: true
requirements:
  - MIGRATE-05
must_haves:
  truths:
    - "course-content/docusaurus.config.ts has title 'LLMOps with Kubernetes' (not 'LLMOps & AgentOps with Kubernetes') in both title field and themeConfig.navbar.logo.alt"
    - "course-content/package.json lists '@docusaurus/plugin-client-redirects' as a dependency"
    - "course-content/docusaurus.config.ts includes the redirects plugin configuration with redirect entries for every removed lab URL (lab-07 through lab-13)"
    - "CHANGELOG.md has a new '## v1.0.0 — split from v0.19.0' entry above the existing [v0.1.0] entry"
    - "README.md exists at the repo root with a 'Which version are you on?' section linking v0.19.0 tag, v1.0.0 main, and 303-agentops"
    - "No lab content files are deleted yet — that is plan 01-04's job (deletion-last per D-10)"
  artifacts:
    - path: "course-content/package.json"
      provides: "@docusaurus/plugin-client-redirects dependency added"
      contains: "@docusaurus/plugin-client-redirects"
    - path: "course-content/docusaurus.config.ts"
      provides: "Title renamed + redirects plugin registered + redirect map for lab-07..13 URLs"
      exports: ["plugin-client-redirects config block", "title: 'LLMOps with Kubernetes'"]
    - path: "course-content/sidebars.ts"
      provides: "lab-07 through lab-13 entries removed from sidebar"
    - path: "CHANGELOG.md"
      provides: "v1.0.0 split entry above v0.1.0"
    - path: "README.md"
      provides: "Repo-root 'which version' section linking tag + main + 303-agentops"
  key_links:
    - from: "docusaurus.config.ts redirects plugin"
      to: "https://github.com/schoolofdevops/303-agentops"
      via: "redirect destination for all removed lab URLs"
      pattern: "@docusaurus/plugin-client-redirects"
    - from: "CHANGELOG.md v1.0.0 entry"
      to: "v0.19.0 git tag"
      via: "explicit tag link in changelog entry"
      pattern: "v0.19.0"
    - from: "README.md 'Which version are you on?'"
      to: "https://github.com/schoolofdevops/302-llmops/tree/v0.19.0"
      via: "version selector section"
      pattern: "Which version are you on"
---

<objective>
Add `@docusaurus/plugin-client-redirects` to the 302-llmops Docusaurus site so that every removed AgentOps lab URL (lab-07 through lab-13) redirects to the 303-agentops repo landing page. Rename the site title from 'LLMOps & AgentOps with Kubernetes' to 'LLMOps with Kubernetes'. Remove lab-07..13 entries from sidebars.ts. Write CHANGELOG.md v1.0.0 split entry. Write repo-root README.md with version selector.

Purpose: Plan 01-02 ensured 303-agentops exists and has the content. This plan prepares 302-llmops BEFORE deletion: redirects so existing links do not 404, title so the site reflects the new scope, CHANGELOG + README so existing learners understand the split. Deletion happens in plan 01-04 AFTER this plan commits — per D-10 order.

Output: 5 files committed to 302-llmops main. Docusaurus site will build without errors after plan 01-04 deletes the lab markdown files (the redirects plugin covers them). Implements D-05, D-06, D-07, D-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v0.19.0-phases/01-curriculum-migration-to-303-agentops/01-CONTEXT.md
@.planning/milestones/v0.19.0-phases/01-curriculum-migration-to-303-agentops/01-02-SUMMARY.md
@course-content/docusaurus.config.ts
@course-content/sidebars.ts
@course-content/package.json
@CHANGELOG.md

<verified_facts>
- course-content/docusaurus.config.ts line 6: `title: 'LLMOps & AgentOps with Kubernetes'` — needs to become `'LLMOps with Kubernetes'` (D-06).
- course-content/docusaurus.config.ts line 51: `navbar.title: 'LLMOps'` — already short; keep as-is.
- course-content/docusaurus.config.ts line 53: `navbar.logo.alt: 'LLMOps & AgentOps Logo'` — update to `'LLMOps Logo'` (D-06).
- course-content/docusaurus.config.ts does NOT contain `@docusaurus/plugin-client-redirects` — needs to be added (D-05).
- course-content/docusaurus.config.ts has `onBrokenLinks: 'throw'` at line 21 — must stay unchanged.
- course-content/package.json version: 3.10.0 for all @docusaurus/* packages — install `@docusaurus/plugin-client-redirects@3.10.0` to match.
- course-content/sidebars.ts lists lab-07-agent-core through lab-13-capstone in the Labs category — these 7 entries need removal.
- CHANGELOG.md current top entry: `## [v0.1.0] — 2026-05-02` — new v1.0.0 entry goes ABOVE this.
- README.md does NOT exist at repo root — needs to be created.
- Redirect target per D-05 default: single landing page `https://github.com/schoolofdevops/303-agentops`.
- Lab URL pattern in Docusaurus with `routeBasePath: '/docs'`: lab-07-agent-core renders at `/docs/labs/lab-07-agent-core`.
</verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @docusaurus/plugin-client-redirects dependency and update package.json</name>
  <files>course-content/package.json</files>
  <read_first>
    - course-content/package.json (current dependencies, note version 3.10.0 for all @docusaurus/* packages)
    - .planning/milestones/v0.19.0-phases/01-curriculum-migration-to-303-agentops/01-CONTEXT.md (D-05 — add @docusaurus/plugin-client-redirects)
  </read_first>
  <action>
Read `course-content/package.json` first (mandatory before any edit). Then use the Edit tool to add `"@docusaurus/plugin-client-redirects": "3.10.0"` to the `dependencies` object. Match the version to the other @docusaurus/* packages (3.10.0). Alphabetical order within dependencies is preferred but not required.

After editing package.json, install the new dependency:
```bash
cd /Users/gshah/courses/llmops/course-content && npm install
```

This updates `node_modules/` locally (not committed — node_modules is in .gitignore).

IMPORTANT: Do NOT run `npm install @docusaurus/plugin-client-redirects` with the add flag (that changes the version resolution strategy). Instead:
1. Edit package.json manually to add the exact version string.
2. Run `npm install` to lock and install.
  </action>
  <verify>
    <automated>grep -q '"@docusaurus/plugin-client-redirects": "3.10.0"' /Users/gshah/courses/llmops/course-content/package.json && test -d /Users/gshah/courses/llmops/course-content/node_modules/@docusaurus/plugin-client-redirects</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"@docusaurus/plugin-client-redirects"' course-content/package.json` exits 0 and returns the `3.10.0` version pin
    - `test -d course-content/node_modules/@docusaurus/plugin-client-redirects` exits 0 (package installed)
    - `npm install` in course-content/ exits 0 (no peer dep errors blocking install)
    - No other dependencies changed in package.json
  </acceptance_criteria>
  <done>@docusaurus/plugin-client-redirects@3.10.0 added to package.json and installed in node_modules.</done>
</task>

<task type="auto">
  <name>Task 2: Update docusaurus.config.ts — title rename, logo alt, and redirects plugin</name>
  <files>course-content/docusaurus.config.ts</files>
  <read_first>
    - course-content/docusaurus.config.ts (READ THE FULL FILE before editing — use the Read tool)
    - .planning/milestones/v0.19.0-phases/01-curriculum-migration-to-303-agentops/01-CONTEXT.md (D-05 redirect map, D-06 title rename)
    - .planning/research/PITFALLS.md §"Pitfall 8" (redirect plugin is the fix for removed lab URLs)
  </read_first>
  <action>
Read `course-content/docusaurus.config.ts` with the Read tool first. Then make exactly three sets of changes using the Edit tool:

**Change 1: Title rename (D-06)**
- Line 6: change `title: 'LLMOps & AgentOps with Kubernetes'` → `title: 'LLMOps with Kubernetes'`
- Line ~53: change `logo.alt: 'LLMOps & AgentOps Logo'` → `logo.alt: 'LLMOps Logo'`
- tagline on line 7 (`'From RAG to production agents on Kubernetes'`) → change to `'Production LLM serving on Kubernetes'`

**Change 2: Add the redirects plugin (D-05)**
Add the following plugins array to the config object. Place it BEFORE the `presets` key:

```typescript
  plugins: [
    [
      '@docusaurus/plugin-client-redirects',
      {
        redirects: [
          {
            from: '/docs/labs/lab-07-agent-core',
            to: 'https://github.com/schoolofdevops/303-agentops',
          },
          {
            from: '/docs/labs/lab-08-agent-sandbox',
            to: 'https://github.com/schoolofdevops/303-agentops',
          },
          {
            from: '/docs/labs/lab-09-observability',
            to: 'https://github.com/schoolofdevops/303-agentops',
          },
          {
            from: '/docs/labs/lab-10-autoscaling',
            to: 'https://github.com/schoolofdevops/303-agentops',
          },
          {
            from: '/docs/labs/lab-11-gitops',
            to: 'https://github.com/schoolofdevops/303-agentops',
          },
          {
            from: '/docs/labs/lab-12-pipelines',
            to: 'https://github.com/schoolofdevops/303-agentops',
          },
          {
            from: '/docs/labs/lab-13-capstone',
            to: 'https://github.com/schoolofdevops/303-agentops',
          },
        ],
      },
    ],
  ],
```

NOTE: `@docusaurus/plugin-client-redirects` only generates redirects for static paths. It does NOT redirect external URLs as `to` in standard Docusaurus build — but it DOES work for the case where `from` is an internal path and `to` is an external URL (it creates a redirect HTML stub). Verify the build passes with this config.

**Change 3: Keep onBrokenLinks: 'throw' unchanged** — Do not touch this line.

After editing, run a quick TypeScript check to confirm no syntax errors:
```bash
cd /Users/gshah/courses/llmops/course-content && npx tsc --noEmit 2>&1 | head -20
# Expected: no errors (or only pre-existing non-blocking warnings)
```
  </action>
  <verify>
    <automated>
      grep -q "title: 'LLMOps with Kubernetes'" /Users/gshah/courses/llmops/course-content/docusaurus.config.ts &&
      grep -q "@docusaurus/plugin-client-redirects" /Users/gshah/courses/llmops/course-content/docusaurus.config.ts &&
      grep -q "lab-07-agent-core" /Users/gshah/courses/llmops/course-content/docusaurus.config.ts &&
      grep -q "lab-13-capstone" /Users/gshah/courses/llmops/course-content/docusaurus.config.ts &&
      grep -q "onBrokenLinks: 'throw'" /Users/gshah/courses/llmops/course-content/docusaurus.config.ts &&
      grep -vq "LLMOps & AgentOps with Kubernetes" /Users/gshah/courses/llmops/course-content/docusaurus.config.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "title: 'LLMOps with Kubernetes'" course-content/docusaurus.config.ts` exits 0
    - `grep "LLMOps & AgentOps with Kubernetes" course-content/docusaurus.config.ts` exits NON-ZERO (old title gone)
    - `grep "'@docusaurus/plugin-client-redirects'" course-content/docusaurus.config.ts` exits 0
    - All 7 redirect `from` entries present: lab-07-agent-core through lab-13-capstone
    - All 7 redirect `to` entries point to `https://github.com/schoolofdevops/303-agentops`
    - `grep "onBrokenLinks: 'throw'" course-content/docusaurus.config.ts` exits 0 (preserved)
    - TypeScript check (`npx tsc --noEmit`) exits 0 or only pre-existing warnings (no new errors)
  </acceptance_criteria>
  <done>docusaurus.config.ts updated: title renamed to 'LLMOps with Kubernetes', redirects plugin registered with 7 redirect entries for removed lab URLs, onBrokenLinks unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Remove lab-07..13 entries from sidebars.ts, write CHANGELOG.md entry, write repo-root README.md</name>
  <files>course-content/sidebars.ts, CHANGELOG.md, README.md</files>
  <read_first>
    - course-content/sidebars.ts (READ FULL FILE before editing — verify lab-07..lab-13 are in the Labs category items array)
    - CHANGELOG.md (READ FULL FILE before editing — new entry goes ABOVE existing [v0.1.0] entry)
    - .planning/milestones/v0.19.0-phases/01-curriculum-migration-to-303-agentops/01-CONTEXT.md (D-07 CHANGELOG content, D-08 README content)
  </read_first>
  <action>
Three sub-actions in sequence:

**Sub-action A: sidebars.ts — remove lab-07..13 sidebar entries**

Read `course-content/sidebars.ts`. Use the Edit tool to remove these 7 entries from the `Labs` category items array:
```
'labs/lab-07-agent-core',
'labs/lab-08-agent-sandbox',
'labs/lab-09-observability',
'labs/lab-10-autoscaling',
'labs/lab-11-gitops',
'labs/lab-12-pipelines',
'labs/lab-13-capstone',
```
The Labs category items should end after `'labs/lab-06-web-ui'`.

After the edit, the Labs category items list should be:
```typescript
items: [
  'labs/lab-00-cluster-setup',
  'labs/lab-01-synthetic-data',
  'labs/lab-02-rag-retriever',
  'labs/lab-03-finetuning',
  'labs/lab-04-model-packaging',
  'labs/lab-05-model-serving',
  'labs/lab-06-web-ui',
],
```

**Sub-action B: CHANGELOG.md — prepend v1.0.0 entry**

Read `CHANGELOG.md`. Use the Edit tool to insert the following NEW entry immediately ABOVE the existing `## [v0.1.0] — 2026-05-02` line:

```markdown
## [v1.0.0] — 2026-05-07

### Changed

- **Course split:** AgentOps content (Labs 07-13: Hermes Agent, MCP tools, Kubernetes Agent Sandbox, OTEL/Tempo observability, cost middleware, guardrails, eval gate, capstone) removed from this repo and migrated to the companion course `schoolofdevops/303-agentops`.
- **Course title renamed:** "LLMOps & AgentOps with Kubernetes" → "LLMOps with Kubernetes" — this course is now focused solely on LLMOps.
- **Lab URLs redirected:** `/docs/labs/lab-07-*` through `/docs/labs/lab-13-*` now redirect to `https://github.com/schoolofdevops/303-agentops` via `@docusaurus/plugin-client-redirects`.

### Removed

- Lab 07 (Agent Core / Hermes Agent)
- Lab 08 (Agent Sandbox)
- Lab 09 (Agent Observability / OTEL Tempo)
- Lab 10 (Agent Autoscaling)
- Lab 11 (Agent GitOps)
- Lab 12 (Agent Training Pipeline with Eval Gate)
- Lab 13 (Capstone)

### For v0.19.0 students

If you were following the combined LLMOps + AgentOps course, your content is preserved at:

- **Git tag:** https://github.com/schoolofdevops/302-llmops/tree/v0.19.0 (SHA `3c4e0b120efd93a147d61f916a943e6a775ec717`)
- **Maintenance branch:** https://github.com/schoolofdevops/302-llmops/tree/v0.19.x (receives bug fixes)
- **AgentOps content:** https://github.com/schoolofdevops/303-agentops

---
```

**Sub-action C: README.md — create at repo root**

Use the Write tool to create `/Users/gshah/courses/llmops/README.md` with exactly this content:

```markdown
# LLMOps with Kubernetes

Hands-on course teaching production LLM serving on Kubernetes — from synthetic data and LoRA fine-tuning through model packaging, three serving patterns (plain vLLM, KServe, vLLM Router), observability, autoscaling, and GitOps.

**Companion course (AgentOps):** https://github.com/schoolofdevops/303-agentops

## Which version are you on?

| Version | Branch/Tag | Content |
|---------|------------|---------|
| **v1.0.0+** (current) | `main` | LLMOps-only: Labs 00-06 + new serving patterns. AgentOps removed. |
| **v0.19.0** (archived) | [`v0.19.0` tag](https://github.com/schoolofdevops/302-llmops/tree/v0.19.0) | Combined LLMOps + AgentOps (Labs 00-13) — 3-day workshop |
| **v0.19.x** (maintenance) | [`v0.19.x` branch](https://github.com/schoolofdevops/302-llmops/tree/v0.19.x) | Bug fixes for v0.19.0 students during the transition window |
| **AgentOps** | [303-agentops](https://github.com/schoolofdevops/303-agentops) | Hermes Agent, MCP tools, Kubernetes Agent Sandbox — companion course |

**If you enrolled in the Udemy course on the combined LLMOps + AgentOps workshop:** pin your fork to the `v0.19.0` tag or `v0.19.x` branch. The v0.19.0 content is permanently available there.

**If you want the latest LLMOps-only course:** you are on the right branch (`main`). v1.0.0 covers multiple serving patterns (plain vLLM Deployment, KServe InferenceService, vLLM Router), disk-based model loading, and a full Argo Workflows training pipeline.

**If you want AgentOps content:** https://github.com/schoolofdevops/303-agentops — builds on this course as a prerequisite.

## Course Site

https://llmops.schoolofdevops.com (Docusaurus — built from `course-content/`)

## Quick Start

See Lab 00 in the course site for cluster setup. Requires Docker Desktop + KIND + 16GB RAM.
```

After writing README.md, verify all three files changed correctly:
```bash
# sidebars check
grep -c "lab-07\|lab-08\|lab-09\|lab-10\|lab-11\|lab-12\|lab-13" /Users/gshah/courses/llmops/course-content/sidebars.ts
# Expected: 0 (no agent lab references remain)

# CHANGELOG check
grep -q "v1.0.0 — split from v0.19.0\|v1.0.0.*2026-05-07" /Users/gshah/courses/llmops/CHANGELOG.md && echo "PASS"

# README check
grep -q "Which version are you on" /Users/gshah/courses/llmops/README.md && echo "PASS"
```
  </action>
  <verify>
    <automated>
      grep -c "lab-07\|lab-08\|lab-09\|lab-10\|lab-11\|lab-12\|lab-13" /Users/gshah/courses/llmops/course-content/sidebars.ts | grep -q "^0$" &&
      grep -q "v1.0.0" /Users/gshah/courses/llmops/CHANGELOG.md &&
      grep -q "Which version are you on" /Users/gshah/courses/llmops/README.md &&
      grep -q "303-agentops" /Users/gshah/courses/llmops/README.md &&
      grep -q "v0.19.0" /Users/gshah/courses/llmops/README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lab-07\|lab-08\|lab-09\|lab-10\|lab-11\|lab-12\|lab-13" course-content/sidebars.ts` returns `0` (all 7 agent lab entries removed)
    - `grep "lab-06-web-ui" course-content/sidebars.ts` exits 0 (last LLMOps lab still present)
    - `grep "v1.0.0" CHANGELOG.md` exits 0 (new entry present)
    - `grep "v0.19.0.*SHA\|3c4e0b120efd93a147d61f916a943e6a775ec717" CHANGELOG.md` exits 0 (SHA in CHANGELOG)
    - `grep "303-agentops" CHANGELOG.md` exits 0 (companion repo linked)
    - `test -f README.md` exits 0 (repo-root README created)
    - `grep -q "Which version are you on" README.md` exits 0
    - `grep -q "v0.19.0" README.md` exits 0 (tag linked)
    - `grep -q "303-agentops" README.md` exits 0 (companion repo linked)
    - `grep -q "v0.19.x" README.md` exits 0 (maintenance branch linked)
  </acceptance_criteria>
  <done>sidebars.ts pruned to Labs 00-06 only. CHANGELOG.md has v1.0.0 entry. README.md created with version selector table.</done>
</task>

<task type="auto">
  <name>Task 4: Commit all 5 changes to 302-llmops main</name>
  <files>course-content/package.json, course-content/docusaurus.config.ts, course-content/sidebars.ts, CHANGELOG.md, README.md</files>
  <read_first>
    - .planning/milestones/v0.19.0-phases/01-curriculum-migration-to-303-agentops/01-CONTEXT.md (D-07 CHANGELOG, D-08 README, D-10 step 6 — this commit is step 6; deletion is step 7 in plan 01-04)
  </read_first>
  <action>
Stage and commit the 5 modified/created files to 302-llmops main. Do NOT stage anything else (especially not the new local 303-agentops clone at `/Users/gshah/courses/303-agentops/` — that is a sibling directory, not in the 302-llmops working tree).

```bash
cd /Users/gshah/courses/llmops

# 1. Verify exactly the right 5 files are dirty (no unexpected changes)
git status --short
# Expected lines: M course-content/docusaurus.config.ts
#                 M course-content/package.json
#                 M course-content/sidebars.ts
#                 M CHANGELOG.md
#                 ?? README.md
# (package-lock.json may also be modified by npm install — that is fine to stage)

# 2. Stage exactly these files
git add course-content/package.json course-content/docusaurus.config.ts course-content/sidebars.ts CHANGELOG.md README.md

# Also stage package-lock.json if npm install modified it (expected)
git add course-content/package-lock.json 2>/dev/null || true

# 3. Verify the staged index
git diff --cached --name-only
# Expected: CHANGELOG.md, README.md, course-content/docusaurus.config.ts,
#           course-content/package.json, course-content/sidebars.ts
#           (+ course-content/package-lock.json if present)

# 4. Commit
git commit -m "chore(migrate): add redirects, rename title, update CHANGELOG + README for 303-agentops split

- Add @docusaurus/plugin-client-redirects@3.10.0 (package.json + docusaurus.config.ts)
- Register 7 redirect entries: /docs/labs/lab-07..13 -> https://github.com/schoolofdevops/303-agentops
- Rename site title: 'LLMOps & AgentOps with Kubernetes' -> 'LLMOps with Kubernetes' (D-06)
- Remove lab-07..13 from sidebars.ts (sidebar cleanup before deletion in next commit)
- CHANGELOG.md: add v1.0.0 entry documenting the split with v0.19.0 SHA pointer (D-07)
- README.md: create at repo root with 'Which version are you on?' section (D-08)

Implements D-05, D-06, D-07, D-08 per 01-CONTEXT.md.
D-10 order: deletion in next commit (plan 01-04) after this commit is on main."

# 5. Push to origin main
git push origin main

# 6. Verify commit landed on origin
git log --oneline -1
git ls-remote --heads origin main | awk '{print $1}'
# Both should show the same SHA
```

If the commit includes unexpected files (e.g., node_modules diff), STOP and surface — do not commit node_modules.
  </action>
  <verify>
    <automated>
      git -C /Users/gshah/courses/llmops log --oneline -1 | grep -q "chore(migrate):" &&
      grep -q "'@docusaurus/plugin-client-redirects'" /Users/gshah/courses/llmops/course-content/docusaurus.config.ts &&
      grep -q "title: 'LLMOps with Kubernetes'" /Users/gshah/courses/llmops/course-content/docusaurus.config.ts &&
      grep -q "v1.0.0" /Users/gshah/courses/llmops/CHANGELOG.md &&
      test -f /Users/gshah/courses/llmops/README.md &&
      git -C /Users/gshah/courses/llmops log --oneline origin/main -1 | grep -q "chore(migrate):"
    </automated>
  </verify>
  <acceptance_criteria>
    - `git -C /Users/gshah/courses/llmops log --oneline -1` shows the `chore(migrate):` commit
    - `git -C /Users/gshah/courses/llmops log --oneline origin/main -1` shows the same commit SHA (pushed)
    - `git -C /Users/gshah/courses/llmops status --porcelain` empty after commit (no lingering dirty files)
    - `git -C /Users/gshah/courses/llmops diff HEAD~1 HEAD --name-only` shows exactly the expected 5-6 files (no node_modules)
    - The commit does NOT contain any deletion of lab-07..13 markdown files (that is plan 01-04's job)
    - The commit does NOT include `/Users/gshah/courses/303-agentops/` content (sibling dir, not in 302 tree)
  </acceptance_criteria>
  <done>5-file commit pushed to 302-llmops main. Redirects registered, title renamed, CHANGELOG updated, README created. Lab files NOT yet deleted (deletion-last per D-10 — that is plan 01-04).</done>
</task>

</tasks>

<verification>
End-to-end check after all 4 tasks pass:

```bash
cd /Users/gshah/courses/llmops

# Title renamed
grep -q "title: 'LLMOps with Kubernetes'" course-content/docusaurus.config.ts

# Redirects plugin registered with all 7 entries
grep -q "@docusaurus/plugin-client-redirects" course-content/docusaurus.config.ts
grep -c "from: '/docs/labs/lab-" course-content/docusaurus.config.ts   # expect: 7

# onBrokenLinks preserved
grep -q "onBrokenLinks: 'throw'" course-content/docusaurus.config.ts

# sidebars pruned
grep -c "lab-07\|lab-08\|lab-09\|lab-10\|lab-11\|lab-12\|lab-13" course-content/sidebars.ts   # expect: 0

# CHANGELOG entry
grep -q "v1.0.0" CHANGELOG.md
grep -q "3c4e0b120efd93a147d61f916a943e6a775ec717" CHANGELOG.md

# README version selector
grep -q "Which version are you on" README.md
grep -q "303-agentops" README.md

# Lab markdown files STILL PRESENT (not deleted yet)
test -f course-content/docs/labs/lab-07-agent-core.md && echo "still present — correct"
test -f course-content/docs/labs/lab-13-capstone.md && echo "still present — correct"

# Commit landed on origin
git log --oneline origin/main -1 | grep -q "chore(migrate):"
```
</verification>

<success_criteria>
- [ ] `course-content/package.json` contains `"@docusaurus/plugin-client-redirects": "3.10.0"`
- [ ] `course-content/docusaurus.config.ts` has `title: 'LLMOps with Kubernetes'` (no more & AgentOps)
- [ ] `course-content/docusaurus.config.ts` has all 7 redirect entries (lab-07 through lab-13)
- [ ] `course-content/docusaurus.config.ts` has `onBrokenLinks: 'throw'` (unchanged)
- [ ] `course-content/sidebars.ts` has 0 references to lab-07..13
- [ ] `CHANGELOG.md` has v1.0.0 entry with v0.19.0 SHA and 303-agentops link
- [ ] `README.md` exists at repo root with "Which version are you on?" section
- [ ] All changes committed to 302-llmops main and pushed to origin
- [ ] Lab-07..13 markdown files are STILL present (deletion in plan 01-04)
- [ ] MIGRATE-05 partially delivered (redirects + title + CHANGELOG + README); plan 01-04 completes MIGRATE-05 by deleting files and running the build check
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.19.0-phases/01-curriculum-migration-to-303-agentops/01-03-SUMMARY.md` recording:
- SHA of the `chore(migrate):` commit on 302-llmops main
- Confirmation that redirects plugin covers lab-07 through lab-13
- Confirmation lab markdown files are still present (not yet deleted)
- Pointer to next plan: 01-04 (delete AgentOps from 302 main + verify Docusaurus build)
</output>
