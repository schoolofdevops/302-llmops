# Lab 08 — K8s Agent Sandbox (Starter)

Follow the [Lab 08 guide](../../../../course-content/docs/labs/lab-08-agent-sandbox.md). Reference `../solution/` only after attempting the manifests yourself.

## First step

```bash
bash ../solution/scripts/verify-sandbox-router-image.sh
bash ../solution/scripts/install-agent-sandbox.sh
```

If the first script reports `port-forward` mode, the lab guide tells you which Router-related step to skip.
