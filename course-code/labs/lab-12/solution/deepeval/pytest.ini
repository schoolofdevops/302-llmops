[pytest]
pythonpath = ..
testpaths = .
# Disable deepeval's own pytest plugin to prevent import conflict
# (our test folder is named 'deepeval/' which shadows the PyPI package on sys.path
#  if __init__.py exists; we avoid __init__.py in this folder to prevent the shadow)
addopts = -p no:deepeval
