apiVersion: v1
kind: Service
metadata:
  name: vllm-smollm2-disk
  namespace: llm-serving
  labels:
    app: vllm-disk
    course: llmops
spec:
  type: NodePort
  selector:
    app: vllm-disk
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    nodePort: 30203
